# 開發日誌 2026-07-22 — v0.4.0：新選單、寄生與睡踢、美術根治

本輪採「Claude 規劃審查＋Codex GPT 5.6-terra 實作」分工。規格書 `SPEC-v0.4.md`、
美術任務三部曲 `ART-TASK.md` → `ART-DIAGNOSIS.md` → `ART-FIX.md` 都在 repo 根目錄。

## 功能
- **玥玥睡覺換圖**：`CHAR_CFG.sleepImage` 通用機制（`src/yueyue-sleep.png`，顯示高 150px 貼地置中）；
  `.sleep-img` 隱藏 #pet 顯示 img，`.sleep:not(.sleep-img)` 保住其他角色的躺平動畫。
- **餵愛心**：`feed(kind)`，love＝❤️、fullness+5／mood+12、不受吃太飽限制、專屬台詞＋愛心噴發。
- **狐狐雙擊 35% 愛心噴發**：`heartBurst()` 6-10 顆 emoji keyframes 上飄淡出，animationend 自清。
- **睡著被踢**（比照小恐龍）：前端 `set_sleeping` 回報 → Rust 全域評估執行緒每 10s，
  同螢幕 250px 內清醒夥伴 8% 機率踢飛；`spawn_pet_physics` 共用 `Phys`＋閂鎖工作區；冷卻 3 分鐘。
- **珍母寄生**：每 20s 12% 機率跳上同螢幕 500px 內夥伴頭頂（y = host.y − 161×scale；161＝腳底 254 −
  頭頂 93），黏 60-180s，點擊提前解除，小拋物線落地，冷卻 5 分鐘。
- **遊戲風選單**：主視窗右鍵改開 `petmenu` 懸浮視窗（300x520，Stardew 木框羊皮紙全 CSS 自繪）；
  夥伴/玩具保留原生選單。**永不自動關閉**（使用者定案），只有 ✕/ESC 會關；所有選項樂觀更新。

## Bug 修復
- **換角色後夥伴消失**：destroy 未完成就同 label build 被 Tauri 拒 → `wait_for_window_gone`
  輪詢（50ms×60）再 build、失敗 500ms 重試（set_companion/set_toy 共用）。
- **玩具被踢到另一螢幕**：物理每 tick 重算 work_area_of，飛越接縫時牆壁跟著搬家 →
  工作區閂鎖（grounded 靜止或拖放後才重取），新增測試 `latched_work_area_keeps_toy_in_bounds`。

## 實測抓到的三個回歸（Codex 產出，審查/實測補救）
1. **主執行緒死鎖**：petmenu 建立寫在 `run_on_main_thread` → 右鍵無選單＋拖曳全滅。
   改 `std::thread::spawn`。審查 checklist：任何 `WebviewWindowBuilder::build()` 必查執行緒。
2. **capabilities 漏白名單**：petmenu 沒進 `capabilities/default.json` → 選單 IPC 全拒。
3. **dpr 競態裁切**：夥伴重建偶發只吃 GetDpiForWindow 的 1.5、漏文字大小 1.25 疊乘
   （360x384 vs 正確 450x480，腳被切）。matchMedia 在註冊前變化會漏接 →
   三種視窗都補 `setTimeout(fitWindow, 600/2000)` 冪等重試。選單同因裁切＝漏做 fit_window 回報。
   選單「沉進工作列」＝定位用了 fit 前的 outer_size → 改 `MENU_W/H × dpr` 計算
   （dpr = window_scale(main)/petSCALE），並把底線 clamp 到主寵視窗底邊（腳底對齊）。

## 美術根治（膠布/玥玥，vtest 驗收全過）
- 膠布 0° 兩處筆畫缺失（被 body 毛色補丁蓋掉）→ 從原圖逐像素補回 `jiaobu-pawR.png`；
  修後 0° 全角色僅剩 1px 差（max 9）。
- 玥玥 (611-612, 333-334) 透明漏點 → 補回 body；**0° 與原圖全零差異**，tail ±1° gap 全 0，
  尾巴 blackline 409-557 → 87。
- 兩角色眼睛從「橢圓蓋＋重畫」改為**原圖眼件工法**（`*-eyeL/R.png` 緊裁、body 烘底色補洞、
  三態同一原眼 scaleY 0.45/0.12），與珍珍/珍母定案一致。
- 手臂端點角 blackline 候選（膠布 205-464、玥玥 161-398）實機條帶人審＝合法部件輪廓掃動
  ＋2-3px 手繪風階梯，非接縫，不做像素手術。
- ⚠ 教訓：`metrics.py` 基準幀 tag 必須命名 `'0'`，否則 fallback 拿中間角度當基準、整組數據作廢。
  Gemini 判官會把「移動部件蓋過毛色」誤報成殘影、把跨幀零差異的眼睛誤報成形狀不一致——
  判官輸出必須配 PIL 像素比對複核。
- Codex 對生圖能力的自我裁決：**不用**——缺損像素可從原圖精確取回，生成必偏離原樣。

## 驗證
- `cargo test` 2/2、`cargo check` 零警告、三個 JS `node --check` 過。
- 視窗實體尺寸以 DPI-aware EnumWindows 實測：pets 450x480、toy 281x225 全對。
- vtest：兩角色全部件角度掃描，指標全低於門檻（除手臂人審合法項）；Gemini 判官
  jiaobu/pawR、yueyue/tail PASS。
- 眼睛三態 PIL 合成目檢通過；眼區跨幀像素差 0。
