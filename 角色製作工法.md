# 角色製作工法（拆件流程）

新角色**一律用原插畫直接拆件，不要手繪重描**——手繪版做不出跟原圖一樣的味道，
已經驗證過兩次失敗。以下是狗狗與狐狐都走過、可重複的完整流程。

## 0. 素材要求

- 一張去背前的角色圖（白底即可），貼紙風：粗黑輪廓、色塊填色
- 部件之間最好有「輪廓線」或「暗色區」當切割藏縫處

## 1. 清圖（Python + PIL）

```
1. 洪水填充去背：從四邊開始 BFS，只移除「與邊緣相連」的近白像素
   （threshold r,g,b > 235）——角色身上的白色因為被輪廓包住不會被誤刪
2. 特定色移除：例如粉紅裝飾花（r>215, 130<g<210, 150<b<220, r-g>30）
3. 連通元件分析：只保留最大元件，清掉飄在外面的殘渣（花的白邊等）
4. 去白邊 defringe：邊界上（四鄰有透明）且 min(r,g,b)>165 的像素逐圈剝除，
   跑 5 輪。不做這步縮小後會有一圈白毛邊
```

## 1.5 尺寸基準（2026-07-27 定案：全部以熱狗狗狗為尺）

**規則：一張畫布上的角色維持彼此的相對身高，整體用一個全域倍率錨定到狗。**

舊做法是「每隻角色各自解方程式，讓頭皮線都落在 CSS y=93」——那會把畫師刻意畫出來的
身高差**全部壓平**（龍跟鸚鵡一樣高）。新素材（`熱狗訂單.psd`）17 個群組畫在同一張畫布上，
相對大小是有意義的，所以改成單一倍率換算。

**倍率怎麼定＝描邊粗細法**。貼紙風的黑色外框筆刷寬度是跨圖最可靠的比例尺：

```
1. 對每張圖量「外剪影黑描邊」的水平厚度中位數
   （只採邊界近乎垂直的列：|x0(y) - x0(y±2)| <= 1，避免斜邊把掃描長度灌水）
2. 狗的站姿原畫描邊 = 19 art px，CSS 倍率 161/346 → 實機描邊 8.84 CSS px
3. 新 PSD 全 17 群組描邊 = 25~28 px（中位數 26.5，離散 ±6% → 確認同一比例）
4. K = 8.84 / 26.5 = 0.3336 CSS px per PSD px
```

**一定要用第二個獨立指標交叉驗證**：把「現役站姿狗」與「PSD 睡姿狗」各自用自己的倍率
縮到實機尺度並排——同一隻狗的**頭必須一樣大**。實測吻合，K 才可採用。
（只信描邊法會翻車：舊 `yueyue-sleep.png` 的描邊相對角色特別細，單用描邊法會算出
318px 的荒謬高度。描邊法只在**同一張畫布內**成立，跨畫布必須另找不變量。）

實機高度（= PSD 高 × 0.3336）：

| 角色 | 站姿 | 睡姿 |
|---|---|---|
| 熱狗狗狗 | 161（基準） | 90.7 |
| 女僕狐狐 | 184 | 103.1 |
| 采華（龍） | 155 | 86.4 |
| 羊咩 | 162 | 100.1 |
| 月月 2.0 | 175 | 112.8 |
| 膠布 2.0 | 184 | 136.4 |
| 珍珍 2.0 | 190 | 132.4 |
| ㄌㄎ（紫鳥） | 196 | 140.8 |
| 墓碑 | — | 115.1 |

不是這張畫布的素材要另外錨定：舊 `yueyue-sleep.png` 用「與新版睡姿等寬」對齊
（102.9px），兩張變體互換時才不會跳尺寸。

**睡圖不套頭皮線規則**：躺著的角色頭本來就低，硬湊 y=93 會整隻放大。睡圖只給
`height`（緊裁 PNG，寬度靠 aspect），底邊 `bottom:2px` 貼地板即可。

## 1.8 分層素材直出（2026-07-27 起的主線工法，采華已驗證）

**先檢查 PSD 有沒有分層。有的話，第 2、3 節那整套切割工法直接跳過。**

`熱狗訂單.psd` 的每個角色群組，畫師已經照部件分好圖層，而且是**線稿／填色成對**：

```
采華（由下而上）
  [0] 前腳填色  [1] 前腳線稿          ← legL（在身體後面）
  [2] 身體填色  [3] 身體線稿  [8] 細節線 ← body（完整一片，沒有挖除區）
  [4] 後腳填色  [5] 後腳線稿          ← legR（在身體前面）
  [6] 尾巴填色  [7] 尾巴線稿          ← tail（在身體前面）
```

判讀方法：`bbox` 兩兩成對＋「深色像素佔比」分類（>0.55 是線稿、<0.15 是填色）。
同一形狀出現在兩個位置＝左右對稱的兩隻腳。

**因此不必做的事**（這些都是為了「從一張平圖硬切出部件」而存在的）：

| 舊工法步驟 | 為何不再需要 |
|---|---|
| 逐列邊界函數切割、藏縫於輪廓帶 | 部件本來就是獨立圖層，沒有切割線 |
| 存根重疊帶 8-12px | 同上 |
| body 補「該處應有的毛色」 | body 是完整一片，底下沒有洞 |
| 描邊斷頭磨圓 | 沒有描邊被切斷 |
| clipPath 圓角防毛刺 | 沒有平切直邊，部件自帶圓輪廓 |
| 眼睛挖洞後補底色 | 眼睛在線稿層，挖掉就露出完整的填色層，零殘留 |
| limbScale/tailScale 降擺幅藏縫 | 沒有縫，采華用滿幅 1.0 |

**還是要做的事**：

1. **確認 z 序**：照 PSD 由下而上的原順序分組，別自己猜。猜錯的症狀＝疊回比對
   出現「最大單通道差 187」這種大數字（采華第一次就是猜成 tail 在最底，錯的）。
2. **樞紐**：腳取部件最頂列的水平中點、尾巴取最左欄的垂直中點（貼身體交界，掃動最小）。
3. **眼睛拆件**：線稿層跑深色連通元件，挑「圓度高、尺寸小」那顆；眉毛／吻部直紋／
   腮紅屬非眼睛標記，留在 body。外擴 2px 把 AA 半透明邊一起帶走，body 才不留灰邊。

**驗收（采華實測值）**：

| 指標 | 門檻 | 采華 |
|---|---|---|
| 部件依 z 序疊回 vs 群組原圖 | 最大單通道差 ≤2（AA 捨入） | **1** ✅ |
| body+eye 疊回 vs 原 body | 0 px 不同 | **0** ✅ |
| ±13° 旋轉時「身體剪影內破洞」 | 0 px | **0**（±6.5/±13 全過）✅ |
| 眨眼 hRatio / 位移 | 0.08 / 0 | **0.08 / 0,0** ✅ |
| happy / closed hRatio | 0.45 / 0.12 | **0.452 / 0.119** ✅ |

一次過關，不需要 `tools/vtest` 那套實機判官——那套是為了裁決「像素手術到底修好了沒」，
分層素材沒有手術可言。（若日後某隻分層不完整而出現接縫，再回到第 2、3 節。）

### 六隻做完後補上的坑（2026-07-27）

- **一層裝「一對」腿 → 不能整層當一件轉**（珍珍）。樞紐取「最頂列中點」會落在其中一隻
  腿上，另一隻在 200px 外被甩飛。正解：連通元件切成單腿再各自給樞紐；切不開的
  （上下兩對分兩層）就只讓其中一對可動、另一對併回 body 當靜態
  （兩對垂直不重疊時改 z 序不影響畫面）。
- **連通元件切件必須把抗鋸齒邊一起帶走**（ㄌㄎ／羊咩）。只複製 `alpha>128` 會丟掉
  邊緣半透明像素，疊回驗證直接跳到「最大差 255」。作法＝先用 `alpha>128` 標連通元件，
  再把 `0<alpha<=128` 的像素反覆指派給相鄰標籤（4 輪）。
- **可動件不一定是腿**（膠布 2.0）。2.0 重新設計後刀沒了，分出來的兩件其實是左右手臂、
  腿長在 body 裡。硬當 legL/legR 轉會像划船。正解：左臂＝legL（走路擺動）、
  右臂＝pawR（保住「舉手提醒」這個招牌動作）、legR 留空 `<g>`。
- **body 的填色層與線稿層不一定相鄰**（膠布 2.0：填色[0] → 左臂[1,2] → 線稿[3]）。
  配對要靠 bbox IoU 而不是相鄰順序；且 body 必須拆成兩張靜態圖，把手臂夾在中間，
  否則 z 序會錯。
- **線稿與填色合併在同一層的角色要補洞**（ㄌㄎ）。挖眼會留真的洞 → 用「洞外圈非深色
  像素的中位色」補平。實測疊回最大差 4/255，body-only 目檢無殘留環，可接受。
  這是唯一沒有拿到「差 ≤2」的角色。
- **不要用位置猜五官**（采華）。我把吻部尖端那顆圓點當成眼睛，其實是鼻孔，真正的眼睛
  在雙角下方。連通元件只能告訴你「有幾顆」，是什麼要看圖或問畫師。

### ⚠ 最重要的一課：畫師會在「被蓋住的地方」留鏤空（月月／珍珍／羊咩／ㄌㄎ）

分層素材**不代表 body 是完整的**。填色層在「反正會被上層蓋住」的位置常常是空的——
眼睛底下、手掌底下、腳根部都中招。靜止時看不出來，一旦**眨眼把眼睛壓扁**或**手臂擺動**，
那塊鏤空就露出桌面背景。使用者回報的「眼睛脫窗＋底部有陰影＋看到背景」就是這個。

- **補法＝墊底層**：補色畫成一張圖放在 body **下面**，再把原 body 疊上去。
  區域＝洞 ∪ 向外擴 3px 中「body 合成後 alpha < 250」的像素——**一定要含半透明邊緣**，
  只填全透明的部分會留一圈灰影，實機上就是使用者說的「眼睛底部有陰影」。
  底色＝洞外圈 alpha>200 的非深色像素中位數。
- **⚠ 絕對不要無條件外擴覆寫**：曾經想「比照熱狗直接把 dilate(眼件,3) 覆寫成身體色」，
  結果把緊貼眼睛的實心線條一起洗掉——**采華的眉毛與眼睛在原畫是連在一起的**，
  眉毛下緣被吃出一道綠縫（使用者回報「眉毛被切掉了」）。
  要動就只動「body 本來就沒填滿」的像素，實心線條一律不碰。
- **收工前一定要對 PSD 原圖做回歸比對**：統計「原圖是深色、現在不是」的像素，
  並看它們的**最大連通塊**——散開的個位數是 AA 捨入雜訊，成塊的就是被吃掉一整段線條。
  這一關抓到兩次錯誤（采華眉毛 306px、羊咩腿線 120px），肉眼與其他指標都沒抓到。
- **⚠ 先分清楚「洞」和「窗」**：body 上的封閉透明區有兩種，補錯會把美術蓋掉。
  - **洞**＝被畫在 **body 前面**的部件蓋住 → 部件一移開就露背景 → **要補**。
  - **窗**＝被畫在 **body 後面**的部件填滿 → 那是畫師故意開的孔，讓後面的部件透出來
    （羊咩的 z 序是 `legL, legR, body, pawR`，兩隻腳畫在身體後面，身體底部那兩個孔
    就是給腳穿出來的）。**補了等於把腳的線條蓋成粉紅色**。
  - 判準：把可動件依 z 序分成 front / behind 兩堆，孔若有 >30% 面積被 behind 填滿
    就跳過。實測羊咩補錯時會吃掉一整段 120px 的腿部線條。
- 其他沒被任何部件蓋住的封閉透明區是畫師故意的，一律別動。
- **驗證方法本身也要修**：原本我拿「body 不透明像素」當基準剪影去數旋轉破洞，
  結果**永遠是 0**——洞本來就不在基準裡，等於自己把答案挖掉。
  正確指標＝「把眼件壓到 scaleY 0.08、部件轉到 ±13°，統計**眼件/部件原覆蓋區內**
  仍然透明的像素」，門檻 0。六隻補完後全部歸零。

### ⚠ 改完素材看不到效果？先清 WebView2 快取

WebView2 會快取 asset 協定的圖片，而它的 user data 資料夾
`%LOCALAPPDATA%\com.clawd.pet\EBWebView` **不會隨著重開 app 或重裝而清空**。
症狀＝檔案明明已更新、app 也重啟了，實機還是舊圖（我就被騙過一次，以為補洞沒生效）。
清法（只清快取，保留 localStorage 才不會洗掉使用者的角色/夥伴/心情設定）：

```powershell
Get-Process clawd-pet -EA SilentlyContinue | Stop-Process -Force
foreach ($s in 'Cache','Code Cache','GPUCache') {
  Remove-Item "$env:LOCALAPPDATA\com.clawd.pet\EBWebView\Default\$s" -Recurse -Force -EA SilentlyContinue
}
```

## 2. 決定拆件與切割線（僅適用於「沒有分層的平圖」——新素材請先看 §1.8）

原則：**切割縫必須藏在「粗黑輪廓帶」或「暗色對暗色」的交界裡**。

| 部件 | 切割位置 | 藏縫原理 |
|---|---|---|
| 腳 | 裙擺/身體底部輪廓帶的中線（水平切） | 輪廓帶厚 15-20px，本體蓋在上面 |
| 手臂 | 袖子與衣服的交界（暗色對暗色） | 黑對黑看不到縫 |
| 尾巴 | 沿身體右緣輪廓（垂直切） | 藏在身體/袖子後面 |

**重疊帶**：部件比 body 挖除區多留 8-12px「存根」，body 的輪廓/暗色蓋在存根上。
部件畫在 body **後面**（SVG 先畫），旋轉時存根邊緣被 body 蓋住。

**陷阱（狐狸鼻子事件）**：矩形遮罩會偷走鄰近部位的像素——刀刃右緣與吻部左緣
只差 13px，矩形切到 x176 就把鼻子劃進手臂圖層，手一動鼻子跟著飛。
**先逐列掃描兩個部位的邊界**（`runs(y)` 列出每列的內容段落），再用「逐列邊界
函數」`cut(y)` 切，不要偷懶用一個大矩形。

**靜態遮蓋技巧**：跟本體交界複雜的部件（如袖子），body **保留一份不挖**，
部件裡的副本藏在後面；動起來只露出本體輪廓外的部分（刀、手），
交界處永遠被 body 的靜態袖子蓋住 → 不會破洞。

**陷阱（膠布刀臂事件）**：部件畫在 body **前面**、body 底下只補純色不補描邊，
只要那部件會「抬離／掃離原位」就**必裂**——0° 時是部件的輪廓在幫 body 描邊接線，
一動就露出 body 的斷頭描邊＋純色縫。**補描邊救不了**：補在部件 0° 位置的描邊會隨
掃出量整片外露，變成飄在毛色上的幽靈刮痕（實測膠布刀臂樞紐距縫 94px、擺 14.5°＝
掃出 23px，遠超描邊厚 ~15px，任何角度都藏不住）。
純「靜態遮蓋」（部件移到 body 後面、body 烘回靜態部件）能根除縫，但只顯示「探出
body 輪廓外」的部分；若部件整個埋在 body 剪影內（膠布的刀在肚子正中央，任何角度
掃出量 = 0），動作會**完全看不見**。
「混合遮蓋」（body 烘靜態副本、部件前置黑對黑藏縫）實機也被退貨：部件抬離後
靜態副本**整個露出＝殘影**，且兩層描邊邊緣永遠對不齊＝「手被分開」。
藏縫要求兩條獨立輪廓像素級重合，物理上做不到。
**正解（用戶定案規則，膠布最終採用）**：part 底下的 body 一律推測補「該處應有的
顏色」（刀底下＝肚子毛色），**不留任何 part 殘留**。part 前置照常顯示動作，移開後
露出純毛色——天然無殘影、無裂縫。代價＝要處理「被 part 切斷的 body 描邊斷頭」：
- **斷頭處理**：只磨「會在 part 旋轉 reveal 區內或邊緣裸露」的描邊斷頭，其餘不動。
  磨法＝把描邊邊緣圓頭化、退到毛色間隙 > 偵測器 side（本例需 ≥4px），毛色填平＋
  1px 羽化模擬圓頭筆觸。該描邊會小幅縮短，屬務實取捨（用戶已認可）。
- **量化判準**：(a) 殘影＝rotate 後 reveal 區內的深色/刀色像素數（必須 0）；
  (b) 裂縫＝side=3 的「深-淺-深」走廊偵測器限定 reveal 區（≤基準雜訊）。
  兩者跑**每 0.5° 全掃描**——只驗端點角度會漏掉中間角度才現形的縫。
- **隔離自身縫**：把部件疊在整片壓平毛色的假 body 上重跑偵測器，可區分
  「body 斷頭縫（可磨）」vs「part 自身輪廓凹口（理論下限，不用管）」。

**拆件不完整 → 一體化重切（膠布刀手第二輪）**：部件動起來「一條線裂成兩半、
中間夾毛色」且 side=3 偵測器抓不到 → 幾乎必然是**部件的描邊沒割乾淨，一段留在
body**（膠布：手臂下緣整條鉤形描邊 ~2900px 漏拆）。處理流程：
- **鑑別**：ref / part / body 三方逐像素比對，part 深色染紅、body 深色染藍疊圖，
  一眼看出哪段藍線該屬紅部件。寬縫（浮件）要用「part 深色連通元件數」抓，
  side=3 只抓 ≤3px 細縫。
- **內部線 vs 剪影線**：用「是否貼近背景」把 body 深色分成外剪影（不能動）與
  內部線（可搬）。可搬的搬進 part、body 原位補毛色；搬完驗 silhouette 差=0。
- **熔接尖端**：內部線一端與外剪影/別的肢 flush 熔接時硬併會撕縫——把 part 那端
  收短＋圓頭化，留出 >side 的毛色間隙（掃「切點 vs 縫值」找臨界點取最靠外乾淨值）。
- **別只搬一半**：part 收短後 body 要補掉**整條原線**，否則殘段變成指標抓不到的
  孤兒浮件。驗收硬指標＝part 深色在全角度都是**單一連通元件**（逐 0.5° label 計數
  0 例外；刀刃亮色分隔內外圈屬允許例外）。
- **Q 版熔接線不要拆成兩肢（膠布第三輪，用戶定案）**：部件與身體在連接點
  「共用同一條描邊」時（手臂下緣＝肚子曲線＝腳的起頭），別為了消移動撕縫把部件端
  收短分家——0° 閒置就會露縫。正解：**連接邊線以 ref 原畫為準補回 body 靜態層**
  （任何角度都在、永遠與剪影連續），部件端保留圓頭收短；0° 深對深相疊＝與原畫
  像素一致，旋轉時部件滑開的窄縫可接受。
  驗收權威指標＝「**0° 合成 vs ref 原畫逐像素 = 0**」（side 偵測器會對原畫自身
  AA 誤報，不可靠）；殘影要分真假：reveal 區深色若「連通到身體主 comp」是該在的
  靜態線，只有孤兒浮段才算殘影。極限角另驗：部件不戳出身體+鄰肢合成剪影、
  不疊鄰肢成怪塊。

**自由外剪影邊的掃出缺口 → body 靜態邊線法（膠布第四輪）**：部件（尾巴/腳）擺動
時外緣掃出薄背景縫，且 exposed 區 100% 是部件自身的自由外剪影邊 → 加寬部件 PNG
**無法歸零**（旋轉非圓形剪影必掃邊）、移樞紐只會搬缺口。正解：掃出區在部件靜止
剪影內部時，把「部件靜止像素」複製進 body（前層）當靜態邊線——0° 被部件蓋住
不變、擺動時前層頂住外緣＝輪廓穩定零缺口。⚠ 只搬 alpha>128 的不透明像素，
搬到半透明會把 body 打穿露白。
~~**接縫用重疊帶不用熔接**：部件端與靜態線相接處加「深對深重疊帶」……~~
**（已作廢——實機翻車）**：深對深重疊帶在 PIL 不透明模擬裡隱形，但實機
Chromium 縮放後兩層深色 AA 半透明邊緣**疊加變更黑**＝明顯黑線。別再用。
**系統性驗證硬紀律**：body 是共用底——任何 body 改動要對「所有會動的部件×各自
全角度＋同時走路組合」重驗，不能只驗剛動到的那件。「縫走廊」（side=3）和
「背景缺口」（exposed-bg）是兩種缺陷要分開量。**一律附實機尺度渲染**：藝術尺度
的 2-4px 在實機常 <1px，嚴重性以實機圖為準，但使用者看得到的就是要修。

## 2.5 實機管線驗證台（v0.3 接縫災難後的最終權威，必用）

PIL 原圖尺度的模擬會過、實機卻壞——實機是 Chromium SVG 光柵化＋縮到
198css×1.875dpr＋各層 AA 半透明邊緣，「深對深疊加變黑」「灰補丁取樣錯位」
都只有實機管線看得到。**任何資產改動的最終驗收＝ `tools/vtest` 驗證台**：
真 Chromium 渲染到實體尺度 → 黑線/背景缺口/殘影三指標 vs 0° → 2x 人審條帶
→ Gemini 視覺判官（「有沒有肉眼可見的接縫/黑線/缺口」）。用法見 tools/vtest/README.md。

v0.3 用它翻案學到的（膠布/玥玥定案）：
- **可見旋轉部件的「黑線/破圖」常常不是接縫**——是部件自身合法輪廓在掃動
  （junction-windowed 指標證明膠布肩接縫任何樞紐都乾淨）。像素手術與移樞紐
  都無效（乾淨門檻下的動作幅度與樞紐無關，radius×sinθ 同比）。
- 此時的實務解＝ CFG 的 **pawScale / tailScale 擺幅上限**＋Gemini 驗收
  （膠布 0.5 保刀動作、玥玥 0.2 微搖）。
- 被前層完全蓋住的「靜態重疊帶」對任何旋轉角的畫面零貢獻——拆不拆都一樣，
  別浪費輪次（θ-vs-0° 位元級對比可證）。
- darkening 指標對大幅合法運動的部件會過度計數（把揮刀當缺陷）——這類部件
  以 Gemini/人審為準，指標當輔助。
- 改樞紐前先用驗證台掃候選（玥玥掃 7 個，原樞紐就是最佳）。

## 3. 驗證（每次改遮罩必做）

```
A. 疊回測試：tail→legL→legR→arm 依序 alpha_composite 再蓋 body，
   與清好的原圖逐像素比對，必須 0 差異（靜止時 = 原圖）
B. 旋轉測試：canvas 把各部件轉到最大擺角再疊圖，輸出 ASCII 目檢——
   看毛刺、破洞、部位分家（鼻子還在不在臉上）
C. 實機管線測試：tools/vtest 驗證台全指標＋Gemini（見 2.5 節）——最終權威
```

截圖工具掛掉時的替代目檢法：瀏覽器 canvas `drawImage` 疊部件 →
`getImageData` → 每 6x10px 取樣輸出 ASCII（透明=空格、深色=#、白=.、灰=o）。

## 4. 接進程式

`src/index.html` 加 `<template id="char-名字">`，結構照抄現有角色：

```html
<svg id="pet" viewBox="留白裁掉後的子窗 x y w h">
  <defs>各部件的圓角 clipPath（rx 13-18，防旋轉毛刺）</defs>
  <ellipse id="shadow" .../>
  <g id="root"><g id="body">
    <g id="tail">…</g> <g id="legL">…</g> <g id="legR">…</g> <g id="pawR">…</g>
    <image href="本體.png"/>   （眼睛已從 body 挖出、洞用周圍底色無縫補平）
    <g id="face"> eyes-open / eyes-happy / eyes-closed 都用「原眼部件」 </g>
  </g></g>
</svg>
```

### 眼睛（用戶定案 2026-07-21：用它本來的眼睛來動就好）

**新規則：新角色眼睛一律「拆原圖眼睛成可動件、body 無縫補洞」；不重畫、不用底色橢圓蓋。**
閉眼／瞇眼＝原眼以自身中心 scaleY 擠壓（不畫弧線）。眉毛／腮紅／捲角紋等
「非眼睛」標記留在 body 不動。

**舊做法（白/底色橢圓蓋掉原眼＋重畫黑眼或弧線）已淘汰**：實機會露「眼窩」——
覆蓋橢圓的外緣蓋不滿原眼黑圈，殘留黑碎片；閉眼弧線又疊在一塊怪異的白/底色橢圓上，
特別醜。根因是「橢圓補色 ≠ 原眼形狀」必留色差殘邊。

拆件與補洞（Python + PIL，見 scratchpad `EYE_build.py`）：
1. 從眼中心對「暗色 mask」洪水填充抓出黑眼圈連通元件 → 填洞（含珍珍的白色高光）
   得到「實心眼形」；珍珍兩顆閃亮眼＝黑圈＋2 白高光整組拆出，眉毛/愛心不算眼睛。
2. 眼件輸出：實心區保留原色 alpha 255；外緣抗鋸齒帶用「黑墨 + alpha=暗度」表示
   （疊回純白底可**逐像素還原**原邊，避免硬邊）。輸出**緊裁小圖**（tight bbox）。
3. body 補洞：把 `dilate(眼件,3)` 區域填成周圍底色（珍珍白毛、珍母圓頂白，實測皆
   純 255,255,255）→ 眼件移開時露出的是乾淨底色，**零殘邊、零色差、零橢圓輪廓**。

template 三狀態都引用**同一顆原眼小圖**（緊裁，`x/y/width/height` 定位在原圖座標）：
- `eyes-open`：`<g class="eye"><image .../></g>`。因為 image 是**緊裁小圖**，
  `.eye` 的 `transform-box:fill-box` bbox＝眼睛自身緊 bbox，眨眼 `scaleY(0.08)`
  才會以**眼中心**擠壓。（陷阱：若用全畫布 image + clipPath，fill-box 會是整張圖
  ——clip 不改 geometry bbox——眨眼會往畫布中心飛。實測緊裁小圖 blink 位移＝0。）
- `eyes-happy` / `eyes-closed`：**同一顆原眼**，用 SVG transform 屬性寫死以眼件 bbox
  中心 (cx,cy) 做垂直擠壓，**不畫弧線**、**不加 class="eye"**（避免被 blink 二次變形）：
  - 閉眼 `translate(cx cy) scale(1 0.12) translate(-cx -cy)`
  - 瞇笑 `translate(0 -N) translate(cx cy) scale(1 0.45) translate(-cx -cy)`（上移 2-3px）
- viewBox 直接開窗原圖座標，PNG 不用縮放裁切；SVG user unit＝PNG 像素座標。

驗證（見 `EYE_render.py` 與瀏覽器 getBoundingClientRect 實測）：疊回眼件 vs 原圖眼區
逐像素 0 差異；補洞區露出無橢圓/色差/殘邊；blink/happy/closed 各狀態的高度比與
中心位移都符合設計（blink hRatio 0.08、happy 0.45、closed 0.12，位移＝0 或設計上移量）。

`src/pet.js` 的 `CHAR_CFG` 加設定：

| 欄位 | 說明 |
|---|---|
| `height` | CSS 顯示高度。**新素材一律用 §1.5 的全域倍率**（PSD 高 × 0.3336）。舊的頭皮等高法（解 `(254-H) + (scalpY-vbY)*H/vbH = 93`）只留給既有六隻，不再套用到新角色 |
| `sleep` | 睡姿圖清單 `[{img, h, w}]`，`w` 是抽選權重（預設 1）。空的話走通用躺平 |
| `limbScale` | 擺幅倍率。切割縫淺的角色調小（狐 0.55），毛刺藏不住就再降 |
| `center` | 臉中心（視窗 CSS 座標），視線跟隨用 |
| `legL/legR/pawR/tail` | 各部件旋轉樞紐（viewBox 座標），放在切割線中點附近讓切邊掃動最小 |
| `up` | pawR 舉起方向。SVG y 向下、順時針為正 → 通常 -1 |
| `hit(px,py)` | 命中判定（CSS 座標）：頭橢圓 + 軀幹矩形，座標 = 佈局換算 `cssX=(240-W)/2+(vx-vbX)*scale` |

選單：`src-tauri/src/main.rs` 的 `show_menu` 加 `p_char_名字` 項與事件轉發。

## 5. 已知座標換算公式

- `scale = height / viewBoxH`
- `cssX = (240 - width)/2 + (vx - vbX) * scale`，`width = viewBoxW * scale`
- `cssY = (254 - height) + (vy - vbY) * scale`（254 = 視窗高 256 - bottom 2px）

## 6. 常見坑備忘

- PowerShell 量視窗要先 `SetThreadDpiAwarenessContext(-4)`，否則座標被虛擬化
- WebView2 的 dpr = 顯示縮放 × 文字大小（例 1.5×1.25=1.875），只信前端回報
- 常駐動畫一律走 30fps JS 迴圈，不用 CSS 無限動畫（透明視窗省合成）
- data-URL SVG 載不到相對路徑 PNG——canvas 目檢要用 fetch 同源圖檔
