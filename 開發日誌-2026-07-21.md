# 開發日誌 2026-07-21（v0.2.0 → v0.3.0）

> 給回家接續開發用的完整脈絡。今日 commit：`ff3ae80`（v0.3.0 主體）→ `ec9c0f8`（驗證台＋擺幅定案）→ `dc4076a`（工法文件）。

## 今天做了什麼

### 一、視窗與 UI 改版
- 視窗 200×220 → **240×256**：大角色（198px）轉圈/跳躍不再被裁切。座標系統性平移 dx+20/dy+36，底錨 218→254、頭頂等高線 57→93，全角色 hit()/center 同步改。
- 泡泡/進度條/汗滴改 **CSS 變數依角色高度定位**（`--charTop`/`--uiTop`），不再壓頭。
- spin 轉圈重做：**剪影包絡數值解**出 7 段 keyframes（最深擠壓 0.67、translateY 放 transform 最外層），六角色全路徑零出界。
- 抓起狀態加 3px 抬升（198px 角色腳底原本會掃出視窗底）。

### 二、新角色：珍珍（綿羊）與珍母（水母）
- 照《角色製作工法》拆件，疊回零差異。珍母觸手 4 條分左右兩組當 legL/legR。
- 兩隻 pawR 都是空 `<g>`（小角拆不乾淨）——**CHAR_CFG 的 pawR 欄位仍必須給值**（spread 缺欄位會 TypeError）。
- **眼睛規則（用戶定案）**：拆原圖眼睛成緊裁小圖可動件（`zhen*-eyeL/R.png`），body 無縫補洞；閉眼/瞇眼＝原眼 scaleY 擠壓（0.12/0.45），**不重畫、不用底色橢圓蓋**（會出「眼窩」）。⚠ 眼件必須緊裁小圖：`transform-box:fill-box` 取 geometry bbox、clipPath 不改 bbox，全畫布圖會讓眨眼飛走。

### 三、多人模式重構（多選夥伴）
- pet2 單夥伴 → 每角色獨立勾選，視窗 label `pet_<char>`，可同款多隻。
- main.rs `CHARS` 資料表驅動選單（加新角色只改這一行）；選單 id 前綴解析（`p_char_`/`p_comp_`/`q_feed@label`）。
- localStorage `petcompanions`（自動遷移舊 petmulti）；Claude 工作量全場 base/rem 均分。
- ⚠ **capabilities 白名單坑**：Tauri 2 視窗不在 `capabilities/default.json` 的 windows 清單＝IPC 整批被拒（症狀：夥伴拖不動＋腰斬）。已改 `["main","pet_*","toy_*"]`（支援 glob）。**之後加新視窗 label 前綴記得檢查這裡**。

### 四、玩具系統（小恐龍）
- `TOYS` 表＋`toy_<id>` 視窗（150×120）＋Rust 物理執行緒 33ms（重力 2800、彈牆 0.8、落地 0.45、摩擦 0.90；視窗不存在執行緒自動收工，零閒置成本）。
- 每 1s 找 600px 內最近寵物發 `chase`（**實體 px**，walk 契約是實體座標）、130px 內發 `kick`＋隨機衝量。
- 恐龍依 vx 方向水平翻面（原圖朝左），靜止保持最後面向。
- pet.js 新增 `chase:`/`kick:`/`toy:`/`toyoff:` 指令與 `.kick` CSS（利用面向翻轉免方向變數）。

### 五、寵物大小調整
- 選單「大小：」五檔 0.7–1.3（`SCALES` 表）、localStorage `petscale`。
- 實作＝`#stage` transform scale(S) origin 0 0 ＋ fitWindow 傳 `dpr*S` ＋ cursor 座標除以 S。玩具不受影響。

### 六、接縫大戰（今天最大的教訓）
膠布刀手歷經**四輪像素修補**（底補毛色→手臂一體化→熔接線回 body→重疊帶+靜態邊線），每輪 PIL 模擬都「通過」、實機都翻車。玥玥尾巴同樣（存根延長→碎片移除→樞紐上移→靜態帶）。最後建了 **`tools/vtest` 實機管線驗證台**才翻案：

- **總根因**：PIL 原圖尺度不透明模擬 ≠ 實機 Chromium SVG 光柵化（縮到 198css×1.875dpr、各層 AA 半透明邊緣）。「深對深重疊帶」在模擬裡隱形、實機疊加變黑線（此工法已作廢）。
- **翻案結論**：膠布黑線與玥玥破圖**都不是接縫**——junction-windowed 指標證明肩接縫在任何樞紐下都乾淨；那是**可見部件自身輪廓的合法掃動**，像素手術與移樞紐皆無效（乾淨門檻下的動作幅度與樞紐無關）。
- **定案**：CFG 新增 `pawScale`/`tailScale` 擺幅上限——膠布 `pawScale: 0.5`（刀尖仍揮 ~11px）、玥玥 `tailScale: 0.2`（±1° 微搖），實機管線 Gemini/人審 PASS。
- 沿途沉澱的有效工法（都在《角色製作工法》）：部件底下補該處身體色不留殘留、拆件一體化＋連通元件證明、Q版熔接線補 body 靜態層、斷頭磨圓、內部線 vs 剪影線鑑別、每 0.5° 全掃描、實機尺度渲染判準。

## tools/vtest 驗證台（之後改任何角色資產必過）
- 真 Chromium 實體尺度渲染 → 黑線/背景缺口/殘影三指標 vs 0° → 2x 人審條帶 → **Gemini 視覺判官**。
- 用法見 `tools/vtest/README.md`（開 launch.json 的 clawd-pet-preview + `recv_server.py`，瀏覽器跑 `vtest.html` 的 `VT.scan()`，再跑 `metrics.py`/`judge.py`）。
- ⚠ `judge.py` 用 dotenv 讀 `GEMINI_API_KEY`——**key 不在 repo**，本機在 `D:\claude\.env`；家裡要自己放一份 .env（**切勿 commit**）。
- 驗證台合格性已證明：能重現使用者回報的兩個實機缺陷、對已知乾淨的狗狗 PASS。

## 回家接續：待辦與可調項
1. **實機觀感確認**：膠布 pawScale 0.5 / 玥玥 tailScale 0.2 的動作幅度是否滿意。膠布 0.5→0.7 之間還有空間（0.7 是「隱約可見黑線」邊界，驗證台掃過）。
2. **重打包安裝檔**：`src-tauri/target/release/bundle/nsis/ClawdPet_0.3.0_x64-setup.exe` 是擺幅修正**前**的 build——確認 OK 後 `npm run build` 重打包（⚠ 桌寵執行中會「存取被拒」，先關）。
3. 想繼續做的候選：珍珍/珍母的 pawR 動作件（目前空）、第二種玩具、玩具大小、更多角色。
4. 建置備忘：cargo 不在 PATH（先加 `%USERPROFILE%\.cargo\bin`）；版本號要動三處（tauri.conf.json / package.json / Cargo.toml）。

## 檔案地圖（今天新增/大改）
- `src/pet.js`：CHAR_CFG 六角色（含 pawScale/tailScale/SCALE 縮放）、夥伴/玩具清單、chase/kick
- `src-tauri/src/main.rs`：CHARS/TOYS/SCALES 表、pet_windows()、set_companion/set_toy、玩具物理、選單前綴解析
- `src-tauri/capabilities/default.json`：windows glob 白名單
- `src/toy.html` `toy.js` `toy-dino.png`：玩具
- `src/zhen*-*.png`：珍珍/珍母部件（含眼件）
- `tools/vtest/`：驗證台（含證據圖 out/strip_*、PHASEB_*）
- `角色製作工法.md`：大幅擴充（2.5 節驗證台為最終權威）
