# 美術瑕疵根治任務：膠布（jiaobu）與玥玥（yueyue）黑邊/拆件殘瑕

## 背景
- 兩角色是從使用者原圖拆件的 PNG 部件角色（工法見 `角色製作工法.md`，歷史見 `開發日誌-2026-07-21.md`）。
- 原圖（唯一權威，**成品不能脫離原圖樣貌，禁止重繪/生成新形狀**）：
  - 膠布：`ref-jiaobu.png`（768x768，拿小刀的鹿角狗）
  - 玥玥：`ref-yueyue.png`（768x768，灰色長吻小獸）
- 昨天已修過多輪（黑線/破圖/殘影），最終用「擺幅上限」壓制（pet.js 的 `pawScale: 0.5`（膠布）、`tailScale: 0.2`（玥玥）），但**使用者實機仍看到瑕疵**。
- 已知結論（tools/vtest/README.md 有校準數據）：PIL 原圖尺度的模擬驗證不可信；實機是 Chromium SVG 光柵化＋縮至 CSS×dpr（使用者 dpr=1.875）＋各層 AA 半透明邊緣，深對深重疊會疊加變黑、灰補丁縮放取樣錯位。

## 本輪目標
找出並根治「靜止與動作全程」肉眼可見的瑕疵，而不是再調擺幅遮掩。優先順序：
1. 靜止 0° 就看得到的任何缺陷（黑邊、色差補丁、描邊斷差）
2. 動作掃描範圍內（pet.js animate() 的實際擺幅）的黑線/缺口/殘影

## 你這一輪要做的事（只診斷討論，不改任何檔案）
1. 讀 `角色製作工法.md`、`tools/vtest/README.md`、`開發日誌-2026-07-21.md` 的相關段落、`src/index.html` 兩角色 template、`src/pet.js` 的 CHAR_CFG 與 animate() 擺幅。
2. 用 Python/PIL 分析現有部件（`src/jiaobu-*.png`、`src/yueyue-*.png`）與原圖的差異：
   - 疊回比對（各部件按 z 順序合成 vs 原圖）逐像素差異圖與統計
   - 模擬實機縮放：合成後縮到實體尺度（膠布高 198CSS×1.875、玥玥同）再比對，找縮放後才浮現的暗線/色差
   - 對每個可動部件，在其實際擺幅端點角度（膠布 pawR ±5.5°走路/+14.5°ask/±11°hi 全乘 limbScale·pawScale；玥玥 tail 全幅×0.2、legs ±6.5°）做旋轉合成，找黑線（兩層描邊 AA 疊加）、缺口（背景穿透）、殘影
3. 產出「診斷報告＋修復方案」寫進 `ART-DIAGNOSIS.md`：
   - 每個發現的瑕疵：位置（座標/部件）、成因、嚴重度、對應的實機可見性推測
   - 每個瑕疵的修復方案：只能用「從原圖取樣的像素」修（補存根、磨描邊斷頭、調 clipPath、重切邊界），或調 SVG 結構/樞紐；**不可以畫新東西、不可以改變輪廓剪影**
   - 若你有圖像生成能力，說明你打算怎麼用（僅限：以原圖為底的局部修補；生成結果必須與原圖對應區域逐像素高度一致，偏離即棄用）
   - 標注哪些方案有風險需要人工確認
4. 不要跑瀏覽器（你跑不了）；實機驗證由審查者用 tools/vtest 做，你的方案要附「審查者驗證清單」（哪些 part 掃哪些角度、看什麼指標）。

## 硬性約束
- 本輪 **read-only**：除了 `ART-DIAGNOSIS.md` 與臨時分析腳本（放 `tools/vtest/` 下、檔名 diag_ 開頭）外，不得改任何檔案。
- 部件檔案、index.html、pet.js 本輪一律不動。
- 分析腳本要可重跑（審查者會重跑驗證你的數據）。
