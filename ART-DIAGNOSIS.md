# 膠布／玥玥美術瑕疵診斷與修復方案

## 本輪結論

找到 **4 項**問題／高風險項目：其中 **2 項已由部件疊回原圖確認**，另 **2 項需要以
Chromium 實機管線及人眼確認**。本輪沒有修改資產、template 或動畫設定。

| # | 狀態／嚴重度 | 角色、位置（原圖座標） | 現象與成因 | 建議修法 |
| --- | --- | --- | --- | --- |
| 1 | 已確認／中 | 膠布 `body`：`x=388..401, y=406..412`，另 `x=475..477, y=537..539` | 靜止 PNG 疊回時，原圖的深色筆畫被 `jiaobu-body.png` 的肚子毛色補丁取代；前置 `pawR` 在第一塊完全沒有 alpha，第二塊只有約 110 alpha，不能還原原畫。排除 SVG 重畫眼睛區後仍有 54 個 RGB 差異像素（最大通道差 154），最大連通塊 36px；縮至 323×371 實體 px 後仍為 24px 的 8×5 區塊。這是 0° 就存在的色差／描邊斷缺。 | 先把這兩塊依 `ref-jiaobu.png` **逐像素取樣**。由人工判斷筆畫歸屬：若是身體與刀臂共用的靜態接線，回補至 `body`；若是刀臂自身線，補回 `pawR`，並把 body 保持肚子毛色。兩種修法都必須使 0° PNG/SVG 合成與原圖逐像素一致，且不能加深重疊帶。 |
| 2 | 已確認／低，但應修 | 玥玥 `x=611, y=333..334` | 所有 PNG 層在此皆為 alpha=0，原圖卻是深色不透明像素；因此是 2px 的靜止透明漏點。`tail` +1° 時，PIL 的 enclosed-hole 預檢仍命中附近 2px（`x=612,y=333..334`）。在 352×371 實體 px 下，靜止縮放差只剩 1px、最大色差 24，單看肉眼可能不可見，但它違反原圖 0° 還原。 | 從 `ref-yueyue.png` 取原像素，依線條是否應隨尾巴動而放入 `yueyue-tail.png` 或 body。優先以「0° 精確還原」作選擇；若放 tail，還要驗證 ±1° 不出現孤兒深色段。 |
| 3 | 高風險待確認／中 | 兩角色眼睛；`src/index.html` 的 `ellipse` 與 happy/closed `path` | template 明確用底色橢圓蓋掉原眼後，以新的橢圓／曲線重畫。這與「原圖拆眼件、body 無縫補洞、同一原眼 scaleY」的既定工法相反；原眼輪廓、AA 與閉眼時的形狀不會逐像素等於原圖，且底色橢圓有眼窩／色差風險。這項不由 PNG 疊回指標計數（為免把 template 有意重繪的眼區混入資產差異，分析腳本已排除眼區）。 | 下輪拆出原圖的緊裁 `eyeL/eyeR` PNG，body 只以眼區周圍的原圖底色補洞；open/happy/closed 均使用同一原眼，以 bbox 中心 `scaleY`（0.08/0.45/0.12）變形。不可重畫路徑或橢圓。這是有視覺取捨的結構改動，先人工確認是否納入本次修復範圍。 |
| 4 | 高風險待確認／中至高 | 膠布 `pawR`（pivot `[533,452]`）與玥玥 `tail`／`pawR`（tail `[608,385]`、paw `[145,515]`） | 膠布手臂是前置可見部件；PIL 的「變暗」候選在最大 ask +7.25° 為 2,757 native px，主要就是合法的刀臂輪廓掃過肚子，不能直接判為黑線。玥玥尾巴即使已縮至 ±1°，仍有約 600 native px 暗化候選；這吻合既有紀錄的「Chrome AA 深對深才變黑」。玥玥手臂在 body **下方**，ask +14.5° 的原靜止剪影流失候選達 1,682px（約 `x=42..101,y=414..525`）；多數可能是允許的外剪影移動，不是已證實破洞，但必須排除動作時被 body/clipPath 截掉而形成明顯缺口。 | 不要再加深色重疊帶，也不要僅靠移樞紐或調小擺幅掩蓋。先以真 Chromium 掃描判別：若是 blackline，針對相交的 AA 邊緣重切，讓 0° 只有一層原圖描邊；若是 body 露出的描邊斷頭，僅在 reveal 區把斷頭退進毛色並圓頭化；若是外剪影缺口，將**原圖不透明像素**作為 body 的靜態邊線，且絕不搬半透明像素。若 vtest/Gemini 判定只是合法可見輪廓，維持目前 `pawScale:0.5`／`tailScale:0.2`，不做像素手術。 |

## 可重跑分析與資料限制

分析腳本：[tools/vtest/diag_art_assets.py](tools/vtest/diag_art_assets.py)；執行：

```powershell
python -B tools/vtest/diag_art_assets.py
python -B tools/vtest/diag_art_assets.py --character jiaobu
python -B tools/vtest/diag_art_assets.py --character yueyue
```

它依 `index.html` 的 layer z-order、clip rect、`pet.js` 的實際倍率合成 PNG，並輸出 JSON：

- 0° 對 `ref-*.png` 的 RGB/alpha 差異（修復後包含原圖眼件區）；
- viewBox 裁切後縮至 `198 CSS × DPR 1.875` 的差異；膠布是 **323×371**、玥玥是 **352×371** 實體 px；
- 各可動件的實際端點：膠布 pawR walk ±2.75°、hi ±5.5°、ask +5.75/+7.25°；玥玥 tail ±1°、腿 ±6.5°、pawR walk ±5.5°、hi ±11°、ask +11.5/+14.5°；
- `silhouette_loss`（只作人工審查候選）與較嚴格的 `enclosed_gap`，以及暗化候選區。

PIL 的 rotate/downscale 不能模擬 Chromium SVG 層級 AA，尤其不能作為 #4 的 blackline 事實判定；該項以 `tools/vtest` 為唯一驗收權威。腳本不寫任何檔案，方便審查者重跑。

## 審查者實機驗證清單

先依 `tools/vtest/README.md` 啟動 page server 與 `recv_server.py`，然後在 Chromium 對每個 part 呼叫 `VT.scan()`；每輪均包含 0°。角度以 0.5° 間距補齊，端點表如下。

| 角色／part | 必掃端點與區間 | 人審重點 | 指標判讀 |
| --- | --- | --- | --- |
| 膠布 `pawR` | `-5.5..+7.25°`，0.5°；特別看 -2.75/+2.75、-5.5/+5.5、+5.75/+7.25 | 刀臂與肚子交會、#1 的 `(388..401,406..412)`、刀臂下端 `(475..477,537..539)`；看黑線、灰/黃補丁、露出的斷頭或孤兒線。 | `blackline_maxcomp >=150` 為 fail；但整條合法刀臂移動會誤報，須以條帶和人眼判斷「是否為接縫」。 |
| 膠布 `legL/legR/tail` | legs `-6.5..+6.5°`；tail `-5..+5°` | 裙/腳根與尾根，特別 legL -6.5° 的 `(289,649)` 2×5 enclosed 預檢點。 | gap/ghost 不可有成片連通塊；小於實體 1px 的點以人眼覆核。 |
| 玥玥 `tail` | `-1..+1°`，0.5° | 尾根及 `(611,333..334)`；看縮放後是否有黑線或針孔。 | #2 要先達 0° 原圖一致；tail +1° 的 gap/blackline 需低於校準門檻。 |
| 玥玥 `pawR` | `-11..+14.5°`，0.5°（另含 walk ±5.5°） | 左臂/身體邊界，尤其約 `(42..101,414..525)`；確認這是合理外輪廓位移，還是 body 下層+clipPath 造成的缺口。 | `gap_maxcomp >=100` 或肉眼可見背景穿透即 fail；若是純外輪廓移動，標記為「非接縫」而非修成重疊黑線。 |
| 玥玥 `legL/legR` | `-6.5..+6.5°`，0.5° | 腳根外輪廓與 body 共用底的任何新洞。 | 全部 part 皆需重跑，因 body 為共用底。 |
| 兩角色眼睛 | open/happy/closed/blink | 對照原圖眼型，找底色橢圓邊、殘留黑圈、閉眼重繪感。 | 這是人審＋0° 疊回比較；不是現有 metrics 的黑線指標可替代。 |

每組產出 `metrics.py` 條帶後，按 README 的 `judge.py` 跑 Gemini；Gemini 的 PASS 不能取代
0° 像素一致性。任何資產修改後，所有會動部件與「走路同時擺動」都需重新掃描。

## 修復邊界與人工決策

- 只允許從 `ref-jiaobu.png`／`ref-yueyue.png` 取像素，或調整 SVG 的層級、clipPath、pivot。不得重繪形狀、改變外輪廓剪影，亦不得使用深色重疊帶。
- 我不建議使用圖像生成；本案的缺失是可精確取回的原圖像素。若日後不得已使用，僅可把原圖當底做局部修補，並以對應原圖區逐像素比對；有任何偏差即棄用。
- **需要人工確認**：#1 兩段筆畫應歸 body 還是 pawR；#3 是否授權把眼睛改為原圖眼件；#4 的 vtest 條帶是否把候選確定為真正的黑線／缺口，或只是合法輪廓運動。

## 修復後驗證

執行日期：2026-07-22；命令：`python -B tools/vtest/fix_art_assets.py` 後接
`python -B tools/vtest/diag_art_assets.py`。診斷器已同步改為合成四張原圖眼件，0° 比對不再
排除眼區。

| 項目 | 修復前診斷 | 修復後診斷 |
| --- | --- | --- |
| 膠布 FIX-1 指定區 | 54 個 RGB 差異、最大通道差 154、最大連通塊 36px；實體縮放後為 24px 的 8×5 區塊 | 指定兩區逐像素合成均等於 `ref-jiaobu.png`；全角色（現含眼區）僅餘 1 個 RGB 差異像素、最大通道差 9。這個殘餘點在 `(596,527)`，不在 FIX-1 區域。 |
| 膠布 pawR 動態預檢 | 本修復前未把缺筆畫交給 pawR | `pawR` 的 -2.75、+2.75、-5.5、+5.5、+5.75、+7.25° 均為 `enclosed_gap_px=0`。 |
| 玥玥 FIX-2 | 0° 漏 2px `(611,333..334)`；tail +1° 在鄰近 `(612,333..334)` 命中 2px enclosed gap | 0°：RGB/alpha/縮放差異皆為 0；tail -1°、+1° 均為 `enclosed_gap_px=0`。 |
| 兩角色 FIX-3 眼睛 | 原診斷刻意排除 SVG 重畫眼區，無可比較的像素數據 | 原圖緊裁眼件已納入比較；玥玥 0° 全角色 RGB/alpha 差異為 0。膠布的四眼件各自與已補平 body 的重組皆逐像素一致。 |

膠布全角色仍顯示 `alpha_diff_pixels=239` 與縮放後 56 個 RGB 差異像素（最大 255）；這些位於既有
`pawR` AA 邊緣，並非 FIX-1 的兩個座標區。它們保留供 FIX-4 的 Chromium 實機掃描判定，本輪未做像素手術。
