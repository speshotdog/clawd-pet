本輪已交付地圖、美術建置腳本、更新後的驗收腳本與原尺寸截圖；**驗收尚未全數通過：221 PASS／3 FAIL／2 NEEDS_DEVICE**。原第一輪 **161 項全部執行、沒有刪除**，其中 157 PASS、2 FAIL、2 項需要實機量。沒有 commit 或 push。

已先讀第二輪 brief、藝術方向、Claude 複驗筆記、C 視覺規劃、第一輪 brief／成品／驗收腳本／REPORT，以及最新美術報告；亦讀第一輪指向的關卡規劃、編隊與地圖裁決、抽卡 CSS 與 1.0 按鈕覆寫。識別物採修訂稿 seg1b 座標，沒有使用美術報告歷史附錄的舊座標。

交付：[單檔 map20.html](../../../../_art/holo-test/map20.html)、[建置腳本](../../../../_art/holo-test/build_map20.py)、[可編輯模板](../../../../_art/holo-test/map20.template.html)、[驗收腳本](../../../../_art/holo-test/check_map20.py)、[完整 acceptance.json](acceptance.json)。

```powershell
python _art/holo-test/build_map20.py
python _art/holo-test/check_map20.py
```

最後驗收使用 Chromium **136.0.7103.25**，結束碼 **1**。以下數字均取自本次 JSON。

| 尺寸 | 導軌／非卡片 UI（70～80%） | 紋理／非卡片 UI（14～19%） | 磨損灰階差（6～14／255） |
|---|---:|---:|---:|
| 1440×900 | 76.585% | 16.073% | 10.425 |
| 1024×768 | 73.990% | 18.089% | 9.186 |
| 390×844 | 70.277% | 15.508% | 7.929 |

導軌分子沿用導航與摘要，分母仍是非卡片 UI 聯集，標頭不計入導軌。紋理量接縫支承條帶，並另量實際開關像素差；沒有把整塊面板都算成紋理。磨損採固定、不等距的邊緣刮痕與轉角缺口，重新載入圖樣一致；面板內縮 24 px 的中央樣區，開關平均灰階差三個尺寸皆 **0／255**。

| 尺寸 | 地景可見／畫面 | UI 覆蓋地景／畫面（10～20%） | 識別物可見／畫面（15～25%） |
|---|---:|---:|---:|
| 1440×900 | 65.392%（60～75%） | 10.811% | 20.214% |
| 1024×768 | 62.505%（60～75%） | 12.424% | 16.351% |
| 390×844 | 55.046%（55～75%） | 11.259% | **8.761%：FAIL** |

面積表採 `progress=2` 的固定常態構圖，分母是完整瀏覽器 viewport。手機地景可見率仍接近下限，保留三位小數與原尺寸圖供複核。

地景可見面積扣除真正不透明的繪製區域；UI 覆蓋另計所有正 alpha 的面板、標籤、圖示、邊線、路線與括框。中空節點的空心處、圖示 contain 留白不計覆蓋；半透明面板仍按實際足跡計入 UI 覆蓋。SVG 路徑每 ≤1 個來源像素取樣，筆畫寬度向上取整至裝置像素。量測期間發現並修正 Pillow 二值遮罩反相問題，最終數字已使用修正後遮罩。

三個識別物遮罩為 **(25,329)–(332,693)、(665,564)–(950,740)、(663,892)–(947,1009)**，依等比地景映射到畫面。三個尺寸的識別物被 UI 遮住皆 **0%**；不透明面板遮住識別核心亦皆 **0%**。核心採整個已標定包圍框作更嚴格檢查，沒有自行縮小核心避開遮擋。遮罩圖一併保存。

手機下方摘要 **240 px／28.436% 視窗高**，符合 0～32%。三個尺寸各跑 `progress=0…20`，靜止入口的節點本體邊緣裁切皆 **0%**；段落指示與第二至五段的佔位文字皆通過。捲動停止時停在節點本體間隙，手動捲動期間地景與節點共同移動。地景／節點相對捲動誤差三個尺寸皆 **0 px**，沒有獨立視差。

**三項未通過的原因如下，門檻沒有調整。**

| 未通過項目 | 實測／門檻 | 原因與保留決定 |
|---|---|---|
| `boss.contrast` | **9.263:1／4～6:1** | 原圖閘口的藍色填色中位數 RGB (46,70,107)，對其實際奶油底色 (254,252,242) 的對比過高。保留來源像素；沒有重新調色、濾鏡或降低整體透明度湊門檻。 |
| `controls.icon_stroke` | **0.608 px／1.5～2 px** | 原鎖頭在 y=360 的兩側黑線為 12、11 個來源像素；按 22 px 圖示盒的 contain 比例 22/416 縮放，平均線寬為 0.608 px。遵守原圖裁切與圖示盒尺寸後，無法同時套用舊向量圖的線寬要求。 |
| `390x844.landmarks_visible` | **8.761%／15～25%** | 三個來源包圍框合計 195,136 px²；整張等比放入約 374 px 寬的手機畫面，理論占比約 8.8%。目前三個物件都完整可見、UI 遮擋為 0%。要達 15% 需改構圖／素材或允許不同呈現方式，不能靠重新命名遮罩達成。 |

我認為「原樣使用這批點陣母題」與「沿用向量線寬及固定對比帶寬」，以及「完整等比手機構圖」與「識別物 15% 下限」，需要重新協調。這是提出規格衝突，**不是自行批准例外**；JSON 仍然判 FAIL。

舊腳本會讀取圖示上的 CSS `stroke` 與 `stroke-width`，但改為點陣圖後這些屬性已不參與繪製。本輪改量實際來源填色、奶油背景與縮放後線寬，避免保留無作用的 CSS 數字而假通過。勾號對其奶油底的實際對比為 **3.218:1**，仍符合 3～4.5:1。

其餘回歸包括 **20 站／5 王／19 條線**、一般節點 **52 px**、相鄰縱距／橫移 **120／36 px**、路線 **2.5 px**、已走／未走 alpha **0.72／0.28**、括框間隙 **8 px**、普通按鈕按壓位移 **0 px**、減少動態追蹤位移 **0 px**、無任務死路與執行期錯誤。通關、鎖定、最終完成與冷卻到期皆執行。

| 動態 | 時間軸 | 實際播放三次平均 |
|---|---:|---:|
| 落勾 | 140 ms（120～160） | 149.975 ms |
| 連線 | 280 ms（240～320） | 293.275 ms |
| 括框 | 190 ms（160～220） | 196.642 ms |
| 遮擋時鏡頭 | 330 ms（280～380） | 340.367 ms |

鏡頭單次位移三次皆為視窗高 **12%**（12～28%）；減少動態狀態切換 **130 ms**（100～160）。決定性截圖均先暫停 Web Animations API、設 `currentTime`、等待雙重 rAF；實際播放計時另外量測。

**冷卻隱藏狀態需要實機量。** 三次切頁經過 **5114／5111／5115 ms**，倒數皆減 **5 秒**，但 `visibilityState` 皆為 `visible`，事件紀錄皆為空。`cooldown.hidden_state` 與依賴真實隱藏狀態的倒數判定均標 `NEEDS_DEVICE`。

另照複驗筆記殺光所有 timer，畫面維持「冷卻 03:00」；6 秒後只發送明確標示為合成的 `visibilitychange`，畫面變為「冷卻 02:54」。這證明 timer 停擺期間仍使用真實到期時間，沒有冒充真實分頁隱藏事件。程式保留 `Date.now()+180000`。

HTML **837,232 bytes（0.837 MB）**，低於 2.5 MB；地景 WebP 品質 **94**、原尺寸 **971×1619**。圖示先沿 x=627／y=627 確定性分四格，再依記錄的安全邊界裁去留白，以無損 WebP 內嵌。四個解碼裁片與相應來源像素的最大差皆 **0／255**；圖示與地景 filter 均為 none，外部請求 **0**。詳細裁切座標、來源 SHA-256 見 [build.json](build.json)。重新建置前後 HTML SHA-256 完全相同，見 [rebuild.json](rebuild.json)。

地景以等比長卷連續鋪設，第二至五段在摘要明確標「地景佔位：沿用第一段後院美術」。沒有更動任何 `map-art/` 圖片。已目視檢查最終桌面、平板與手機圖，未見明顯 WebP 壓縮髒污；場景配對正確率、灰階 C 辨識與最終視覺品質仍留給人工複驗，未編造玩家測試數字。

全部並排圖皆 **左關、右開**，不縮圖；手機每側維持 **390×844**。

| 尺寸 | UI 關／開 | 灰階 | 磨損關／開 | 地景關／開 |
|---|---|---|---|---|
| 1440×900 | [並排](1440x900-pair.png) | [灰階](1440x900-pair-gray.png) | [磨損](1440x900-wear-pair.png) | [地景](1440x900-terrain-pair.png) |
| 1024×768 | [並排](1024x768-pair.png) | [灰階](1024x768-pair-gray.png) | [磨損](1024x768-wear-pair.png) | [地景](1024x768-terrain-pair.png) |
| 390×844 | [並排](390x844-pair.png) | [灰階](390x844-pair-gray.png) | [磨損](390x844-wear-pair.png) | [地景](390x844-terrain-pair.png) |

UI 開關三個尺寸的節點位置差皆 **0 px**。另保留單張、面積遮罩、動態中間格與冷卻截圖。Python 語法編譯、重建一致性與 `git diff --check` 通過；`src/`、`card_face.js`、`ceremony.*`、`pool_data.py`、所有 `map-art/` 圖片及第一輪截圖與驗收紀錄皆未變動，正式 RATE 與初始券數未修改。
