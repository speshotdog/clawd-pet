本輪已產出單檔地圖、Playwright 驗收腳本與截圖，但依簡報的硬性規則，**本輪仍未完成驗收**：最終結果為 **160 PASS／1 FAIL**，唯一失敗項為 `cooldown.hidden_state`。未 commit、未 push；未修改任何既有追蹤檔案。

交付檔案：[map20.html](../../../../_art/holo-test/map20.html)、[check_map20.py](../../../../_art/holo-test/check_map20.py)、[完整驗收 JSON](acceptance.json)。HTML 可直接以 `file://` 開啟，沒有外部資源、npm、Canvas 或 WebGL。範圍僅包含地圖與狀態預覽；「記錄通關／記錄失敗」用來檢視地圖反應，沒有實作戰鬥。

已讀簡報指定的關卡規劃、C 視覺規劃、編隊與地圖設計（含第七節裁決）、完整 `ceremony.css` 以及 `src/clicker.css` 第 181 行附近實際覆寫的按鈕規則。實作保留五段新順序、每段三個一般關與一個王關、五個預告標記及其文字；未延伸到編隊、卡面、地景美術或王戰演出。

最終執行：`python _art/holo-test/check_map20.py`，Chromium **136.0.7103.25**，結束碼 **1**。以下均來自該次 `acceptance.json`，不是預估值。

| 量測項目 | 實測 | 規定區間 |
|---|---:|---:|
| 關卡／王關／路線邊數 | 20／5／19 | 20／5／19 |
| 一般節點本體 | 52 px | 44–60 px |
| 相鄰縱距／橫向偏移 | 120／36 px | 104–144／24–56 px |
| 普通輪廓對比 | 4.893:1 | 3–5:1 |
| 路線線寬 | 2.5 px | 2–3 px |
| 已走／未走 alpha | 0.72／0.28 | 0.65–0.80／0.22–0.34 |
| 路線分岔 | 0 處 | 0 處 |
| 當前括框間隙／對比 | 8 px／6.685:1 | 6–10 px／5–7:1 |
| 已通關勾號尺寸／對比 | 16 px／4.181:1 | 14–18 px／3–4.5:1 |
| 已通關、未解鎖標籤對比 | 6.686:1 | 4.5–7:1 |
| 未解鎖輪廓及鎖 alpha | 0.36 | 0.30–0.42 |
| 王節點寬度／一般節點 | 1.6 倍 | 1.4–1.8 倍 |
| 王節點外形對比 | 5.608:1 | 4–6:1 |
| 推進連線峰值 alpha | 0.84 | 0.78–0.90 |
| 靜止節點、括框切換、按壓位移 | 皆 0 px | 0 px |
| 已在視野內的鏡頭移動 | 0 px | 0 px |
| 遮擋時單次鏡頭移動 | 119 px，視窗高 13.222% | 12–28% |
| 減少動態追蹤位移 | 0 px | 0 px |
| 未解鎖站的可挑戰控制 | 0 個 | 0 個 |
| 任務死路／執行期錯誤／橫向溢出 | 0／0／0 px | 皆 0 |

控制元件亦通過：圓角 1 px、邊線 1 px、正文 16 px、標題 28 px、圖示 22 px、線寬 1.8 px。所有 `progress=0…20` 狀態、最終通關、王關失敗後 180 秒冷卻、冷卻期間禁用操作及到期恢復皆已跑過。

| 尺寸 | 共用導軌占比（65–85%） | 完整外框漂浮面板（0–15%） | 紋理支承區占比（12–22%） | 空白樣區灰階差（6–14／255） |
|---|---:|---:|---:|---:|
| 1440×900 | 65.963% | 0% | 12.993% | 12.214 |
| 1024×768 | 66.361% | 0% | 12.657% | 12.214 |
| 390×844 | 65.773% | 0% | 12.849% | 12.214 |

面積採螢幕可見區域的像素遮罩聯集，巢狀不重複計算。分母包含標頭、路線選單、摘要、節點本體與站名標籤；排除地圖空白。三個尺寸的導軌分子一致，只計路線選單與摘要，**不計標頭**。紋理分子是實際固定 SVG 刮痕的接縫支承條帶，並非把整塊帶紋理的面板算滿；它也不是「每條刮痕實際著色像素」的比例。已交付 `ui-mask`、`rail-mask`、`texture-mask` 供複核。空白樣區固定在標頭上緣、不含文字，座標記錄於 JSON。

| 時間項目 | 時間軸值 | 實際播放／操作三次平均 | 規定區間 |
|---|---:|---:|---:|
| 落勾 | 140 ms | 149.987 ms | 120–160 ms |
| 連線填滿 | 280 ms | 293.287 ms | 240–320 ms |
| 括框切換 | 190 ms | 196.620 ms | 160–220 ms |
| 遮擋時鏡頭 | 330 ms | 331.267 ms | 280–380 ms |
| 減少動態切換 | 130 ms | 未另量實際播放；三次讀取時間軸皆 130 ms | 100–160 ms |

推進三段依序排在 0／140／420 ms 起始。決定性截圖會先暫停 Web Animations API、設定 `currentTime`，再等雙重 rAF；另以瀏覽器時間軸記錄真正播放完成時刻，兩者分開呈現。上述絕對時間只描述本機這次執行，不用於跨機器比較。

**冷卻隱藏驗證需要實機量。** 冷卻用 `Date.now() + 180000`，沒有引用可見性感知時鐘。三次切換分頁的實際經過時間分別為 **5100／5100／5101 ms**，剩餘秒數皆減少 **5 秒**；但三次 `document.visibilityState` 都仍是 **visible**，`visibilitychange` 事件記錄皆為空。因此只能證明實時倒數，**不能證明分頁隱藏後倒數**，腳本明確將總結果判為 FAIL。

另外嘗試過本機 Chrome／Edge、Chrome 視窗模式、關閉 focus emulation、最小化瀏覽器等方式，這個執行環境仍回報 visible；未偽造 visibility 屬性或事件。可在可正常切換分頁的互動桌面重跑：

```powershell
python _art/holo-test/check_map20.py --headed --channel chrome
```

腳本仍會要求實際 `hidden` 狀態，不能僅靠選用 `--headed` 就過關。此項通過前，本輪不宣稱完成。

三組並排圖皆為左關右開，節點位置差 **0 px**，没有重排或縮圖：

| 尺寸 | 彩色 | 灰階 |
|---|---|---|
| 1440×900 | [原尺寸並排](1440x900-pair.png) | [灰階並排](1440x900-pair-gray.png) |
| 1024×768 | [原尺寸並排](1024x768-pair.png) | [灰階並排](1024x768-pair-gray.png) |
| 390×844 | [780×844 並排，每側 390×844](390x844-pair.png) | [原尺寸灰階](390x844-pair-gray.png) |

亦交付各尺寸單張 on／off／texture-off、推進中間格、減少動態與王關冷卻截圖。已目視檢查桌面、平板及原尺寸手機；灰階中是否足以辨識 C 的結構，仍保留給使用者人工裁決，未編造玩家辨識率。

`git diff --check` 與 Python 語法編譯通過。Git 狀態僅新增兩個交付程式與本報告／截圖目錄；`src/`、`card_face.js`、`ceremony.*`、`pool_data.py` 及正式數值均未變動。
