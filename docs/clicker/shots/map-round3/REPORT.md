本輪已建置交付物並跑完驗收，但尚未全數通過：**244 PASS／1 FAIL／2 NEEDS_DEVICE／1 BASELINE**。唯一 FAIL 是手機識別物可見面積 **10.459%**，未達 **12～25%**；手機構圖問題尚未完成。沒有 commit 或 push。

已先讀第二輪 brief 與自己的第二輪 REPORT，再依第三輪裁決實作；並回讀第一輪門檻、美術方向、複驗筆記、C 視覺規劃、美術座標報告與既有建置／驗收程式。

交付：[單檔 map20.html](../../../../_art/holo-test/map20.html)、[模板](../../../../_art/holo-test/map20.template.html)、[建置腳本](../../../../_art/holo-test/build_map20.py)、[驗收腳本](../../../../_art/holo-test/check_map20.py)、[透明圖](../../../../_art/holo-test/map-art/icons-sheet-cutout.png)、[acceptance.json](acceptance.json)、[執行紀錄](run.log)。

```powershell
python _art/holo-test/build_map20.py
python _art/holo-test/check_map20.py
```

最後驗收結束碼 **1**，Chromium **136.0.7103.25**。第二輪 JSON 實際有 **226 項**（224 個 PASS／FAIL 加 2 個 NEEDS_DEVICE），撤換兩條 alpha 後，本輪回歸 **224／224 項執行，遺漏 0**。閘口對比依新裁決保留 BASELINE，不冒充 PASS。

**去背與圖示**

從整張來源圖四個外緣做四向 flood fill，奶油候選條件為最小 RGB 通道 ≥235、最大最小通道差 ≤25。只移除外緣連通區域，僅修改 alpha，RGB 不變。閉合貼紙輪廓內的奶油底保留，因此閘口內部仍有奶油色負空間；四周不透明方形背景已移除。

以下以實際內嵌裁片為分母，保留＋移除等於裁片總像素。已逐一目視：勾號外框、鎖頭奶油提把、爪印外框與閘口外框都保留。細緣仍有少量來源抗鋸齒毛邊，沒有用濾鏡或重新上色掩蓋。

| 母題 | 保留像素 | 移除像素 | 去背前／後 |
|---|---:|---:|---|
| 勾號 | 63,790 | 87,306 | [配對](icon-check-cutout-pair.png) |
| 鎖 | 102,054 | 33,146 | [配對](icon-lock-cutout-pair.png) |
| 爪印 | 96,639 | 52,236 | [配對](icon-paw-cutout-pair.png) |
| 閘口 | 151,289 | 65,659 | [配對](icon-gate-cutout-pair.png) |

無損 WebP 解碼後與透明裁片的 RGBA 最大差皆 **0／255**。原始地景與圖示母片 SHA-256 與第二輪紀錄一致，設定與雜湊見 [build.json](build.json)。

普通節點 **60 px**、王節點 **96 px**（1.6 倍）、中心縱距 **144 px**、橫移 **36 px**。鎖頭盒 **58×58 px**，來源 y=360 外側兩條黑線為 12／11 px，採較細的 11 px 計算：**11×58÷416＝1.534 px**，通過 1.5～2.4 px。這是指定截面的外輪廓線寬，並非宣稱所有尖端與抗鋸齒像素都等厚。

第三輪明確要求放大點陣母題，因此 `complete.check_size`／`controls.icon_size` 舊 14～18／20～24 px 盒規格按新指令更新；名稱仍保留並執行，盒尺寸區間由來源線寬與新 1.5～2.4 px 推導。沒有宣稱仍符合舊小圖示盒，也沒有拿無作用的 SVG stroke 屬性充數。

閘口舊 4～6:1 門檻已撤回。以下以實際顯示的藍色填色像素，對同一座標、隱去節點後的地景量測新基準；逐點 RGB 與位置保存於 JSON。

| 尺寸 | 樣點數 | 閘口／地景中位數 | 最低～最高 |
|---|---:|---:|---:|
| 1440×900 | 185 | 5.675:1 | 2.003～11.638:1 |
| 1024×768 | 190 | 5.675:1 | 2.120～11.684:1 |
| 390×844 | 186 | 5.669:1 | 1.931～11.761:1 |

來源藍色對「保留下來的貼紙奶油底」仍為 **9.263:1**；另存這個歷史比較值，沒有把它當作地景對比，也沒有調色湊數。

**路線改量實際對比**

`route.walked_alpha`／`route.unwalked_alpha` 兩條被撤換的理由：插畫底色會變，CSS alpha 無法代表路線是否可見。新路線採 **3 px 黑色主線**、已走／未走透明度分級，沒有描邊或投影，總寬 **3 px**。alpha 只參與呈現，不再作靜態驗收標準；通關動畫原有峰值 alpha 回歸仍執行。

每隔 **8 px** 沿 SVG 路徑取樣，排除視窗外及節點／標籤／面板覆蓋部分；用路線開／關同構圖截圖讀取最近整數像素 RGB，不挑鄰近最亮或最暗像素。已走中位數需 3～6、未走需 1.6～2.4；最低 10% 中每一點都需 ≥下限×80%。樣本是 `progress=2` 三尺寸，並非全旅程所有像素的證明。桌面已走線段短，只有 3 個樣點，全部位置保留供複核。

| 尺寸 | 已走樣點 | 已走中位數／最差值 | 未走樣點 | 未走中位數／最差值 |
|---|---:|---:|---:|---:|
| 1440×900 | 3 | 4.628／4.452 | 13 | 2.131／1.971 |
| 1024×768 | 4 | 4.412／4.410 | 13 | 2.134／1.971 |
| 390×844 | 10 | 4.486／3.663 | 6 | 2.135／2.135 |

**地景、遮蔽與磨損**

桌面地景最大寬度縮為 1040 px，維持原比例；節點與地景共用捲動座標，沒有獨立視差。末端延伸地景，讓第五段也有完整捲動餘量。第二至五段仍標明第一段佔位。

| 尺寸 | 地景可見 | UI 覆蓋地景 | 識別物可見 | 五段看過高度：最低～最高 |
|---|---:|---:|---:|---:|
| 1440×900 | 60.807% | 10.220% | 17.257% | 71.336～71.394% |
| 1024×768 | 62.448% | 12.962% | 16.351% | 75.807～84.179% |
| 390×844 | 55.586% | 10.801% | **10.459%：FAIL** | 100～100% |

面積分母是完整 viewport，識別物沿用第二輪三個來源包圍框。圖示遮罩讀實際 RGBA（含 contain 比例），所有正 alpha 計 UI 足跡，僅不透明像素扣除地景。三尺寸識別物被 UI 遮住 **0%**、不透明核心遮擋 **0%**；全部 21 個 progress 入口的節點本體裁切 **0%**。手機面板 **240 px／28.436%**。

行程量測逐段捲動 4 站、432 px，以 12 px 步進，每步等待雙重 rAF，再把實際可見範圍反投影到來源高度；同一佔位圖跨鋪貼邊界時取來源區間聯集，不重複加總。這是手動走完一段的可見行程，不等同只按通關按鈕的自動鏡頭路程。每步 scrollTop／區間保留於 JSON。五段皆通過 70～100%。

中央來源 30% 帶沿用原地景並等比顯示，沒有添加碎石、刮痕或濾鏡。已目視三尺寸原圖：中央仍是大色塊與寬路徑，細節集中兩側。未編造玩家辨識率。

**手機構圖未解決**：目前地景寬 480 px，在約 374 px 的視窗左右裁切；放大後由第二輪 8.761% 增至 10.459%，仍低於 12%。這不是 UI 遮擋，遮擋已是 0%；增加倍率也會把兩側樹、柵欄與野餐布裁得更多。本輪沒有找到同時滿足面積與完整識別物構圖的呈現，因此保留 FAIL。不能把左右裁切說成已修好，也不能以全段高度通過替代靜態面積通過。後續仍須重新處理手機構圖。

| 尺寸 | 導軌／非卡片 UI（70～80%） | 紋理（14～19%） | 磨損灰階差（6～14／255） |
|---|---:|---:|---:|
| 1440×900 | 78.802% | 16.226% | 10.425 |
| 1024×768 | 75.117% | 17.981% | 9.186 |
| 390×844 | 71.105% | 15.691% | 7.929 |

**磨痕不是等距**。固定水平 SVG 主刮痕起點為 0、209、293、507、718、907，主間距 **209、84、214、211、189**；驗收把 x=2 轉角短痕納入後，序列為 **2、207、84、214、211、189**。垂直接縫主起點 y=0、153、246、443、584，間距 **153、93、197、141**。無亂數，重新載入路徑一致；面板內縮 24 px 的中央樣區，開／關灰階差三尺寸皆 **0**。

**冷卻與其餘回歸**

冷卻保持 `Date.now()+180000`。三次切頁經過 **5117／5102／5115 ms**，倒數均減 **5 秒**，但 visibilityState 仍為 visible、事件紀錄為空，所以兩個依賴真正隱藏狀態的項目標 NEEDS_DEVICE。另殺掉全部 timer，6 秒後以明確標示為合成的 visibilitychange 重繪，由 **03:00 → 02:54**；這項通過，沒有冒充真實隱藏分頁。

實際播放三次平均：落勾 **150.013 ms**、連線 **293.347 ms**、括框 **196.580 ms**、鏡頭 **339.867 ms**；鏡頭位移 **14.556%**。20 站／5 王／19 線、無分岔、無任務死路、減少動態與執行期錯誤回歸皆通過。

HTML **843,416 bytes**，低於 2.5 MB；地景 WebP 品質 **94**、原尺寸 **971×1619**，外部請求 **0**。截圖使用 WAAPI 暫停、設定 currentTime、雙重 rAF。重建 HTML／透明圖雜湊一致，見 [rebuild.json](rebuild.json)。Python 語法解析與 git diff --check 通過。

並排圖皆左關右開、不縮圖，手機每側 **390×844**。路線圖青色為已走、洋紅為未走，數字是該點對比。

| 尺寸 | UI 關／開 | 灰階 | 磨損關／開 | 地景關／開 | 路線取樣 |
|---|---|---|---|---|---|
| 1440×900 | [並排](1440x900-pair.png) | [灰階](1440x900-pair-gray.png) | [磨損](1440x900-wear-pair.png) | [地景](1440x900-terrain-pair.png) | [逐點對比](1440x900-route-samples.png) |
| 1024×768 | [並排](1024x768-pair.png) | [灰階](1024x768-pair-gray.png) | [磨損](1024x768-wear-pair.png) | [地景](1024x768-terrain-pair.png) | [逐點對比](1024x768-route-samples.png) |
| 390×844 | [並排](390x844-pair.png) | [灰階](390x844-pair-gray.png) | [磨損](390x844-wear-pair.png) | [地景](390x844-terrain-pair.png) | [逐點對比](390x844-route-samples.png) |

改動限於 map20 成品／模板、建置與驗收腳本、新增透明圖與本輪驗收目錄。`src/`、`card_face.js`、`ceremony.*`、`pool_data.py`、地景原圖、圖示原圖、第二輪紀錄、正式 RATE 與初始券數未修改。沒有 commit 或 push。

