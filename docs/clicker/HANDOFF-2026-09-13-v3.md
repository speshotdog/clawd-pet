# 交接：1.0 數值 v3（balance-v3 分支，2026-09-13）

**接手先讀這份。** 規格 `DESIGN-balance-v3.md`（含 §十三～十六 追加定案）、試做報告 `REPORT-2026-09-12-balance-v3-prototype.md`（含 §二之二、二之三 的模擬數字）、末世架構 `DESIGN-2026-09-12-content-architecture.md`（在 holo-5.0）。

## 〇、2026-09-13 深夜收工狀態（下次開工先看這裡）

- **沒有上線**（使用者：先不上）。`balance-v3` 與 `holo-5.0` 都已推到 origin；main 仍在 `276e6fc`、gh-pages 沒動、exe 沒出。
- balance-v3 對 main 是 fast-forward；上線流程：併 main → `python tools/export-web.py` → 照 `HANDOFF-next-session.md` 二節推 gh-pages → `npm run build` 出 exe。**每一步都要單獨問**。
- 試玩：`python -m http.server 8123 -d dist-web`（dist-web 已是最新 export）。
- 做到哪：三之一～三之五。最新一版＝末世關卡能打（三之五）。
- 下一步候選（使用者還沒排）：① 實玩調 `ApocEconomy.RULES` 數字；② 王關機制（護盾／召喚／節奏）；③ 獨立技能格效果；④ 舊測試基準重寫（round14/16/17/19/20、clicker-browser round7）；⑤ 觀星動態卡；⑥ 黑角回報。
- 5.0 卡的教訓與工法：`REPORT-5.0-cards.md` 末三節（不摳圖、臉置中 40%、字型子集要重跑 `build_round4_fonts.py`）。

### 〇之二、接續 09-13 收工後的這一輪（仍未上線）

- **修掉招募的死畫面**（使用者回報：卡片在、按鈕全沒了）。真因在 `clicker-save.js` 的「王包」驗證：
  v3 定案「路障小王不鎖招募」，但驗證仍禁止 `boss` 與 `pending` 共存。招募層蓋著時背景照樣拆包，
  拆到第 10 包自動開打路障 → 下一次 `purchaseDraw` 的 pending 永遠存不進去 → `commit` 失敗、消費鎖住，
  而 `collect(stay)` 已先把兩顆收下鍵藏掉。三處修正：驗證放行小王（大王照舊互斥）、
  `start()` 回傳有沒有真的開演（起不來就 `close()` 收尾）、`open()` 只擋大王（重開才收得下 pending）。
  回歸測試在 `clicker-v3.test.js` 最後一則；重現腳本跑 30 輪連抽不再出現。
- **底欄／頂欄順序**（使用者指定）：統計、場景移到頂欄錢幣盤左右各一（直式收成純圖示）；
  底欄改成 換桌布 → 編隊 → 名冊 → 商店。補圖示：場景＝`clicker-ui-scene.png`（Codex 生的票券）、
  換桌布＝桌墊、編隊＝隊旗、末世地圖＝滅世珍獸。標題膠帶 250→224 讓位給「統計」。
- `npm test` 190；v3 七支＋round32/33 全綠。

### 〇之三、同一輪的後半：交接第五節清單全推（仍未上線）

使用者：「全都做，需要決策的問我」。決策只問了兩題：末世節奏（選 3～5 天）、
王關機制（選護盾＋節奏）、技能格（選沿用 1.0 語意）。其餘自己做完。

| 項目 | 結果 |
|---|---|
| 末世數值 | `tools/sim/apoc.js`／`apoc-sweep.js` 掃出定案：第 1 站 4 分 → 第 20 站 33 分、累計 202 分鐘、第 4 天全線、抽 210 次、王關失敗 0 次 |
| 王關機制 | 護盾（放置只剩 35%）＋節奏（點滿 15 下破防 8 秒、全傷害 ×2、門檻 ×1.25）；**時限拿掉**（模擬器量到限時會讓一輪失敗 800 多次） |
| 技能格 | 由卡的稀有度決定：神話 10 下 ×10／傳說 15 下 ×3／史詩 全隊 ×1.5 二十秒／精良 其他格 −8 秒 |
| 末世星數 | team20 卡面右上 ★張數（`setStars()`，build_apoc.py 打補丁） |
| 舊測試基準 | round14/16/17/19/20 ＋ `clicker-browser.py` 全部改成對不變式或對現行定案，**全綠** |
| 黑角 | 補掃演出途中（`tools/test/clicker-v3-black-corner.py`）：結果頁有鑑別力且乾淨；演出途中負控制咬不動，如實列為「綠燈不算數」。見 `VERIFY-black-corner.md` 第五節 |
| 觀星動態卡 | 素材與比對頁做好（holo-5.0 `5f29f17`），**沒接進卡池**——`card_assets.py` 會把動畫壓成一格，要接得動凍結檔案 |
| 圖示 | 場景＝票券、換桌布＝翻角桌巾（都是 Codex imagegen 生的） |

**我原本回報的「`#draw-five` 在小王演出中看起來可按、按了沒反應」是錯的**：
`numbers()` 每秒會重跑 `gacha.render()`，那顆鍵在 `bossBusy` 期間確實是 disabled，只是最多晚 1 秒。

驗收：`npm test` 194；v3 七支＋apoc 兩支＋round14/16/17/19/20＋clicker-browser＋黑角 全綠。

### 〇之四、末世改用 1.0 介面（使用者：「整體 UI 邏輯非常差勁」）

使用者回報：末世按鈕裁切、技能格沒縮圖、沒有卡冊、地圖沒有可玩畫面；指示是
「1.0 的版本比較好，乾脆把 1.0 的 UI 介面稍微調色拿來用就好，但卡片要是末世卡版本」、
「末世地圖的選項放在場景裡面⋯⋯兩個主系統，選哪個就算重新整理了」、
「兩邊邏輯不要差太多，不同的是 UI 的色調」。

**根因是同一個：末世是把原型頁縮進 22 MB 的 iframe。所以 iframe 整個拿掉。**

| 項目 | 現在長怎樣 |
|---|---|
| 架構 | 末世直接畫進 1.0 的節點（`clicker-apoc-ui.js`），`body[data-world="apoc"]` 換色票 |
| 卡面 | 71 張輸出成 `src/apoc/cards/*.webp`（0.77 MB），欄位形狀同 `GachaPool.CATALOG` |
| 切換 | 場景面板：七站＋末世並排，存進 `settings.world` |
| 招募 | 兩個世界共用同一層與五種演出，`clicker-gacha.js` 的 `W()` 轉接器只換卡池／貨幣／落帳 |
| 卡冊・編隊・拖曳 | 同樣各加一層世界轉接器 |
| 升階／超越 | 改成收下時自動做（`E.autoGrow`），詳情頁不再放那兩顆鍵 |
| 抽卡結算 | 新的 `#draw-summary`：新夥伴／升星／自動升階／自動超越／粉塵 |
| 刪掉 | `clicker-apoc.js`、`src/apoc/index.html`（22 MB）、`gacha.html`（19 MB）、`build_apoc.py`、`extra.html`；dist-web 104 → 64 MB |

**UI 體檢器 `tools/test/clicker-ui-audit.py`**：字被裁／出界／被蓋住／按鈕互疊／按鈕在畫面外，
46 個畫面 × 桌機手機。第一版報 1504 條（大多是假陽性），扣掉之後 86 條真的，逐條修完 0 條。
順便抓到三個本來就有的手機版臭蟲（場景票券寫死兩欄 300px、徽章牆九欄、分享鍵在畫面外）。

**Codex gpt-6-astra 複檢三輪**（`VERDICT-apoc-ui{,-2,-3}.md`）：第一輪八條、第二輪十四條，
全部處理完。兩條用負控制證實過：拿掉快取作廢，體檢器會報「切回桌邊留著 10 張末世卡」；
拿掉 pending 恢復，會報「末世重開後的待收下：找不到 #collect」。

驗收：`npm test` 199；UI 體檢 46 畫面 0 條；v3 六支＋末世兩支＋round14/16/17/19/20＋
`clicker-browser` 全綠。**仍未上線**（main 與 gh-pages 都沒動）。

### 〇之五、末世改回精裝：卡面、模式、關卡地圖、精裝典藏包（仍未上線）

使用者退回〇之四：「末世的卡怎麼還有舊的圖繪」「新的抽卡 UI 被你毀了」「**沒有的 UI 才用舊的改色**，
不然之前做這麼辛苦的東西不就白費了」「**模式是額外的大選項，切換後就是之前做的關卡地圖**」。
問答定案：戰鬥畫面＝1.0；卡池只要精裝（71 張全是）；卡面＝HoloCardFace；抽卡演出＝精裝典藏包。

- **根因**：〇之四的 `export_cards.py` 把去背主體壓在黑底、沒有卡框 → 精裝卡看起來像舊圖。已刪掉那支與 71 張輸出。
- **精裝卡面**：`tools/apoc/build_holo.py` 從 holo-5.0 拆出卡面 CSS／圖層／遮罩／字型成實體檔（`src/apoc/`）；
  `src/clicker-holo.js` 用 shadow root 包 HoloCardFace。夥伴列、技能格、卡冊、編隊、結算卡、拖曳殘影都換了。
  ⚠ 遮罩網址要絕對（自訂屬性裡的相對網址以 `apoc/holo.css` 為基準）。
- **模式**：頁尾第一顆 `#mode-open` → 兩張卡（珍母點點／末世）。場景面板不再有末世；末世裡場景鍵叫「地圖」。
- **關卡地圖**：`tools/apoc/build_map.py`（素材）＋`src/clicker-apoc-map.js`（照 map20 原型：20 站、五區段、插畫地形）。
  末世一進來就是地圖，「進入戰鬥」才是 1.0 戰鬥畫面。直式另有一套版面。
- **精裝典藏包**：`tools/apoc/build_ceremony.py` → `src/apoc/ceremony.html`（81 KB，演出本體不改）。
  扣券與結果先在主頁寫進 `apoc.pending`，再交給 iframe 演出；收下時 postMessage 回主頁落帳＋結算卡；重新整理會回到結果頁。
- **UI 體檢器**：兩條判準修正（`overflow:visible` 的字只看是否超出外層裁切容器；捲出捲動容器的鍵不檢查被蓋住），
  有反向對照。56 畫面 0 條。
- 驗收：`npm test` 200、`clicker-v3-apoc.py`／`clicker-v3-apoc-play.py` 全綠。
  `clicker-browser.py` 第 944 行 `#hero > svg` 偶發失敗，A/B 實測 HEAD 也是 3 次 1 次（點擊區啟用當下主角還沒掛上），不是這輪造成。
- 複檢：`REVIEW-BRIEF-apoc-deluxe.md` → `VERDICT-apoc-deluxe.md`。
- **Codex 複檢四條必修已修，逐條瀏覽器實證**（`codex_fix_probe` 的做法：真的讓 `localStorage.setItem` 丟例外、真的轉直式）：
  - B1 收下時存檔失敗 → 典藏包已清場卻落帳不成。改成把同一批結果放回結果頁；存檔鎖住時收下會先自己重試儲存（主頁的重試鍵被典藏包蓋住按不到）。
  - B2 桌邊選了只支援多連的演出，末世單抽被鎖 → 末世的單抽支援不看桌邊演出。
  - B3 地圖開著轉直式，進戰鬥舞台縮成 0 → 關地圖時重跑 `fitStage()`。
  - B4 模式面板按 Esc 掉進 `closeWindow()` → `closeTopPanel()` 補上 `modes`。
  - 值得修也一起做了：精裝卡面離開 DOM 5 秒後 `unobserve`；體檢器的裁切容器一路往外查；地形貼圖改順排（原本第二張起全落在容器外）。
- 舊測試：round21／round31 在 HEAD 就已失敗（A/B 實測），是測試跟不上 09-13 的設計（抽卡結算卡、頁尾顆數），已改測試。
  black-corner 原本開的是已刪的 `apoc/gacha.html`，改成驅動 `apoc/ceremony.html`。

### 〇之六、末世第二輪回饋：舊化 1.0 配色、技能施放、卡片旋轉、編隊版面（仍未上線）

使用者看完 `38660a7`：字難讀（深色底深色字）、卡冊星星要圖示、詳情卡要能用滑鼠轉＋反光、
主畫面「什麼都看不到」、技能要有 1.0 的施放演出、技能槽不要兩行、編隊資訊要壓縮／成員直接放精裝卡／詳情卡不要外框、
配色「說不定可以變成舊化材質的 1.0」。

- **主畫面夥伴列是桌邊的**（根因）：`clicker.js` 初始化不分世界呼叫 `stage.setPartners()`，從末世重新整理就被蓋掉，
  末世的快取又以為沒變。修：初始化分世界＋`#buddies`／`#slots` 標 `data-world="apoc"` 所有權，被蓋就重畫。
- **配色**：做了深藍／舊化／焦黑三張樣張，使用者選**舊化 1.0**。`tools/apoc/build_theme.py` 把 1.0 貼圖做舊
  → `src/apoc/theme/aged/`；`src/apoc/theme.css`（`body[data-apoc-theme="aged"]`，進末世掛上）。
  外層面板 9-slice、內層分區不貼紙（貼圖撐滿會變雙層框＋框角壓字）；戰鬥舞台墊廢墟後院插畫。
  WCAG 對比掃描修到只剩刻意半透明的停用翻頁鍵。
- **技能施放**：`ClickerCutin.play()` 接 `effect.spec／entry／actor`，末世同一套分鏡、主角是精裝卡。
- **卡冊**：星星圖示；詳情卡 `ClickerHolo.interactive()`（照典藏包手感：反光跟游標、拖曳轉、放開回正）。
- **編隊**：技能槽一排四格（直式只放卡）；四條上限一排四格；末世成員＝裸精裝卡；詳情卡無外框。
  ⚠ 規則要帶 `#game`／`#roster`，1.0 的 `#game button` 權重會把奶油框畫回來。
- 驗收：`npm test` 200、UI 體檢 56 畫面 0 條、`clicker-v3-apoc`／`apoc-play` 全綠。複檢：`REVIEW-BRIEF-apoc-aged.md` → `VERDICT-apoc-aged.md`。
- **Codex 第二輪複檢**（`VERDICT-apoc-aged.md`）兩條必修已修並實證：抽卡結算／全線通行在紙底上仍是深藍版淺字（1.74:1 → 7.52:1）、
  卡片放開後游標一動回正就停在半途（回正中且非拖曳時不打斷）。值得修的也做了：直式技能格補 title、桌邊重畫時清掉 `data-world` 標記。
- **round26 偶發失敗查到底**（HEAD 一樣會壞，不是這輪造成）：測試直接 `skillSlots[0]=src`，輪到 `qinghua` 時同一張卡在兩格，
  剛好碰上每 5 秒的自動存檔就「存檔驗證失敗：重複槽位」→ 消費鎖住 → 招募鍵永遠灰。遊戲的鎖住行為是對的，改測試（已在槽就不塞）。
  同一段還有第二個搶快：`mythicSkill` 讀舞台快取的 `latestState.skillSlots`、特效掛在已畫好的技能鈕上，改完槽要等每秒的 `changed()` 畫好（1.3 秒）再發動。修完連跑兩次全過。
  追法：替 `#recruit-five.disabled` 掛 setter 抓呼叫堆疊＋同時看 `#mode-select`／`#collect` 的停用狀態反推 `store.blocked`。
- round32 單獨跑會過，整批跑時跟別的瀏覽器測試搶 CPU 偶發逾時。

### 〇之七、末世第三輪回饋：技能貼紙、怪置中＋點擊回饋、直接花錢抽卡、商店／戰績、收藏卡精裝（仍未上線）

使用者看完 `6cc7a53` 的六條回饋與後續拍板（原話重點）：
技能格「不要把整張卡放上去」；怪要置中、「點下去什麼都沒有」「怪甚至跑出螢幕外」；
「換金卷再去抽有點脫褲子放屁」→ 追問兩次才定案：**「我幹嘛拿錢換卷再抽卡，為什麼不直接花錢抽卡」**、
**「末世幣就是只能用在 2.0；1.0 的幣可以讓玩家用很差的比值去交換 2.0 抽卡卷，讓玩家覺得 1.0 練的卡還有一點用處」**、
「隊伍訓練本來就要有這功能」「打完王現在會給很多錢，不用特別送抽卡卷」；按鈕配置選樣張 A；統計／商店做、輪迴先放鍵；
收藏卡卡冊「做去哪了」。**中途嚴正抗議：「2.0 不准出現任何 1.0 卡面，魔花少女是之前花最多心力作的卡」**。

| 項目 | 做法 |
|---|---|
| 技能格 | 圓形貼紙頭像：外圈稀有度色（神話彩虹）、裡面是精裝卡主體圖；取景 `tools/apoc/build_sticker.py` → `src/apoc/sticker.js`（alpha 外框取景）；冷卻用 1.0 同一個圓形遮罩。夥伴列維持精裝卡（使用者只點名技能格） |
| 怪置中 | **根因**：`#apoc-enemy` 用 `transform:translate(-50%,-50%)` 置中，點擊的 WAAPI `transform:scale()` 整個蓋掉 → 一點就往右下跳半身、出舞台。改成獨立的 `translate` 屬性，中心 (304,150)；`#tap` 跟著移。怪鳥／灰狼是 4 格動畫表，整張放會變「一排三四隻」，`object-view-box` 只取第一格 |
| 點擊回饋 | `clicker-apoc-ui.js` 自己畫（1.0 stage 在末世是 stop 的，不能叫它的 float/burst）：傷害浮字、碎片＋火花、閃白壓扁；爆擊／破盾／擊倒加衝擊圖與震動；放置打死也有擊倒演出。浮字＝`A.tapDamage()`（跟實際扣血一致）。座標先換螢幕座標再換進 `#floaters`／`#click-fx`（直式兩層盒子不同） |
| 錢 | **末世金幣只在 2.0 用**：直接抽卡（1000 × 1.02^付費抽數）、點擊力（+5%／級，20000 × 2^L）、全隊訓練（+3%／級，40000 × 2^L）。**券只從桌邊金幣換（時薪券）**：一張＝桌邊每秒收益 ×600 秒，當天每多換一張 ×1.25、隔天重置；抽卡先用券。沒有打站送券。舊檔 `ticketsBought` 搬成 `paidDraws`（價格不倒退），手上的券保留 |
| 換券定價的漏洞 | 只用當下每秒收益會被鑽：把桌邊隊伍換成一張弱卡、每秒收益掉下去 → 券變超便宜。基準改成 `max(當下, 10^peakRateStamp)`（1.0「每秒破 10^n」紀錄只會往上）。`clicker-v3-apoc.py` 有這條 |
| 商店三張卡（樣張 A） | 點擊力｜全隊訓練｜招募，招募下一行「用桌邊金幣換券」→ 末世商店。⚠ 這行字要短：直式跟價牌共用一欄，長字把欄撐寬、十連鍵被擠出畫面（UI 體檢抓到） |
| 頂列／頁尾 | 兩個世界同位置：統計→**末世戰績**（借 `#stats`：10 格數字＋共用的匯出／匯入存檔——以前末世連匯出存檔都點不到）；商店→**末世商店**（借 `#wardrobe`：兌換所＋受擊特效外觀 4 種，末世金幣買）；換桌布→**重走廢土**（停用，全線通行後開放，內容未設計） |
| 收藏卡卡冊 | 以前 `#collect-open` 兩個世界都沒打開過（桌邊只排在卡冊最後一頁，末世卡冊只列末世卡池）。現在兩邊有收藏卡就顯示；**末世裡魔花少女嵌精裝頁** `apoc/mohuashaonv.html?embed=1`（`data-embed` 藏標題／提示、透明底；詳情可傾斜、點一下做動作） |
| 2.0 不准出現 1.0 卡面 | 卡冊、編隊（縮圖＋詳情）、抽卡結算、拖曳殘影、技能格、夥伴列的 fallback 全部改成空白底 `.apoc-face-blank`；`clicker-v3-apoc.py` 掃「2.0 畫面沒有 `.character-png`／非精裝 `.card`」，並**進 iframe 裡**確認嵌入模式真的生效 |

模擬（`node tools/sim/apoc.js`，一天三段 20 分）：改版前 202 分・抽 210；新經濟不訓練 218 分；**定案 201 分・第 4 天・抽 200**；
＋換券（1.0 剛解鎖 P1e10／存 1e14）186 分・換到 45 張；（後期 P1e12／存 1e17）181 分・48 張——沒有一次換爆。
`tools/sim/apoc.js` 可用 `ONE_P`／`ONE_COINS`／`TEAM_*`／`CLICK_*`／`TRAIN_SHARE` 覆寫。

**這一輪踩到的坑（下一個人別再踩）**
- **DOM 檢查全綠 ≠ 看起來對**。收藏卡第一版用了 1.0 平面卡、測試只數元素就綠了；嵌入模式壞掉（標題＋深紫底還在）時測試只看 iframe src 也是綠的。視覺改動一定要自己打開截圖看。
- **從 Python 往 JS 塞字串別用反斜線跳脫**：`'\\b'` 在一般字串被寫成退格字元 `\x08`，正則 `/[?&]embed\b/` 永遠不成立。
- **Windows 上 Python `write_text` 會把 `\n` 寫成 `\r\n`**：10 MB 的精裝頁整份換行被改。用 `write_bytes` 或 `newline=''`。
- bash heredoc 塞大段 JS 會被引號搞壞（unexpected EOF）；大段程式先寫成檔案再用 Python 拼接。

驗收（含〇之七之二的修正之後）：`npm test` 203；UI 體檢 56 畫面 0 條（第一次 4 條全是直式十連鍵被換券入口擠出畫面，已修）；
`clicker-v3-apoc`／`apoc-play`／gate／prestige／cleanup／team／teamui／drag／black-corner、round14／16／17／19／20、`clicker-browser` 全過。
複檢：`REVIEW-BRIEF-apoc-round3.md` → `VERDICT-apoc-round3.md`。

#### 〇之七之二、Codex 第三輪複檢（`VERDICT-apoc-round3.md`）與處理

| 等級 | 問題 | 處理 |
|---|---|---|
| 必修 | 換弱隊仍可把券價壓到九分之一：`peakRateStamp` 只存指數（9e10 記成 10） | 另存精確值 `apoc.onePeak`：桌邊每秒 tick 只往上記（在 apoc 底下，換桌布不歸零）；定價＝`max(當下, onePeak, 10^peakRateStamp)` |
| 必修 | 桌邊商店留下的 `shopCategory` 讓末世商店按 Esc 重畫成桌邊商店 | `album.escape()` 在末世不走分類返回，直接關 |
| 值得修 | 點怪後立刻招募，末世粒子畫到招募層的畫布；招募中放置擊倒又把畫布切回舞台 | `pauseStage()` 叫 `apocUI.stopFx()`；招募層開著時不噴粒子、不演擊倒 |
| 值得修 | 末世商店以每秒增加的餘額當重建條件，Tab 焦點每秒被刪 | 快取鍵改成「買不買得起」 |
| 值得修 | 日期往返（前進一天再調回）可重置當天加價 | 日界只往前走：`exchangeToday` 對「今天 ≤ 紀錄日」沿用張數，`exchange.day` 取 max |
| 不用修 | 舊券／付費進度／pending 收下、換券兩邊同一次 commit、貼紙資料、收藏卡 iframe 不會每秒重載 | Codex 實測過，維持 |

⚠ 我自己的測試踩的坑：驗證換券定價時把種子的桌邊金幣拉到 `coins=1e20`，但 `lifetimeCoins` 還是 1e13——
`clicker-save.js` 要求「累計收入 ≥ 手上金幣」→ 驗證失敗、消費鎖住，後面「換券」「切回桌邊」「色票拿掉」一連五六條一起紅。
（第一次我以為是改了隊伍，拿掉之後照樣紅；用 `ClickerSave.validate` 逐項試才抓到真兇。）
修法：lifetimeCoins 一起拉，並加「存檔沒有鎖住」的檢查，種子改壞時會先紅這一條、不會連坐。

### 〇之八、末世第四輪回饋：自動接關、點擊撐滿舞台、結果卡徽章、1.0 加成套進 2.0、放大鏡、1.0 統計新版（仍未上線）

使用者看完 `5583ae4`（原話重點）：「打死上一隻怪物之後下一隻出現，然後就無法點了」「點擊範圍不要只有怪物本身，空白地方也要能造成傷害（除了王的特殊機制）」
「編隊名字非常不好閱讀，下面有黑色陰影，加上他是黑字」「抽卡結算直接提示在抽卡結果，不是用一個額外視窗」
「末世地圖的預設畫面是戰鬥畫面，不是選擇關卡」「印記商店加成 2.0 吃得到嗎？2.0 也要有印記商店」
「2.0 技能雖然重新設計，但名稱不要改，1.0 有的名稱就直接沿用」「卡面下方要有放大鏡，放大到畫面正中間、背景變暗，好好把弄觀賞」
「1.0 的統計也改成新版，比較好閱讀」；中途追加王關：「這不是動圖」「字體幾乎看不到，顏色問題」「第一次遭遇應該直接進入關卡，失敗之後才會在右上方有進入選項」。
**「跟我說話請用繁體中文」。**

問答定案兩題：**2.0 印記＝1.0 的印記加成直接套進 2.0**（不另做 2.0 商店）；**結算提示兩個世界都改**（拿掉「收下了！」）。

| 項目 | 做法 |
|---|---|
| 打死之後點不動 | 根因：打死後 `stage=null`，要再按「開戰」，點擊區直接 return。改 `autoFight()`：戰鬥畫面上自動接下一站（地圖／招募層／結局開著、隊伍空、這一站的王輸過都不自動）；`tap()` 沒有戰鬥時先自動開打再算傷害 |
| 點擊範圍 | `#tap` 撐滿舞台 608×360；受擊特效固定打在怪身上；王的護盾照舊每下都算破盾 |
| 王關 | 怪鳥／灰狼 4 格動畫表用 `object-view-box` 逐格播（照 1.0：每格 130ms、`steps(4, jump-none)`）；舊化配色的護盾／破防標籤深墨字；`bossFailed`：這一站的王輸過才停止自動開打、開戰鍵寫「再次挑戰」。⚠ `RULES.BOSS_TIME` 目前是 0，正常遊戲裡王不會輸——要不要加回時限是設計決定，還沒問 |
| 戰鬥畫面預設 | 三個進末世的入口 `apocMap.open()` → `close()`；地圖只從頂列「地圖」開 |
| 1.0 加成套進 2.0 | `clicker-apoc-ui.js` 的 `oneBoost(s)` 照 1.0 `rates()`：戰力 ×markMul×blessMul、點擊 ×(1+10%×攻擊力祝福)、技能效果 ×(1+5%×技能祝福)、冷卻 ×(1−2%×冷卻祝福)。每次從 1.0 存檔現算掛在 `a.boost`，提交前刪、`normalize` 也刪，不寫進存檔。戰績多一格「1.0 印記加成」 |
| 技能名 | `skillOf` 查 1.0 同名角色的 `characters[id].skill`（例：快樂海豹→「快樂拍拍」、滅世珍獸→「滅世光線」），效果仍照稀有度；1.0 沒有這張卡維持稀有度的名字 |
| 結果卡徽章 | `clicker-gacha.js` 的 `drawBadges()`：收下前用同一份 pending 試算 `W().collect()`，照抽到順序標「新夥伴／★a→★b／升階／超越」。1.0 掛在 `#cards` 子節點；末世傳給典藏包，`build_ceremony.py` 的橋接包一層 `completeSlot` 掛 `.apoc-badges`（產生器重跑，先確認過重跑前後產物一模一樣）。拿掉 `showDrawSummary`／`#draw-summary`；萬用粉塵溢出改寫在結果頁提示字 |
| 編隊卡名 | 精裝卡的深色光暈壓在名字上 → 名字墊紙色底、跟卡拉開 |
| 放大鏡 | 卡冊詳情卡下方「🔍放大」→ `#card-zoom`：末世精裝卡面（拖曳轉、反光跟游標）、收藏卡嵌精裝頁、1.0 卡**保持原尺寸整張 `scale(--zoom-k)`**（1.0 卡內部是固定像素排版，撐大外框只會變大空框）；直式以寬為準；Esc／關閉詳情先收放大層 |
| 1.0 統計 | 生涯數字從一整行「｜」改成數字格 `#stats-tiles`（跟末世戰績共用 `.stat-tile`）；直式一排 4 格、字縮小 |

**這一輪踩到的坑**
- **測試種子不合法 → 存檔鎖住 → 末世整個停止重畫**，看起來像「加成沒套上」。`blessing=5` 要先有 `marksClaimed≥15`（印記商店驗證）；`coins` 要 ≤ `lifetimeCoins`。追法：攔截 `#click-rate` 的 textContent 寫入發現 2.5 秒沒人寫 → `ClickerSave.validate` 直接印錯誤。測試裡都加了「存檔沒有鎖住」的檢查。
- 1.0 五連沒有徽章不一定是壞：種子每張 4 顆粉塵，再抽到 5 顆仍是 3★，沒新卡也沒升星本來就不標。測試改成只留 3 位夥伴。
- 從 Python 用字串比對打補丁，中文＋跳脫（`'\\n'`、`\b`）很容易對不到；大段內容先寫成檔案、用純英數定位點或行號替換。
- 詳情卡的進場動畫 `fill:forwards` 停在 `scale(1.15)`，蓋過直式的 `scale(1)`——直式其實一直是放大的，下方元素要留 44px。
- 怪鳥／灰狼的動畫表圖案本身貼齊格線，翅膀在格邊被切齊，是素材本身（1.0 同圖同樣），不是取景錯。

驗收：`npm test` 203（末世經濟 20）；UI 體檢 54 畫面 0 條（過程中抓到直式 1.0 統計被推出畫面 26 條、直式放大鍵被卡面蓋住 1 條，都修了）；
`clicker-v3-apoc`／`apoc-play` 全過；截圖都親眼看過。複檢：`REVIEW-BRIEF-apoc-round4.md` → `VERDICT-apoc-round4.md`。

#### 〇之八之二、Codex 第四輪複檢（`VERDICT-apoc-round4.md`）與處理

| 等級 | 問題 | 處理與實證 |
|---|---|---|
| 必修 | 放大卡一按就關：`#card-zoom` 掛在 `#game`、不在 `.panel` 裡，按住卡面被 `pointerdown` 判成「點面板外」→ `closeZoom()` | 面板外點擊的排除清單加 `#card-zoom`。`clicker-v3-apoc.py`：按住卡面拖曳後放大層還在 |
| 必修 | 典藏包還沒收下，背景照樣自動接下一站：`autoFight()` 只擋 `#recruit-layer`，典藏包是另一層 `#apoc-ceremony`，也沒擋 `apoc.pending` | 兩個都擋。`clicker-v3-apoc-play.py`：典藏包開著時清掉戰鬥，等兩個 tick 仍沒開打 |
| 值得修 | 1.0 連抽最後收下，`close()` 播累積入隊後 `collect()` 又播本包 → 同一隻飛兩次 | 本包先併進 `pendingJoins`，統一由 `close()` 去重播一次；連抽起不來也只叫 `close()` |
| 值得修 | 放大層 Tab 焦點跑回背後卡冊；Esc 關掉沒還焦點 | Tab 焦點循環優先 `#card-zoom`；開啟時記下來源鍵、關閉時還回去。測試有這兩條 |
| 值得修 | 1.0 放大卡轉向後 `--zoom-k` 沒重算 | `album.relayout()` 裡 `refitZoom()`：重量 1.0 卡比例、精裝卡面 refit |
| 值得修 | 我插 `badgeNode()` 時把 `summary()` 結尾的 `$('collect').focus(); render();` 擠到 `return box` 後面，變成死碼 | 移回 `summary()` 結尾 |
| 不用修 | 招募層沒掛 boost、徽章試算的副作用、王輸過旗標擋不擋手動重打、典藏包橋接 skip／restore／清場 | Codex 記憶體實測過，維持 |

驗收（修完之後）：`npm test` 203；`clicker-v3-apoc`／`apoc-play`（含上面四條新實證）全過；v3 gate／prestige／cleanup／team／teamui／drag／black-corner、round14／16／17／19／20 全過；UI 體檢 54 畫面 0 條。

## 一、狀態一眼看

- 分支 `balance-v3`，worktree `D:\claude研究\clawd-pet-v3`，從 main `276e6fc` 開。**沒有上線、沒有出 exe、沒有動 gh-pages**（使用者：不更新網頁）。
- 程式全部由 Opus 做；素材用 SVG／CSS 佔位（使用者：先用簡單 SVG 排版面）；09-13 晚 Codex 額度恢復，生圖派給它（見三之三）。
- `npm test` **182**；Playwright：五支 v3（`clicker-v3-{gate,prestige,cleanup,team,drag}.py`）＋十一支舊套件全綠。

## 二、規則（都已落地在 `src/`）

| 規則 | 定案來源 |
|---|---|
| 小王每 10 包（門檻包之前）、30 秒、`GATE_MUL 2`×當前包需求、area 第 5 隻 ×1.5；**第一次遇到自動開打，輸過就等玩家點「挑戰」**（冷卻 30 秒） | 使用者 09-12 深夜 |
| **30 秒火力 ≥ 血量 10 倍的小王直接讓路**（給獎金、不演出）——剛換桌布或壓倒性強的時候不會每 10 包看一次動畫 | 09-13 修舊套件時發現的噪音 |
| 大王血量 `BOSS_K[站] × 門檻包需求 × 站係數`，K＝10/8/6/5/8/8/2；每輪可重打；贏過的站門檻減半 | 兩週日曆掃描 |
| C 路獎金：小王 `需求×8`、大王 `門檻需求×係數×40`，只進錢包 | 使用者拍 C |
| 編隊 20、前綴上限 2/6/12/20 **用出身算**；只有隊伍產錢；空隊伍＝自動編隊；**裝技能自動編入**；新夥伴自動入隊 | 使用者拍 (a) |
| 印記＝本輪王勝＋100/300 包＋滅世 +3（≤12/輪）、**總量上限 100**、輪迴條件＝本輪贏過王；**7 條神器線**第 r 級收 r 枚各有頂；招募券下架 | 使用者拍 D |
| 本輪當家 2 張 ×1.5／CD −20%，換桌布重抽 | — |
| 派遣 3 格 4 小時，回收粉塵 1（傳說／神話半顆）＋萬用 1，每日 9 次 | — |
| seal→充能、chaichai→重整；寶箱包 3%（+Lv150 隊員各 1%、寶箱祝福）；點擊附加商店兩項 | — |
| 大掃除 v2→v3：legacy 封存、印記＝12×輪迴次數、祝福買到頂；徽章「舊時代的珍母」＋魔花少女收藏卡 | 使用者 09-12 |
| 成長側 `PARTNER_G 1.02`（夥伴每級再乘 1.02^L）；v2 公式測試把它釘回 1 再量 | 兩週日曆掃描 |
| 路障小王**不鎖招募**（大王演出才鎖） | 抽卡是主爽感 |
| 魔花少女收藏卡：`Pool.CATALOG` kind `collect`、`s.collectibles`，只在大掃除發 | 使用者 09-13 |

## 三、UI 做到哪（都是簡單版）

| 畫面 | 有 | 沒有 |
|---|---|---|
| 舞台 | 路障（用該站大王素材縮小，sprite 逐幀）、挑戰鍵（發光≥90%）、「30 秒火力 ≈ n%」框、獎金橫幅、金色寶箱包＋小標、**夥伴列只列隊伍＋本輪當家旗**、**夥伴列拖到技能槽（`clicker-drag.js`，照 team20 骨架）** | 大王 K 的說明 |
| 換桌布面板 | 本輪帳（王勝／包數）、七條神器線（第一順位）、下一輪更快的說明、路障自動開打開關（電動手指頁） | 神器圖示（都用同一張章） |
| 大掃除頁 | 保留／整理／補償／怎麼算、收下才關、匯入也會看到一次；**魔花少女收藏卡真的發**（`card-mohuashaonv.png` 由禮物卡圖層合成、special 粉框、卡冊最後一頁） | — |
| 卡冊 | 隊伍摘要與上限、隊／派章、本輪當家旗、編入／移出、派遣／收回、收藏卡格 | 卡冊內拖曳（主畫面已有） |

## 三之二、2026-09-13 下午追加

| 項目 | 做法 |
|---|---|
| 大掃除二選一 | 「保留進度」＝重算印記、無補償；「從零開始」（按兩次）＝新存檔、`legacy.reset=true`、魔花少女＋徽章「舊時代的珍母」。收藏卡只屬於末世 |
| 1.0 編隊畫面 | `clicker-team.js`／底部「編隊」：上限列、20 格、候選、點選編入／移出／裝槽、自動編隊；**塞不進去講原因**（哪一層滿、要移出誰） |
| 末世入口殼 | `clicker-apoc.js`／底部「末世地圖」：打贏滅世珍獸 → `apoc.unlocked`、演出後自動開；四分頁 地圖／編隊（`apoc/map20.html` iframe，`team20.setScreen`）／精裝招募（`apoc/gacha.html`）／收藏卡（`apoc/mohuashaonv.html`，只在擁有時載入）；六步新手引導（遮罩＋對話泡）記在 `apoc.tutorial` |
| `src/apoc/` | 三個自包含頁從 holo-5.0 `_art/holo-test/` 複製（map20 11.6MB、deluxe-gacha-b-standalone 16MB、gift 10MB）；`export-web.py` 加 `apoc/**`。⚠ map20 的 `terrainLayout` 補了 0 寬保護；iframe 切換用 visibility 不用 display:none |
| 2.0 經濟 | **還沒接**：末世頁面是展示殼，抽到的卡不進隊、地圖不能推進。接線照 `DESIGN-2026-09-12-content-architecture.md` §八 |

## 三之三、2026-09-13 晚追加（使用者看兩張截圖後的五點）

| 項目 | 做法 |
|---|---|
| 末世＝整框模式，不是懸浮視窗 | `body[data-mode="apoc"]`：`#apoc` 蓋滿 960×640 含頂欄底欄；`src/apoc/index.html` 由 `tools/apoc/build_apoc.py` 從 holo-5.0 的 map20 模板＋`tools/apoc/extra.html` 注入自己的頂欄（末世金幣、末世券、返回 1.0）與底欄（地圖／編隊／精裝招募／卡冊・收藏卡）。postMessage：父→框 `apoc-state`／`apoc-go`，框→父 `apoc-ready`／`apoc-back`／`apoc-tab` |
| 收藏卡位置 | 末世「卡冊・收藏卡」分頁＝team20 編隊收藏畫面最下面加一列 `.team-collect`；點開用原本 three.js 禮物卡頁（`apoc/mohuashaonv.html`） |
| 從零開始 → 獎勵視窗 | `rewardPage()`：卡＋徽章、收下才關 |
| 進度快照 | `legacy.snapshot`（collection／dust／promotions／transcend／partnerLevels／badges／生涯幣／包數／輪迴）；徽章牆「舊時代的相簿」`mementoPage()` 回味 |
| 1.0 編隊＝2.0 同一套邏輯 | `clicker-team.js` 重寫照 team20：累加上限列（格子、受影響／違規）、隊伍網格 10 張一頁、獨立技能格、詳情面板（新增／替換目前成員／移除）、挑選器 `<dialog id="t20-picker">`；**塞不進去逐層講原因**「神話合計已達 2，加入後將為 3（上限 2，用出身算）」。`clicker-drag.js` 通用化 `bind()`，舞台夥伴列與編隊網格共用 |
| 驗收 | `clicker-v3-teamui.py`（含編隊畫面拖曳）、`clicker-v3-apoc.py`；round14/16/17/19/20 五支舊套件在 HEAD 之前就壞（角色數 21／25 等舊基準），不是這輪造成 |
| 生圖 | Codex 額度恢復後派 imagegen：`_art/v3-gen/BRIEF.md`（7 神器圖示＋寶箱包），成品進 `_art/v3-gen/`，尚未接進 `src/` |

## 三之四、2026-09-13 夜（使用者：今天要做到「點頭就上線」）

| 項目 | 做法 |
|---|---|
| 末世整框太擠 | `#apoc iframe` 以 1280 寬畫、`--apoc-scale`＝容器寬/1280（桌機 960→0.75）；<800px 直式手機不縮，交給 map20 自己的手機版面。隊伍網格桌機改 5×148（`tools/apoc/extra.html`，`@media (min-width:800px)`） |
| 精裝招募銜接太慢 | holo-5.0 `ceremony.js`：READ/RETURN 縮到一般卡 ≈660ms 一張（實測 common 0.66–0.72s，同 1.0 流星投遞 580+80）；傳說蓄力 900→700、神話 1300→900；`ceremony-layout.js` 十連卡片下限 190→150、邊界 140→210，「收下」不再壓卡 |
| 5.0 九張卡 | Astra 做完（holo-5.0 `711824b`，`REPORT-5.0-cards.md`）；**七張名字與稀有度是提案待使用者定**；星願龍主體偏小待調；觀星動態版待辦。已收進 `apoc/gacha.html`（72 張） |
| 素材 | Codex 生的 7 神器圖示＋寶箱包已接進 `src/`（`af26fe0`） |
| 上線前狀態 | balance-v3 對 main 是 fast-forward（main 仍在 `276e6fc`）；`npm test` 182、v3 七支全綠；dist-web 101 MiB（含 apoc 三頁）。**舊套件 round14/16/17/19/20 與 `clicker-browser.py` round7 是舊數值基準，v3 改了規則後必然紅，不是回歸**——上線後要重寫基準 |
| 已知未做 | 末世經濟未接（金幣 0、抽到的卡不進隊、地圖不能推進）；上線的是「1.0 v3＋末世展示殼」 |

## 三之五、2026-09-13 深夜：末世關卡串接第一版（能玩了）

| 項目 | 做法 |
|---|---|
| 經濟 | `src/clicker-apoc-economy.js` 純邏輯（`ApocEconomy`）：`s.apoc = {coins, tickets, progress 0–20, cooldownUntil, collection{id:n}, roster[], skills[4], stage, gifted}`。開門禮普發玥玥＋10 券；戰力 100/85/72/60 ×(1+0.25×(張數−1))；血量 3000×1.22^i（王 ×6）；通關獎勵＝血量×0.1；放置金幣＝戰力×0.01／秒；點一下＝戰力×0.5；券 1000；王關 60 秒／失敗冷卻 3 分鐘 |
| 橋 | `clicker-apoc.js`：每秒 `tick()`（1.0 的 tickTimer 叫）；frame→parent `apoc-fight／apoc-tap／apoc-buy-ticket／apoc-drawn／apoc-team`；parent→frame `apoc-state`（view）＋`apoc-win／apoc-fail` |
| 組頁補丁 | `tools/apoc/build_apoc.py`：全卡池 71 張進 team20（index.html 22.6 MB）、產 `src/apoc/pool.js`、team20 加 `setOwned／setRoster／setSkills` 與 `window.onTeamChange`、map20 加 `setCooldown／setProgress／selected`、招募頁加 `apocGacha.setTickets` 與抽到回報 `window.top` |
| HUD | `tools/apoc/extra.html`：地圖右欄戰鬥區（血條／開戰／攻擊！／王關倒數／冷卻）、頂欄「換券 1000」、收藏計數 |
| 數字 | **全部是第一版猜的**（模擬器還沒跑）：普發玥玥一張 60 戰力打第 1 站 3000 血約 50 秒放置；十連後約 700 戰力。第 20 站 780k 血。要調就改 `ApocEconomy.RULES` |
| 沒做 | 王關機制（護盾／召喚／節奏）——目前王只是血多＋限時；獨立技能格沒有效果（只記錄）；末世星數不顯示在卡面；觀星動態卡 |
| 驗收 | `node --test`（189）、`clicker-v3-apoc-play.py`（端到端） |

## 四、模擬器

`node tools/sim/clicker-v3.js 4 20 14`；環境變數覆寫 `B.V3` 任何鍵、`BOSS_K=10,8,6,5,8,8,2`、`CITY_MUL`、`NO_PRESTIGE=1`。
真人模型：一天三段 20 分鐘＋離線；只有卡住才換桌布；派遣下線前派 3 張。
現況形狀：第 1 天前四站、夜市第 1 天晚上、冰箱第 2～3 天、滅世第 10 天、6 次輪迴、14 天 5★ 35/52。
⚠ 兩個儀器教訓：模擬器換場景清單漏了 `city` 曾讓「滅世永遠過不了」；`PARTNER_G` 一個小數點＝一天與一週。

## 五、接下來

1. 素材：神器圖示、寶箱包（**生圖是唯一不由 Opus 做的**）；小王與魔花少女已用現成素材。
2. 大王 K 遞減與滅世係數要不要再調，等使用者實玩。
3. 併回 main 前**單獨問**；併之前把 `docs/clicker` 兩邊同步。

## 六、驗收

```
npm test                                            # 182
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-gate.py       # 路障、挑戰鍵、火力預估、獎金（含 60 秒真實等待）
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-prestige.py   # 換桌布面板、神器
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-cleanup.py    # 大掃除頁（v2 存檔）
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-team.py       # 卡冊編隊、派遣
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-drag.py       # 夥伴列拖到技能槽
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-teamui.py     # 編隊畫面、塞不進去的原因
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-apoc.py       # 末世入口、新手引導、四分頁
＋ NEXT-SESSION-KICKOFF.md 的十一支舊套件
```

## 七、本機試玩（不是網頁版，gh-pages 沒動）

```
cd D:\claude研究\clawd-pet-v3
python tools/export-web.py            # → dist-web/（360 檔）
python -m http.server 8123 --bind 127.0.0.1 --directory dist-web
```
開 http://127.0.0.1:8123/ 。localhost 是獨立存檔（跟 speshotdog.github.io 不通），要試大掃除就在網頁版徽章牆「匯出存檔」、在本機「匯入存檔」。
收藏卡：卡冊頂端「收藏卡」鍵（第一版只放卡與名字；正式分頁另做）。
