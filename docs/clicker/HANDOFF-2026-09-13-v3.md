# 交接：1.0 數值 v3（balance-v3 分支，2026-09-13）

**接手先讀這份。** 規格 `DESIGN-balance-v3.md`（含 §十三～十六 追加定案）、試做報告 `REPORT-2026-09-12-balance-v3-prototype.md`（含 §二之二、二之三 的模擬數字）、末世架構 `DESIGN-2026-09-12-content-architecture.md`（在 holo-5.0）。

## 〇、2026-09-13 深夜收工狀態（下次開工先看這裡）

> ⚠ **這一段已經是舊的。最新狀態在 §〇之二十一（2026-09-14）**，
> §〇 到 §〇之二十 由新到舊往下讀；下面這幾行只有「上線流程」與「試玩指令」還準。
> 2026-09-14 的重點：末世養成軸（兩週養滿）已完成並掃過數字、手機版面一輪修完、
> 收藏卡換成最新版並修好空字型、2.0 的入口改成一直看得到。
> ⚠ 使用者在 **GitHub Pages** 上測會看到 09-08 的舊版，回饋前先確認網址是 Cloudflare 試玩版。
> ⚠ `build_round4_fonts.py` 那支**是壞的**（少 populate()，會產空字型），
> 已由 `tools/apoc/build_fonts.py` 取代，不要再用它。


- **沒有上線**（使用者：先不上）。`balance-v3` 與 `holo-5.0` 都已推到 origin；main 仍在 `276e6fc`、gh-pages 沒動、exe 沒出。
- balance-v3 對 main 是 fast-forward；上線流程：併 main → `python tools/export-web.py` → 照 `HANDOFF-next-session.md` 二節推 gh-pages → `npm run build` 出 exe。**每一步都要單獨問**。
- 試玩：`python -m http.server 8123 -d dist-web`（dist-web 已是最新 export）。
- 做到哪：三之一～三之五。最新一版＝末世關卡能打（三之五）。
- 下一步候選（使用者還沒排）：① 實玩調 `ApocEconomy.RULES` 數字；② 王關機制（護盾／召喚／節奏）；③ 獨立技能格效果；④ 舊測試基準重寫（round14/16/17/19/20、clicker-browser round7）；⑤ 觀星動態卡；⑥ 黑角回報。
- 5.0 卡的教訓與工法：`REPORT-5.0-cards.md` 末三節（不摳圖、臉置中 40%、字型子集要重跑 `build_round4_fonts.py`）。

### 〇之二、接續 09-13 收工後的這一輪（仍未上線）

- **修掉招募的死畫面**（使用者回報：卡片在、按鈕全沒了）。真因在 `clicker-save.js` 的「王包」驗證：
  v3 定案「路障小王不鎖招募」，但驗證仍禁止 `boss` 與 `pending` 共存。招募層蓋著時背景照樣拆包，
  拆到第 10 包自動開打路障 → 下一次 `purchaseDraw` 的 pending 永遠存不進去 → `commit` 失敗、消費鎖住，
  而 `collect(stay)` 已先把兩顆收下鍵藏掉。三處修正：驗證放行小王（大王照舊互斥）、
  `start()` 回傳有沒有真的開演（起不來就 `close()` 收尾）、`open()` 只擋大王（重開才收得下 pending）。
  回歸測試在 `clicker-v3.test.js` 最後一則；重現腳本跑 30 輪連抽不再出現。
- **底欄／頂欄順序**（使用者指定）：統計、場景移到頂欄錢幣盤左右各一（直式收成純圖示）；
  底欄改成 換桌布 → 編隊 → 名冊 → 商店。補圖示：場景＝`clicker-ui-scene.png`（Codex 生的票券）、
  換桌布＝桌墊、編隊＝隊旗、末世地圖＝滅世珍獸。標題膠帶 250→224 讓位給「統計」。
- `npm test` 190；v3 七支＋round32/33 全綠。

### 〇之三、同一輪的後半：交接第五節清單全推（仍未上線）

使用者：「全都做，需要決策的問我」。決策只問了兩題：末世節奏（選 3～5 天）、
王關機制（選護盾＋節奏）、技能格（選沿用 1.0 語意）。其餘自己做完。

| 項目 | 結果 |
|---|---|
| 末世數值 | `tools/sim/apoc.js`／`apoc-sweep.js` 掃出定案：第 1 站 4 分 → 第 20 站 33 分、累計 202 分鐘、第 4 天全線、抽 210 次、王關失敗 0 次 |
| 王關機制 | 護盾（放置只剩 35%）＋節奏（點滿 15 下破防 8 秒、全傷害 ×2、門檻 ×1.25）；**時限拿掉**（模擬器量到限時會讓一輪失敗 800 多次） |
| 技能格 | 由卡的稀有度決定：神話 10 下 ×10／傳說 15 下 ×3／史詩 全隊 ×1.5 二十秒／精良 其他格 −8 秒 |
| 末世星數 | team20 卡面右上 ★張數（`setStars()`，build_apoc.py 打補丁） |
| 舊測試基準 | round14/16/17/19/20 ＋ `clicker-browser.py` 全部改成對不變式或對現行定案，**全綠** |
| 黑角 | 補掃演出途中（`tools/test/clicker-v3-black-corner.py`）：結果頁有鑑別力且乾淨；演出途中負控制咬不動，如實列為「綠燈不算數」。見 `VERIFY-black-corner.md` 第五節 |
| 觀星動態卡 | 素材與比對頁做好（holo-5.0 `5f29f17`），**沒接進卡池**——`card_assets.py` 會把動畫壓成一格，要接得動凍結檔案 |
| 圖示 | 場景＝票券、換桌布＝翻角桌巾（都是 Codex imagegen 生的） |

**我原本回報的「`#draw-five` 在小王演出中看起來可按、按了沒反應」是錯的**：
`numbers()` 每秒會重跑 `gacha.render()`，那顆鍵在 `bossBusy` 期間確實是 disabled，只是最多晚 1 秒。

驗收：`npm test` 194；v3 七支＋apoc 兩支＋round14/16/17/19/20＋clicker-browser＋黑角 全綠。

### 〇之四、末世改用 1.0 介面（使用者：「整體 UI 邏輯非常差勁」）

使用者回報：末世按鈕裁切、技能格沒縮圖、沒有卡冊、地圖沒有可玩畫面；指示是
「1.0 的版本比較好，乾脆把 1.0 的 UI 介面稍微調色拿來用就好，但卡片要是末世卡版本」、
「末世地圖的選項放在場景裡面⋯⋯兩個主系統，選哪個就算重新整理了」、
「兩邊邏輯不要差太多，不同的是 UI 的色調」。

**根因是同一個：末世是把原型頁縮進 22 MB 的 iframe。所以 iframe 整個拿掉。**

| 項目 | 現在長怎樣 |
|---|---|
| 架構 | 末世直接畫進 1.0 的節點（`clicker-apoc-ui.js`），`body[data-world="apoc"]` 換色票 |
| 卡面 | 71 張輸出成 `src/apoc/cards/*.webp`（0.77 MB），欄位形狀同 `GachaPool.CATALOG` |
| 切換 | 場景面板：七站＋末世並排，存進 `settings.world` |
| 招募 | 兩個世界共用同一層與五種演出，`clicker-gacha.js` 的 `W()` 轉接器只換卡池／貨幣／落帳 |
| 卡冊・編隊・拖曳 | 同樣各加一層世界轉接器 |
| 升階／超越 | 改成收下時自動做（`E.autoGrow`），詳情頁不再放那兩顆鍵 |
| 抽卡結算 | 新的 `#draw-summary`：新夥伴／升星／自動升階／自動超越／粉塵 |
| 刪掉 | `clicker-apoc.js`、`src/apoc/index.html`（22 MB）、`gacha.html`（19 MB）、`build_apoc.py`、`extra.html`；dist-web 104 → 64 MB |

**UI 體檢器 `tools/test/clicker-ui-audit.py`**：字被裁／出界／被蓋住／按鈕互疊／按鈕在畫面外，
46 個畫面 × 桌機手機。第一版報 1504 條（大多是假陽性），扣掉之後 86 條真的，逐條修完 0 條。
順便抓到三個本來就有的手機版臭蟲（場景票券寫死兩欄 300px、徽章牆九欄、分享鍵在畫面外）。

**Codex gpt-6-astra 複檢三輪**（`VERDICT-apoc-ui{,-2,-3}.md`）：第一輪八條、第二輪十四條，
全部處理完。兩條用負控制證實過：拿掉快取作廢，體檢器會報「切回桌邊留著 10 張末世卡」；
拿掉 pending 恢復，會報「末世重開後的待收下：找不到 #collect」。

驗收：`npm test` 199；UI 體檢 46 畫面 0 條；v3 六支＋末世兩支＋round14/16/17/19/20＋
`clicker-browser` 全綠。**仍未上線**（main 與 gh-pages 都沒動）。

### 〇之五、末世改回精裝：卡面、模式、關卡地圖、精裝典藏包（仍未上線）

使用者退回〇之四：「末世的卡怎麼還有舊的圖繪」「新的抽卡 UI 被你毀了」「**沒有的 UI 才用舊的改色**，
不然之前做這麼辛苦的東西不就白費了」「**模式是額外的大選項，切換後就是之前做的關卡地圖**」。
問答定案：戰鬥畫面＝1.0；卡池只要精裝（71 張全是）；卡面＝HoloCardFace；抽卡演出＝精裝典藏包。

- **根因**：〇之四的 `export_cards.py` 把去背主體壓在黑底、沒有卡框 → 精裝卡看起來像舊圖。已刪掉那支與 71 張輸出。
- **精裝卡面**：`tools/apoc/build_holo.py` 從 holo-5.0 拆出卡面 CSS／圖層／遮罩／字型成實體檔（`src/apoc/`）；
  `src/clicker-holo.js` 用 shadow root 包 HoloCardFace。夥伴列、技能格、卡冊、編隊、結算卡、拖曳殘影都換了。
  ⚠ 遮罩網址要絕對（自訂屬性裡的相對網址以 `apoc/holo.css` 為基準）。
- **模式**：頁尾第一顆 `#mode-open` → 兩張卡（珍母點點／末世）。場景面板不再有末世；末世裡場景鍵叫「地圖」。
- **關卡地圖**：`tools/apoc/build_map.py`（素材）＋`src/clicker-apoc-map.js`（照 map20 原型：20 站、五區段、插畫地形）。
  末世一進來就是地圖，「進入戰鬥」才是 1.0 戰鬥畫面。直式另有一套版面。
- **精裝典藏包**：`tools/apoc/build_ceremony.py` → `src/apoc/ceremony.html`（81 KB，演出本體不改）。
  扣券與結果先在主頁寫進 `apoc.pending`，再交給 iframe 演出；收下時 postMessage 回主頁落帳＋結算卡；重新整理會回到結果頁。
- **UI 體檢器**：兩條判準修正（`overflow:visible` 的字只看是否超出外層裁切容器；捲出捲動容器的鍵不檢查被蓋住），
  有反向對照。56 畫面 0 條。
- 驗收：`npm test` 200、`clicker-v3-apoc.py`／`clicker-v3-apoc-play.py` 全綠。
  `clicker-browser.py` 第 944 行 `#hero > svg` 偶發失敗，A/B 實測 HEAD 也是 3 次 1 次（點擊區啟用當下主角還沒掛上），不是這輪造成。
- 複檢：`REVIEW-BRIEF-apoc-deluxe.md` → `VERDICT-apoc-deluxe.md`。
- **Codex 複檢四條必修已修，逐條瀏覽器實證**（`codex_fix_probe` 的做法：真的讓 `localStorage.setItem` 丟例外、真的轉直式）：
  - B1 收下時存檔失敗 → 典藏包已清場卻落帳不成。改成把同一批結果放回結果頁；存檔鎖住時收下會先自己重試儲存（主頁的重試鍵被典藏包蓋住按不到）。
  - B2 桌邊選了只支援多連的演出，末世單抽被鎖 → 末世的單抽支援不看桌邊演出。
  - B3 地圖開著轉直式，進戰鬥舞台縮成 0 → 關地圖時重跑 `fitStage()`。
  - B4 模式面板按 Esc 掉進 `closeWindow()` → `closeTopPanel()` 補上 `modes`。
  - 值得修也一起做了：精裝卡面離開 DOM 5 秒後 `unobserve`；體檢器的裁切容器一路往外查；地形貼圖改順排（原本第二張起全落在容器外）。
- 舊測試：round21／round31 在 HEAD 就已失敗（A/B 實測），是測試跟不上 09-13 的設計（抽卡結算卡、頁尾顆數），已改測試。
  black-corner 原本開的是已刪的 `apoc/gacha.html`，改成驅動 `apoc/ceremony.html`。

### 〇之六、末世第二輪回饋：舊化 1.0 配色、技能施放、卡片旋轉、編隊版面（仍未上線）

使用者看完 `38660a7`：字難讀（深色底深色字）、卡冊星星要圖示、詳情卡要能用滑鼠轉＋反光、
主畫面「什麼都看不到」、技能要有 1.0 的施放演出、技能槽不要兩行、編隊資訊要壓縮／成員直接放精裝卡／詳情卡不要外框、
配色「說不定可以變成舊化材質的 1.0」。

- **主畫面夥伴列是桌邊的**（根因）：`clicker.js` 初始化不分世界呼叫 `stage.setPartners()`，從末世重新整理就被蓋掉，
  末世的快取又以為沒變。修：初始化分世界＋`#buddies`／`#slots` 標 `data-world="apoc"` 所有權，被蓋就重畫。
- **配色**：做了深藍／舊化／焦黑三張樣張，使用者選**舊化 1.0**。`tools/apoc/build_theme.py` 把 1.0 貼圖做舊
  → `src/apoc/theme/aged/`；`src/apoc/theme.css`（`body[data-apoc-theme="aged"]`，進末世掛上）。
  外層面板 9-slice、內層分區不貼紙（貼圖撐滿會變雙層框＋框角壓字）；戰鬥舞台墊廢墟後院插畫。
  WCAG 對比掃描修到只剩刻意半透明的停用翻頁鍵。
- **技能施放**：`ClickerCutin.play()` 接 `effect.spec／entry／actor`，末世同一套分鏡、主角是精裝卡。
- **卡冊**：星星圖示；詳情卡 `ClickerHolo.interactive()`（照典藏包手感：反光跟游標、拖曳轉、放開回正）。
- **編隊**：技能槽一排四格（直式只放卡）；四條上限一排四格；末世成員＝裸精裝卡；詳情卡無外框。
  ⚠ 規則要帶 `#game`／`#roster`，1.0 的 `#game button` 權重會把奶油框畫回來。
- 驗收：`npm test` 200、UI 體檢 56 畫面 0 條、`clicker-v3-apoc`／`apoc-play` 全綠。複檢：`REVIEW-BRIEF-apoc-aged.md` → `VERDICT-apoc-aged.md`。
- **Codex 第二輪複檢**（`VERDICT-apoc-aged.md`）兩條必修已修並實證：抽卡結算／全線通行在紙底上仍是深藍版淺字（1.74:1 → 7.52:1）、
  卡片放開後游標一動回正就停在半途（回正中且非拖曳時不打斷）。值得修的也做了：直式技能格補 title、桌邊重畫時清掉 `data-world` 標記。
- **round26 偶發失敗查到底**（HEAD 一樣會壞，不是這輪造成）：測試直接 `skillSlots[0]=src`，輪到 `qinghua` 時同一張卡在兩格，
  剛好碰上每 5 秒的自動存檔就「存檔驗證失敗：重複槽位」→ 消費鎖住 → 招募鍵永遠灰。遊戲的鎖住行為是對的，改測試（已在槽就不塞）。
  同一段還有第二個搶快：`mythicSkill` 讀舞台快取的 `latestState.skillSlots`、特效掛在已畫好的技能鈕上，改完槽要等每秒的 `changed()` 畫好（1.3 秒）再發動。修完連跑兩次全過。
  追法：替 `#recruit-five.disabled` 掛 setter 抓呼叫堆疊＋同時看 `#mode-select`／`#collect` 的停用狀態反推 `store.blocked`。
- round32 單獨跑會過，整批跑時跟別的瀏覽器測試搶 CPU 偶發逾時。

### 〇之七、末世第三輪回饋：技能貼紙、怪置中＋點擊回饋、直接花錢抽卡、商店／戰績、收藏卡精裝（仍未上線）

使用者看完 `6cc7a53` 的六條回饋與後續拍板（原話重點）：
技能格「不要把整張卡放上去」；怪要置中、「點下去什麼都沒有」「怪甚至跑出螢幕外」；
「換金卷再去抽有點脫褲子放屁」→ 追問兩次才定案：**「我幹嘛拿錢換卷再抽卡，為什麼不直接花錢抽卡」**、
**「末世幣就是只能用在 2.0；1.0 的幣可以讓玩家用很差的比值去交換 2.0 抽卡卷，讓玩家覺得 1.0 練的卡還有一點用處」**、
「隊伍訓練本來就要有這功能」「打完王現在會給很多錢，不用特別送抽卡卷」；按鈕配置選樣張 A；統計／商店做、輪迴先放鍵；
收藏卡卡冊「做去哪了」。**中途嚴正抗議：「2.0 不准出現任何 1.0 卡面，魔花少女是之前花最多心力作的卡」**。

| 項目 | 做法 |
|---|---|
| 技能格 | 圓形貼紙頭像：外圈稀有度色（神話彩虹）、裡面是精裝卡主體圖；取景 `tools/apoc/build_sticker.py` → `src/apoc/sticker.js`（alpha 外框取景）；冷卻用 1.0 同一個圓形遮罩。夥伴列維持精裝卡（使用者只點名技能格） |
| 怪置中 | **根因**：`#apoc-enemy` 用 `transform:translate(-50%,-50%)` 置中，點擊的 WAAPI `transform:scale()` 整個蓋掉 → 一點就往右下跳半身、出舞台。改成獨立的 `translate` 屬性，中心 (304,150)；`#tap` 跟著移。怪鳥／灰狼是 4 格動畫表，整張放會變「一排三四隻」，`object-view-box` 只取第一格 |
| 點擊回饋 | `clicker-apoc-ui.js` 自己畫（1.0 stage 在末世是 stop 的，不能叫它的 float/burst）：傷害浮字、碎片＋火花、閃白壓扁；爆擊／破盾／擊倒加衝擊圖與震動；放置打死也有擊倒演出。浮字＝`A.tapDamage()`（跟實際扣血一致）。座標先換螢幕座標再換進 `#floaters`／`#click-fx`（直式兩層盒子不同） |
| 錢 | **末世金幣只在 2.0 用**：直接抽卡（1000 × 1.02^付費抽數）、點擊力（+5%／級，20000 × 2^L）、全隊訓練（+3%／級，40000 × 2^L）。**券只從桌邊金幣換（時薪券）**：一張＝桌邊每秒收益 ×600 秒，當天每多換一張 ×1.25、隔天重置；抽卡先用券。沒有打站送券。舊檔 `ticketsBought` 搬成 `paidDraws`（價格不倒退），手上的券保留 |
| 換券定價的漏洞 | 只用當下每秒收益會被鑽：把桌邊隊伍換成一張弱卡、每秒收益掉下去 → 券變超便宜。基準改成 `max(當下, 10^peakRateStamp)`（1.0「每秒破 10^n」紀錄只會往上）。`clicker-v3-apoc.py` 有這條 |
| 商店三張卡（樣張 A） | 點擊力｜全隊訓練｜招募，招募下一行「用桌邊金幣換券」→ 末世商店。⚠ 這行字要短：直式跟價牌共用一欄，長字把欄撐寬、十連鍵被擠出畫面（UI 體檢抓到） |
| 頂列／頁尾 | 兩個世界同位置：統計→**末世戰績**（借 `#stats`：10 格數字＋共用的匯出／匯入存檔——以前末世連匯出存檔都點不到）；商店→**末世商店**（借 `#wardrobe`：兌換所＋受擊特效外觀 4 種，末世金幣買）；換桌布→**重走廢土**（停用，全線通行後開放，內容未設計） |
| 收藏卡卡冊 | 以前 `#collect-open` 兩個世界都沒打開過（桌邊只排在卡冊最後一頁，末世卡冊只列末世卡池）。現在兩邊有收藏卡就顯示；**末世裡魔花少女嵌精裝頁** `apoc/mohuashaonv.html?embed=1`（`data-embed` 藏標題／提示、透明底；詳情可傾斜、點一下做動作） |
| 2.0 不准出現 1.0 卡面 | 卡冊、編隊（縮圖＋詳情）、抽卡結算、拖曳殘影、技能格、夥伴列的 fallback 全部改成空白底 `.apoc-face-blank`；`clicker-v3-apoc.py` 掃「2.0 畫面沒有 `.character-png`／非精裝 `.card`」，並**進 iframe 裡**確認嵌入模式真的生效 |

模擬（`node tools/sim/apoc.js`，一天三段 20 分）：改版前 202 分・抽 210；新經濟不訓練 218 分；**定案 201 分・第 4 天・抽 200**；
＋換券（1.0 剛解鎖 P1e10／存 1e14）186 分・換到 45 張；（後期 P1e12／存 1e17）181 分・48 張——沒有一次換爆。
`tools/sim/apoc.js` 可用 `ONE_P`／`ONE_COINS`／`TEAM_*`／`CLICK_*`／`TRAIN_SHARE` 覆寫。

**這一輪踩到的坑（下一個人別再踩）**
- **DOM 檢查全綠 ≠ 看起來對**。收藏卡第一版用了 1.0 平面卡、測試只數元素就綠了；嵌入模式壞掉（標題＋深紫底還在）時測試只看 iframe src 也是綠的。視覺改動一定要自己打開截圖看。
- **從 Python 往 JS 塞字串別用反斜線跳脫**：`'\\b'` 在一般字串被寫成退格字元 `\x08`，正則 `/[?&]embed\b/` 永遠不成立。
- **Windows 上 Python `write_text` 會把 `\n` 寫成 `\r\n`**：10 MB 的精裝頁整份換行被改。用 `write_bytes` 或 `newline=''`。
- bash heredoc 塞大段 JS 會被引號搞壞（unexpected EOF）；大段程式先寫成檔案再用 Python 拼接。

驗收（含〇之七之二的修正之後）：`npm test` 203；UI 體檢 56 畫面 0 條（第一次 4 條全是直式十連鍵被換券入口擠出畫面，已修）；
`clicker-v3-apoc`／`apoc-play`／gate／prestige／cleanup／team／teamui／drag／black-corner、round14／16／17／19／20、`clicker-browser` 全過。
複檢：`REVIEW-BRIEF-apoc-round3.md` → `VERDICT-apoc-round3.md`。

#### 〇之七之二、Codex 第三輪複檢（`VERDICT-apoc-round3.md`）與處理

| 等級 | 問題 | 處理 |
|---|---|---|
| 必修 | 換弱隊仍可把券價壓到九分之一：`peakRateStamp` 只存指數（9e10 記成 10） | 另存精確值 `apoc.onePeak`：桌邊每秒 tick 只往上記（在 apoc 底下，換桌布不歸零）；定價＝`max(當下, onePeak, 10^peakRateStamp)` |
| 必修 | 桌邊商店留下的 `shopCategory` 讓末世商店按 Esc 重畫成桌邊商店 | `album.escape()` 在末世不走分類返回，直接關 |
| 值得修 | 點怪後立刻招募，末世粒子畫到招募層的畫布；招募中放置擊倒又把畫布切回舞台 | `pauseStage()` 叫 `apocUI.stopFx()`；招募層開著時不噴粒子、不演擊倒 |
| 值得修 | 末世商店以每秒增加的餘額當重建條件，Tab 焦點每秒被刪 | 快取鍵改成「買不買得起」 |
| 值得修 | 日期往返（前進一天再調回）可重置當天加價 | 日界只往前走：`exchangeToday` 對「今天 ≤ 紀錄日」沿用張數，`exchange.day` 取 max |
| 不用修 | 舊券／付費進度／pending 收下、換券兩邊同一次 commit、貼紙資料、收藏卡 iframe 不會每秒重載 | Codex 實測過，維持 |

⚠ 我自己的測試踩的坑：驗證換券定價時把種子的桌邊金幣拉到 `coins=1e20`，但 `lifetimeCoins` 還是 1e13——
`clicker-save.js` 要求「累計收入 ≥ 手上金幣」→ 驗證失敗、消費鎖住，後面「換券」「切回桌邊」「色票拿掉」一連五六條一起紅。
（第一次我以為是改了隊伍，拿掉之後照樣紅；用 `ClickerSave.validate` 逐項試才抓到真兇。）
修法：lifetimeCoins 一起拉，並加「存檔沒有鎖住」的檢查，種子改壞時會先紅這一條、不會連坐。

### 〇之八、末世第四輪回饋：自動接關、點擊撐滿舞台、結果卡徽章、1.0 加成套進 2.0、放大鏡、1.0 統計新版（仍未上線）

使用者看完 `5583ae4`（原話重點）：「打死上一隻怪物之後下一隻出現，然後就無法點了」「點擊範圍不要只有怪物本身，空白地方也要能造成傷害（除了王的特殊機制）」
「編隊名字非常不好閱讀，下面有黑色陰影，加上他是黑字」「抽卡結算直接提示在抽卡結果，不是用一個額外視窗」
「末世地圖的預設畫面是戰鬥畫面，不是選擇關卡」「印記商店加成 2.0 吃得到嗎？2.0 也要有印記商店」
「2.0 技能雖然重新設計，但名稱不要改，1.0 有的名稱就直接沿用」「卡面下方要有放大鏡，放大到畫面正中間、背景變暗，好好把弄觀賞」
「1.0 的統計也改成新版，比較好閱讀」；中途追加王關：「這不是動圖」「字體幾乎看不到，顏色問題」「第一次遭遇應該直接進入關卡，失敗之後才會在右上方有進入選項」。
**「跟我說話請用繁體中文」。**

問答定案兩題：**2.0 印記＝1.0 的印記加成直接套進 2.0**（不另做 2.0 商店）；**結算提示兩個世界都改**（拿掉「收下了！」）。

| 項目 | 做法 |
|---|---|
| 打死之後點不動 | 根因：打死後 `stage=null`，要再按「開戰」，點擊區直接 return。改 `autoFight()`：戰鬥畫面上自動接下一站（地圖／招募層／結局開著、隊伍空、這一站的王輸過都不自動）；`tap()` 沒有戰鬥時先自動開打再算傷害 |
| 點擊範圍 | `#tap` 撐滿舞台 608×360；受擊特效固定打在怪身上；王的護盾照舊每下都算破盾 |
| 王關 | 怪鳥／灰狼 4 格動畫表用 `object-view-box` 逐格播（照 1.0：每格 130ms、`steps(4, jump-none)`）；舊化配色的護盾／破防標籤深墨字；`bossFailed`：這一站的王輸過才停止自動開打、開戰鍵寫「再次挑戰」。⚠ `RULES.BOSS_TIME` 目前是 0，正常遊戲裡王不會輸——要不要加回時限是設計決定，還沒問 |
| 戰鬥畫面預設 | 三個進末世的入口 `apocMap.open()` → `close()`；地圖只從頂列「地圖」開 |
| 1.0 加成套進 2.0 | `clicker-apoc-ui.js` 的 `oneBoost(s)` 照 1.0 `rates()`：戰力 ×markMul×blessMul、點擊 ×(1+10%×攻擊力祝福)、技能效果 ×(1+5%×技能祝福)、冷卻 ×(1−2%×冷卻祝福)。每次從 1.0 存檔現算掛在 `a.boost`，提交前刪、`normalize` 也刪，不寫進存檔。戰績多一格「1.0 印記加成」 |
| 技能名 | `skillOf` 查 1.0 同名角色的 `characters[id].skill`（例：快樂海豹→「快樂拍拍」、滅世珍獸→「滅世光線」），效果仍照稀有度；1.0 沒有這張卡維持稀有度的名字 |
| 結果卡徽章 | `clicker-gacha.js` 的 `drawBadges()`：收下前用同一份 pending 試算 `W().collect()`，照抽到順序標「新夥伴／★a→★b／升階／超越」。1.0 掛在 `#cards` 子節點；末世傳給典藏包，`build_ceremony.py` 的橋接包一層 `completeSlot` 掛 `.apoc-badges`（產生器重跑，先確認過重跑前後產物一模一樣）。拿掉 `showDrawSummary`／`#draw-summary`；萬用粉塵溢出改寫在結果頁提示字 |
| 編隊卡名 | 精裝卡的深色光暈壓在名字上 → 名字墊紙色底、跟卡拉開 |
| 放大鏡 | 卡冊詳情卡下方「🔍放大」→ `#card-zoom`：末世精裝卡面（拖曳轉、反光跟游標）、收藏卡嵌精裝頁、1.0 卡**保持原尺寸整張 `scale(--zoom-k)`**（1.0 卡內部是固定像素排版，撐大外框只會變大空框）；直式以寬為準；Esc／關閉詳情先收放大層 |
| 1.0 統計 | 生涯數字從一整行「｜」改成數字格 `#stats-tiles`（跟末世戰績共用 `.stat-tile`）；直式一排 4 格、字縮小 |

**這一輪踩到的坑**
- **測試種子不合法 → 存檔鎖住 → 末世整個停止重畫**，看起來像「加成沒套上」。`blessing=5` 要先有 `marksClaimed≥15`（印記商店驗證）；`coins` 要 ≤ `lifetimeCoins`。追法：攔截 `#click-rate` 的 textContent 寫入發現 2.5 秒沒人寫 → `ClickerSave.validate` 直接印錯誤。測試裡都加了「存檔沒有鎖住」的檢查。
- 1.0 五連沒有徽章不一定是壞：種子每張 4 顆粉塵，再抽到 5 顆仍是 3★，沒新卡也沒升星本來就不標。測試改成只留 3 位夥伴。
- 從 Python 用字串比對打補丁，中文＋跳脫（`'\\n'`、`\b`）很容易對不到；大段內容先寫成檔案、用純英數定位點或行號替換。
- 詳情卡的進場動畫 `fill:forwards` 停在 `scale(1.15)`，蓋過直式的 `scale(1)`——直式其實一直是放大的，下方元素要留 44px。
- 怪鳥／灰狼的動畫表圖案本身貼齊格線，翅膀在格邊被切齊，是素材本身（1.0 同圖同樣），不是取景錯。

驗收：`npm test` 203（末世經濟 20）；UI 體檢 54 畫面 0 條（過程中抓到直式 1.0 統計被推出畫面 26 條、直式放大鍵被卡面蓋住 1 條，都修了）；
`clicker-v3-apoc`／`apoc-play` 全過；截圖都親眼看過。複檢：`REVIEW-BRIEF-apoc-round4.md` → `VERDICT-apoc-round4.md`。

#### 〇之八之二、Codex 第四輪複檢（`VERDICT-apoc-round4.md`）與處理

| 等級 | 問題 | 處理與實證 |
|---|---|---|
| 必修 | 放大卡一按就關：`#card-zoom` 掛在 `#game`、不在 `.panel` 裡，按住卡面被 `pointerdown` 判成「點面板外」→ `closeZoom()` | 面板外點擊的排除清單加 `#card-zoom`。`clicker-v3-apoc.py`：按住卡面拖曳後放大層還在 |
| 必修 | 典藏包還沒收下，背景照樣自動接下一站：`autoFight()` 只擋 `#recruit-layer`，典藏包是另一層 `#apoc-ceremony`，也沒擋 `apoc.pending` | 兩個都擋。`clicker-v3-apoc-play.py`：典藏包開著時清掉戰鬥，等兩個 tick 仍沒開打 |
| 值得修 | 1.0 連抽最後收下，`close()` 播累積入隊後 `collect()` 又播本包 → 同一隻飛兩次 | 本包先併進 `pendingJoins`，統一由 `close()` 去重播一次；連抽起不來也只叫 `close()` |
| 值得修 | 放大層 Tab 焦點跑回背後卡冊；Esc 關掉沒還焦點 | Tab 焦點循環優先 `#card-zoom`；開啟時記下來源鍵、關閉時還回去。測試有這兩條 |
| 值得修 | 1.0 放大卡轉向後 `--zoom-k` 沒重算 | `album.relayout()` 裡 `refitZoom()`：重量 1.0 卡比例、精裝卡面 refit |
| 值得修 | 我插 `badgeNode()` 時把 `summary()` 結尾的 `$('collect').focus(); render();` 擠到 `return box` 後面，變成死碼 | 移回 `summary()` 結尾 |
| 不用修 | 招募層沒掛 boost、徽章試算的副作用、王輸過旗標擋不擋手動重打、典藏包橋接 skip／restore／清場 | Codex 記憶體實測過，維持 |

驗收（修完之後）：`npm test` 203；`clicker-v3-apoc`／`apoc-play`（含上面四條新實證）全過；v3 gate／prestige／cleanup／team／teamui／drag／black-corner、round14／16／17／19／20 全過；UI 體檢 54 畫面 0 條。

### 〇之九、末世第五輪：王的動畫切邊、難度放寬、王關統一 60 秒、被動傷害數字（仍未上線）

使用者看完 `1076bb2`：「GIF 看得到切邊，還會有別的東西跑進來」（怪鳥左邊一截黑線、翅膀被切）、「幫我驗證難度問題，我覺得現在的難度可能偏難」。
問答：難度選「放寬」、王關選「加回時限」→ 追問秒數時回答**「統一 60，幫我把難度調整」**。
中途我加了「輸了保留部分傷害」，使用者：**「我不想要保留血量的機制，你直接調整血量就好」**→ 整個拿掉；又說「2.0 似乎沒有顯示被動傷害，能加回來嗎」。

| 項目 | 做法 |
|---|---|
| 王的動畫 | 查證：怪鳥／灰狼動畫表每格都是一整塊、都在自己那一格裡，**不是素材問題**（上一輪我回報「素材本身」是錯的）。根因是用 `object-view-box` 做 `steps()` 動畫，切格瞬間被補間、取景框滑到兩格之間。改用 1.0 的王驗證過的做法：動畫表當 `background-image`、`background-position` 用 `steps(4, jump-none)` 硬切（沿用 1.0 的 `@keyframes boss-frames`），`<img>` 本身內容用 `object-position:-10000px` 推到框外；框照單格比例（怪鳥 194×200、灰狼 261×200）。實證：每 25ms 連拍 24 張，`background-position-x` 只有 0%／33.3%／66.7%／100%，總覽圖親眼看過沒有切邊與雜物 |
| 難度 | 模擬器原本假設「每秒 6 下、在場全在點」，太勤快。新增 `CPS`／`TAP_SHARE`／`SKILLS` 環境變數，以一般玩家（每秒 3 下、一半時間在點）量：舊數字第 1 站 9 分、全線第 12 天 686 分。定案 `BASE_NEED 300000`、`GROWTH 1.17` |
| 王關 60 秒 | `BOSS_TIME 60000`、冷卻 60 秒；**輸了從滿血重來**（不保留血量——使用者拒絕）。第一次遇到自動開打，輸過就不自動，右上角「再次挑戰・N秒」（冷卻中停用），下方寫冷卻後按「再次挑戰」。倒數寫在護盾標籤前面「王關剩 N 秒｜…」，剩 10 秒內變紅 |
| 王血 | 護盾的放置倍率（0.35～1.0）與破盾下數（10～15）對輸幾次**幾乎沒影響**，卡的是王血跟 60 秒的比例。沒有保留時是懸崖：×0.07 對每秒 3 下／五成時間是 141 分，每秒 2 下／四成時間就掉到第 12 天、輸 267 次。王血 ×2.5 → **×0.04**；一般站血量從 200000 拉回 300000 補總時間 |
| 結果（×0.04） | 一般玩家（每秒 3 下、五成時間、放技能）：第 1 站 3 分、**第 3 天 142 分**、輸 2 次、抽 140 次。每秒 2 下／四成時間：172 分、輸 0 次。每秒 2 下／三成時間／不放技能：第 4 天 199 分、輸 4 次（×0.05 時這組卡到第 12 天）。勤快（每秒 6 下）：第 2 天 61 分。帶 1.0 加成 ×2（收益祝福 Lv10）：一般 69 分、勤快第 1 天 31 分——只回報，Codex 建議不為它再調 |
| 被動傷害數字 | 2.0 原本只有點擊數字。`tick()` 每秒比對同一站結算前後的血量差，在怪右側飄一個淡色 `-N`（`.floater.passive.apoc-passive`，2.6 秒上飄淡出；減少動畫只淡入淡出）；地圖開著、招募層開著、沒有怪時不飄；總數超過 12 個先移掉最舊的被動數字；離開末世清掉 |

#### 〇之九之二、Codex 第五輪複檢（`VERDICT-apoc-round5.md`，當時還有保留傷害）與處理

| 分級 | 問題 | 處理 |
|---|---|---|
| 必修 1 | 加了 60 秒但看不到倒數（`#boss-timer` 在末世是隱藏的） | 倒數寫進 `#effect-label`：「王關剩 N 秒｜護盾…／破防…」，≤10 秒 `.urgent` 紅字；apoc-play 驗 |
| 必修 2 | 舊存檔進行中的戰鬥是舊血量、`deadline` null，永遠不會輸 | `normalize`：`stage.need` 跟新版 `need()` 不同就照剩血比例換算一次（換完 need 相同，不會再換）；期限在第一次 `settle` 才給，不在 normalize 給（避免每次載入續時）；單元測試 |
| 必修 3 | 逾時後、下一次 tick 前點擊仍判勝 | `tapDamage`／`tap()` 過期回 0／不變；`settle` 放置傷害只算到期限為止再判勝負（期限前的致死傷害照樣贏）；單元測試 |
| 值得修 | 冷卻與保留傷害沒呈現 | 冷卻顯示「再次挑戰・N秒」；保留傷害已拿掉，不需要 |
| 值得修 | 模擬器沒算 1.0 加成、沒裝技能格 | `ONE_BOOST` 環境變數；每次抽完把戰力前 4 張裝進技能格 |

#### 〇之九之三、Codex 定案版複檢（`VERDICT-apoc-round5b.md`）與處理

| 分級 | 問題 | 處理 |
|---|---|---|
| 必修 1 | 期限過後、下一次 tick 前點擊：UI 用 `settle(…, 0)`，期限前還沒結算的放置傷害被吞掉，本來會贏的判輸 | `tap()` 看到期限已過，先用距上次 tick 的 dt 結算（放置傷害照算到期限），`lastTick` 往前推避免重算；這一下不算；被補算打死時浮字寫「擊倒！」。單元測試證明 dt 補算會贏、dt 0 會輸 |
| 必修 2 | 破防與期限同時結束：先重新起盾再算整段，期限前的 ×2 被算成護盾 ×0.35 | `settle` 依破防結束、技能到期、期限切段，各段用段中時間的倍率；傷害算完才重新起盾。單元測試：同時結束會贏；破防 1 秒＋護盾 1 秒＝100×(2+0.35) |
| 值得修 | 失敗說明寫進 `#package-result`，但 1.0 這格預設 `opacity:0`，看不到 | 末世輸了王才掛 `.apoc-shown`（`opacity:1`），`leave()` 拿掉；apoc-play 驗 computed opacity |
| 值得修 | 被動數字滿 12 個時會刪掉點擊數字 | 只換掉舊的被動數字；全是點擊數字就這一秒不飄 |
| 不用修 | 舊存檔殘留 `startHp`、王血 ×0.04 讓王比前一站短（第 4 站王 19219 vs 第 3 站 410670，獎勵 384 vs 8213） | 無讀取用途；屬體驗取捨，回報使用者 |

驗收：`npm test` 207；`clicker-v3-apoc`／`apoc-play` 全過；v3 gate／prestige／cleanup／team／teamui／drag、round14／16／17／19 全過；UI 體檢 54 畫面 0 條。

**這一輪踩到的坑**
- 模擬器的「技能放／不放」兩組結果一模一樣：模擬器從來沒把卡裝進技能格，技能沒被模擬到。
- 瀏覽器測試把 `BASE_NEED` 壓到 3000 加速，王血只剩一般站 ×0.04，點滿 15 下破盾之前王就死了。只在畫面上假改 `stage.need` 沒用：「再次挑戰」照真實 `need(3)` 開打瞬間打死。改成暫時把 `RULES.BOSS_MUL` 放大、測完還原成原值（不要寫死數字，調難度時會漏改）。
- 補丁的防呆斷言寫「檔案裡不能再有 object-view-box」，結果我新寫的註解就有這個字——斷言擋在寫檔之前，檔案沒被改到。斷言要檢查「舊規則拿掉、新規則加上」，不要拿註解會出現的字當判斷。
- 只看一般玩家一組數字會選錯倍率：×0.07 一般玩家很好看，稍微慢一點就是懸崖。調固定時限要多掃幾組慢的節奏。

複檢：`REVIEW-BRIEF-apoc-round5.md` → `VERDICT-apoc-round5.md`（保留傷害版）；`REVIEW-BRIEF-apoc-round5b.md` → `VERDICT-apoc-round5b.md`（定案版）。

### 〇之十、末世第六輪：王變門檻（照 Sakura Clicker 等比縮到約 1 小時）、輸了刷前一站、訓練改乘算（仍未上線）

使用者看完 `86a9bea`：**「關卡的難度幫我也同步，不然會造成王太簡單、路上太難」**（第五輪王血壓到一般站 ×0.04）。
我回報限制：2.0 戰力靠收藏，抽滿就停在兩三千；王要比路上硬又是 60 秒，路上一站只剩幾十秒、整條幾分鐘。
問答：選 **「王變門檻：打不過就去抽卡／練」**，補充 **「對照之前的參考遊戲，然後用等比的方式把時長降低」**，並給了 `D:\SteamLibrary\steamapps\common\Sakura Clicker`；時長選 **「約 1 小時（縮到 1/6）」**。

**參考曲線**（`REFERENCE-2026-09-12-sakura-clicker.md` 的逆向公式，重跑舊的 `sakura_sim.py` 每 5 區取點）：一般玩家（每秒 3 下）到第 10／20／30／40／50 區是 2.5／11.6／34／108／326 分，剛好對應我們 5 個王站（第 4／8／12／16／20 站）。目標＝它的 1/6：**0.4／1.9／5.7／18／54 分**。

| 項目 | 做法 |
|---|---|
| 血量 | 一般站 `5000 × 2.1^站`；王站再 × `BOSS_MULS [2,3,4,5,6]`（照 Sakura 的王＝該區怪 ×2～6 逐隻遞增）→ 王一定比前一站硬 |
| 獎勵 | `血量 × 0.08 × 1.066^站`（Sakura 金／血成長比 1.59/1.55≈1.026／區，一站≈2.5 區）；拿掉王血 `BOSS_MUL` |
| 王關 | 維持 60 秒、冷卻 60 秒、輸了滿血重來 |
| 刷怪 | 王輸過（`bossFailed === progress`）→ 自動回前一站一直打（`fight(a, now, true)`，stage 帶 `farm:true`）：拿前一站獎勵、不推進度；刷怪中「再次挑戰」照樣看得到，按了直接換掉刷怪的戰鬥；`normalize` 只留合法的刷怪戰鬥（這站是王、輸過、index＝progress−1） |
| 訓練 | 線性 → **乘算**：全隊／點擊每級 ×1.1、價格 500／300 × **1.3^級**。戰力 ∝ 花掉的錢^0.36，越後面越沒效率＝牆（收藏會抽滿，只有訓練能一直長） |
| 畫面 | 血條「第 N 站・刷怪中」、失敗說明「王關失敗：在前一站刷錢變強…」、通知同步；刷怪打死也有擊倒演出；訓練預覽改乘算 |

**結果**（`tools/sim/apoc.js`，一天三段 20 分，技能格裝戰力前 4 張，戰力推估夠了才再挑戰）：

| 玩家 | 5 個王站（分） | 全線 | 王關輸 |
|---|---|---|---|
| 一般（每秒 3 下、五成時間；三個種子＋Codex 另跑兩個） | 0.4～0.7／3～4.6／16～17／29～32／49～54 | **49～54 分**（第 1 天） | 3～4 |
| 慢（每秒 2 下、四成時間） | 0.8／6／25／52／85 | 82～89 分（第 2 天） | 3～4 |
| 很懶（每秒 2 下、三成時間、不放技能） | 1／9／32／65／110 | 102～114 分（第 2 天） | 4 |
| 勤快（每秒 6 下） | 0.3／1.5／6.7／11／18 | 18 分 | 3 |
| 一般＋1.0 加成 ×2 | 0.2／1.3／8／15／30 | 30 分 | 3 |

形狀：總長對準 54 分，但中段（第 8～16 站）比參考遊戲前重、最後一段沒有它那麼陡——Sakura 最後 10 區佔 2/3 時間，我們最後 4 站約佔 4 成。

**這一輪踩到的坑**
- 原型模擬器（scratchpad `apoc_gate.js`）用「事件獎勵 − 舊版獎勵」補正金幣，舊版 `BASE_NEED 300000` 前幾站的獎勵很大，錢被扣成負的，數字全錯（一般 62 分 → 真的經濟模組 22 分）。原型只能拿來看方向，數字要用真的經濟模組跑。
- 模擬的慢玩家「點 50 秒、停 50 秒」規律切換：王關整場落在停手那段 → 打掉比例很低 → 推估要戰力 ×20 才再挑戰 → 卡在刷怪幾十分鐘，同一組參數小改就從 27 分跳到 172 分。改成「打王一定會點，TAP_SHARE 只套在一般站與刷怪」後，三種玩家的比例穩定在 1 : 1.7 : 2.2。
- 獎勵跟血量等比例，所以單純拉高每站血量成長拉不長總時長（血多錢也多）；決定長度的是「每點血給多少錢」和「訓練價格成長」。
- 放置收入（每秒戰力 ×1%）不是限制長度的原因（壓到 0.1% 幾乎沒變）。
- 改血量後 `clicker-v3-apoc.py` 的「點 12 下扣血」會失敗：第 1 站被打死換到血更多的第 2 站。改成「換站也算」；「點舞台空白處扣血」同一個原因，點之前先把這場的血加厚。
- 血條標籤欄在 1.0 固定 72px、`overflow:hidden`：「第 4 站・王關」從第五輪起就只露出「第 4 站・」，這輪的「刷怪中」也被切。測試只讀 `inner_text` 抓不到——看截圖才發現。末世改成跟著字長，apoc-play 補驗 `scrollWidth ≤ clientWidth`。

#### 〇之十之二、Codex 第六輪複檢（`VERDICT-apoc-round6.md`）與處理

| 分級 | 問題 | 處理 |
|---|---|---|
| 必修 | 刷怪沒有開場間隔：一擊必殺時連點會「打死 → `tap()` 裡的 `autoFight` 立刻開下一場」，實測 750ms 刷 6 場；模擬器一秒只結算一場，約 1 小時的驗收不代表這條路 | 經濟層加 `FARM_RESPAWN 1000`：刷怪勝利記 `farmNextAt`，`canFarm(a, now)` 過了才能開下一場（跟模擬器一秒一場一致）；空檔畫面照樣畫前一站、寫刷怪中，不閃成王；單元測試 |
| 值得修 | 存檔把 `teamLevel`／`clickLevel` 改成 10000 → 乘算變 Infinity | `TRAIN_MAX 1000`：`normalize` 夾上限、`train` 到頂丟「已經練到頂了」；單元測試 |
| 不用修 | 勝王後殘留 `bossFailed`、刷怪重載／冷卻／重挑、舊檔 Lv10 變 ×2.59、1.0 自己的 `BOSS_MUL` | Codex 實測沒有漏洞，維持 |

驗收（修完之後）：`npm test` 210（末世經濟含刷怪流程、刷怪一秒一場、等級上限、王血逐隻遞增）；`clicker-v3-apoc`／`apoc-play`（輸了自動刷怪、血條標籤沒被切、再次挑戰滿血）全過；1.0 的 `clicker-v3-gate`／`team` 全過；UI 體檢 54 畫面 0 條；模擬器加上刷怪間隔後數字不變（一般 54／慢 86／很懶 111／勤快 18 分）。
複檢：`REVIEW-BRIEF-apoc-round6.md` → `VERDICT-apoc-round6.md`。

### 〇之十一、第七輪：點擊特效華麗化＋噴金幣（兩個世界）、2.0 招募入口與繼續抽、收下淡出、地圖五段美術、技能格縮小（仍未上線）

使用者看完 `01362b1`（附圖：技能格、地圖往下重複）：
- 「抽卡的部分，裡面的介面已經有單抽、五連、十連的選項了，我希望招募夥伴那邊可以改成引導進入抽卡選單的按鈕，簡單顯示是否可以抽卡」「按下收下後的過場回到主畫面很不流暢」「我也希望他有繼續抽的功能（看當下是幾抽）」
- 「現在的點擊特效都不夠華麗，有辦法幫我強化嗎」「有沒有辦法製作類似 Sakura Clicker 點擊噴出金幣的特效，噴金幣應該可以帶來不少爽感」
- 「2.0 的技能格幫我縮小約 15%，比照 1.0 的大小」
- 「地圖的美術是不是沒有完善，他只有兩個區域，往下就重複而已，幫我完善」
問答（先做 `dist-web/fx-demo.html` 三欄樣品 A 現況／B 華麗／C 華麗＋噴金幣）：特效「**效果都不錯，都做**」、「**兩個世界都套**」；招募入口「**只有 2.0**」。

| 項目 | 做法 |
|---|---|
| 點擊特效 | 新模組 `src/clicker-hitfx.js`（兩個世界共用）：`impact()` 碎片＋光痕＋四芒星＋中心閃光＋衝擊波，等級 0 一般／1 爆擊・技能／2 破盾／3 擊倒（破盾、擊倒再加一圈光環）。2.0：每一下輕微震動、越重越大；浮字大一號、彈出放大。1.0：預設碎紙換華麗版；更衣室換過的點擊特效（緞帶、星星…）保留自己的形狀；技能重擊震動；浮字 26→30px 起跳 |
| 噴金幣 | `coins()`：從落點往上噴 → 在**血條上緣**彈跳落地（第一版落在舞台底，壓到「第 N 站」與血量數字，看截圖才發現）→ 停一下 → 貝茲曲線飛進錢包，抵達時錢包跳一下、冒小閃。一般 2／爆擊 5／破盾 8／擊倒 18 枚，**每秒最多 24 枚**。1.0：一般 2、技能 5、拆完一包 8。減少動畫：1.0 維持原碎紙、不噴；2.0 只留浮字 |
| 2.0 招募入口 | 招募卡的「招募！」＝「**進入招募**」（小字：可以抽・最多十連／還差 N／有結果還沒收下），十連鍵收起。按下打開典藏包入口（`ceremony.enter()`），原型的單抽／五連／十連三顆鍵打開、小字是價錢、買不起停用，另有「返回」與 Esc。按下去**不是原型的 `pull()`**：橋接在同一顆鍵上用 capture 攔下、`postMessage({apocCeremony:'pull', n})`，主頁扣錢寫 pending 後才 `play()` |
| 繼續抽 | 結果頁「收下」旁邊「**繼續單抽／五連／十連 · 價錢**」（張數＝這一次抽的張數，買不起停用）。按下 → 主頁落帳 → 直接 `start(同張數)`，典藏包不關、不閃回主畫面；起不來就正常收尾。重新整理後放回結果頁也有 |
| 收下過場 | 以前：演出頁 220ms 清卡露出入口的卡包 → 主頁 240ms 硬關，還會 `open(); close();` 招募層一次。現在：一收到「收下」就淡出（`.closing`，280ms）、240ms 後落帳、關閉也是淡出；打開淡入。落帳失敗 `replay` 會把淡出收回來 |
| 地圖美術 | 只有第一段有真美術、二～五段是同一張佔位（`terrain.webp` 六張重複）。照第一段 seg1b 規格派 Codex imagegen 畫二～五段（補給線、夜市攤、冷藏庫深處、滅世都市；識別物、殘骸 ≥6、中央 30% 帶留給節點、上下接段帶色差 ≤18、預告痕跡），10 個 job，原稿／回報／參考在 `_art/apoc-map/`。遊戲裡：`terrain-2～5.webp`，地圖五張依序排、站點在每張的 20／40／60／80%、連線量節點中心；**戰鬥舞台背景跟著目前這一段換** |
| 技能格 | 2.0 貼紙 inset 2→7px（圓 60→50、連白邊約 54px，頭像約 44px；1.0 頭像 40px），冷卻遮罩跟著內縮 |
| 順手修 | 1.0「桌子有點滿了」便條掛在共用舞台上 20 秒：剛冒出來就切末世會留在 2.0 畫面 → 末世不產生、進末世清掉 |

#### 〇之十一之二、Codex 第七輪複檢（`VERDICT-apoc-round7.md`）與處理

| 分級 | 問題 | 處理 |
|---|---|---|
| 必修 | 抽卡選單有五連，但 `normalize()` 只收 1／10 張的 pending：五連扣了券，tick 一提交結果就被清掉 | `DRAW_COUNTS [1,5,10]`：pending 驗證與 `purchaseDraw` 一致；單元測試（扣款、重開後還在、收得下、其他張數不准買）＋驗證腳本實抽五連 |
| 值得修 | 2.0 每一下都震，但前一下的移除計時器沒取消，普通命中後馬上擊倒，擊倒的震動 90ms 就被切掉 | 記住計時器、新的一下先取消 |
| 值得修 | 重開／落帳失敗 `replay` 放回結果頁時沒傳續抽資料，「繼續抽」消失 | `restore(ids, badges, again)`，主頁照 pending 張數算 `againFor`；驗證腳本重新整理後檢查 |
| 值得修 | 地圖連線 x 寫死 43／47%，沒算交錯的 36px 與節點半寬 | 連線端點直接量節點 `offsetLeft + 半寬`、`offsetTop`，viewBox＝路線像素 |
| 不用修 | capture 攔截是否可靠、收下失敗恢復與 280ms 內重開、地圖圖片未載入的高度 | Codex 檢查有處理 |

**這一輪踩到的坑**
- `rm -rf dist-web` 失敗（Device or resource busy）：8123 伺服器是 `cd dist-web && python -m http.server`，行程的工作目錄鎖住資料夾。刪到一半、匯出沒跑，1.0 的靜態網頁測試（round7 讀 dist-web）跟著逾時。改用 `python -m http.server 8123 --directory dist-web`，匯出直接覆寫（`export-web.py` 本來就 `mkdir(exist_ok)` ＋ `copy2`）。
- `clicker-browser.py` 先後卡在浮字字級、點擊粒子數、拆完一包的包裝圖。用 `git worktree` 開 `01362b1` 跑同一支測試對照：**舊版全過**，所以都是這輪造成的——字級與粒子數是刻意改的（斷言改成新配方），包裝圖則是 1.0 拆完一包加了 `shake()`，拿掉之後就過（只在技能重擊震）。中間一次 `#hero > svg` 逾時沒再出現。
- 金幣粒子帶 `Image`，`fxEvents` 傳回 Python 會變成空物件，分不出哪顆是金幣——在頁面裡用 `p.img` 過濾計數。
- 金幣第一版落地線＝舞台底，壓在血條上；自動測試不會發現，截圖才看到。

驗收：`npm test` 211；`clicker-v3-apoc`／`apoc-play` 全過；`clicker-browser.py` 全過；v3 gate／team／teamui、round14／16／17／19 全過；UI 體檢 54 畫面 0 條；招募流程腳本（入口→返回→十連→繼續十連→收下淡出→五連→重新整理放回結果頁仍有繼續抽→收下 +5）全過；兩個世界點擊特效與地圖五段（橫式、直式）實機截圖看過。
複檢：`REVIEW-BRIEF-apoc-round7.md` → `VERDICT-apoc-round7.md`。

### 〇之十二、第八輪：地圖接縫縮小、王圖對上名字、2.0 看得到換桌布、每站 BGM＋神話技能 BGM、手機 UI 巡檢（仍未上線）

使用者看完 `1fdbfa1`：「地圖接縫縮小」「王的名字和圖對不上，請解決」「手機的 UI 介面檢查過了嗎？抽卡、隊伍編輯等等 UI 的呈現都合理嗎？」；途中追加「2.0 也要看得到換桌布相關的東西，不然要升級還要回去 1.0 按，很不直覺」「每個關卡都有自己的 BGM，神話卡的技能施放期間也要有自己的 BGM，幫我看之前的 8BIT 音樂專案去挑選適合的」。

| 項目 | 做法 |
|---|---|
| 地圖接縫 | 相鄰兩張地景疊 22% 圖高，後一張上緣 22% 用 `mask-image` 漸層淡入蓋住前一張的純色接段帶；站點改放每張扣掉疊合區後的 25%～75%（`clicker-apoc-map.js` `OVERLAP` 與 CSS `.map-tile + .map-tile` 要一致）。第一次 .18 手機截圖還看得出一條帶，加到 .22 |
| 王的圖 | 地圖上是 2.0 企劃（`PLAN-2.0-levels.md`）定的五隻，戰鬥卻借 1.0 的鳥／灰狼／罐頭／箱子／袋子。換成企劃指定的原素材 holo-5.0 `_art/holo-test/source-4.0/怪物/`：灰狼犬 `01.gif`（5 格）、貼紙羊 `ej93j4.gif`（7 格）、扛槌兔 `image.gif`（9 格，白底）、雞頭合成怪 `rise.png`（單張，白底），轉成逐格圖放 `src/apoc/boss/`。**去白底**：第一版照「接近白色就 flood fill」會把白色身體一起吃掉（兔子只剩線、雞身變透明）；改成「深色線條加粗成牆、從四邊 flood fill 只填到牆」。滅世珍獸企劃「素材未定」→ 1.0 的滅世珍獸畫裁掉 REC 取景框、四周橢圓淡出（原圖是有大樓的整張畫，直接放像貼一張照片）。逐格改成 `.apoc-sprite`＋`--frames`／`--frame-ms` 變數 |
| 2.0 換桌布 | 頁尾「換桌布」以前在末世是「重走廢土」佔位、按了沒反應。改成末世也打開換桌布面板，預設停在「神器與商店」；在末世按換桌布只蓋印記章，不捲 1.0 的桌布、不掛 1.0 場景 |
| 每站 BGM | 之前的 8bit 專案＝ChipForge（`D:\claude研究\chipforge`，遊戲內 `src/chipforge`），263 個主題、即時作曲。`clicker-apoc-map.js` `MUSIC` 20 站各一個主題＋固定 seed（`apoc-station-N`）：後院 迷失之路／夜間平原／霧之谷／王 宿敵對決；補給線 調車場／機庫整備／監控室／王 機械巨獸；夜市 後巷貓影／霓虹雨／地下俱樂部／王 破城巨兵；冷藏庫 酒窖／極地凍原／密室／王 冰霜巨龍；滅世都市 廢棄殖民地／雨中街道／黑洞邊緣／王 真·最終戰。刷怪放前一站的曲 |
| 神話技能 BGM | 施放神話卡（10 下 ×10）期間疊「熱血王道戰」（heartbattle）；`useSkill` 記 `fx.mythic`，10 下用完清掉；傳說卡同樣是點擊加倍但不觸發。`clicker-music.js` 抽出 `current(state)`：1.0＝掛上的場景曲＋lastboss、末世＝這一站的曲＋神話技能曲；key 換了就交叉淡化，技能曲一起換（跟新曲同調、帶新曲動機）。末世的王關不再疊技能曲（王站自己就是頭目戰曲）。呼叫點：每秒 tick、施放、10 下用完那一擊、離開末世 |
| 手機 UI 巡檢 | 390×844 觸控，兩個世界主畫面、點擊、招募入口／結果、編隊、名冊與詳情、商店、戰績／統計、換桌布、模式、地圖、五個王站各截一張看過。修掉：①**網頁版按 Esc（沒有面板開著時）會掉進 `closeWindow()`：`visible=false`＋`suspend()`，之後所有 `action()` 被擋、整個遊戲沒反應**——巡檢腳本卡在「模式」打不開才追出來；改成只有桌面版（Tauri）才關視窗。②2.0「進入招募」按鈕固定高度，小字從底邊溢出 → 高度跟內容走、字改短（可抽・十連／差 N）。③2.0 夥伴列兩端淡出遮罩吃掉第一張卡的名字 → 左右留白。④2.0 編隊第一排的編號被格線上緣切掉 → 上方留白 |

驗證：`npm test` 212（加神話技能 `fx.mythic` 開關）；BGM 腳本（1.0 lastboss、末世第 1 站迷失之路、第 4 站宿敵對決、刷怪放第 3 站霧之谷、神話技能疊熱血王道戰且場景曲壓到 .036、10 下用完技能曲淡出、回桌邊換回 1.0、2.0 換桌布停在神器與商店）全過。
複檢：`REVIEW-BRIEF-apoc-round8.md` → `VERDICT-apoc-round8.md`。

### 〇之十三、第九輪：開封演出、一站 5 隻、1.0 測試冷啟動時序（仍未上線）

使用者：「我想要有更好的開封效果，點下去卡包微微震動、發光、縮小，然後回彈噴出卡片」「可以利用增加每一站需要打的小站數量拉長遊戲時長，再幫我注意難度平衡」；另要求接著查 1.0 測試。

| 項目 | 做法 |
|---|---|
| 開封演出 | `build_ceremony.py` 替換原型 `prelude` 的兩段：點下去 620ms 蓄力（左右微震、縮到 .84、亮度與光暈漸增，`sound('charge')`）→ 240ms 回彈到 1.24 閃光 → 原本的撕包與發牌。拿掉「往下壓 5px、縮回 .985」（會吃掉回彈）。每 80ms 連拍看過節奏 |
| 一站 5 隻 | `RULES.WAVES`：一般站連打 N 隻，stage 帶 `wave`／`waves`，打死換下一隻滿血、事件 `wave`，最後一隻才推進度；王站與刷怪一隻。畫面「第 3 站・2/5」。**第一版每隻給整份獎勵，錢變 N 倍、訓練長太快，打越多隻全線反而越短（一般玩家 54 分 → 8 隻 38 分）**；改成一隻給 `reward ÷ WAVES`（刷怪同），時長才照隻數拉長。模擬一般玩家 1／3／5／8 隻：54／106／164／239 分，王關輸 3～4 次不變。使用者選 **5 隻**（慢 329、很懶 435、勤快 37 分） |
| 1.0 測試 | `clicker-browser.py` round4 的 `#hero > svg` 失敗：對照「跳過前面幾輪直接跑」必失敗、「先跑過 round6／round5」就過——冷啟動時點擊區比珍母早啟用，等待條件補上珍母。上一版 `1fdbfa1` 同樣會壞，不是第八輪造成的 |

### 〇之十四、第十輪（使用者睡前交代、自己做到完）：待辦總表與定案

使用者 09-14 深夜：「幫我把所有還沒做的都完成」「更新 GIT，我明天去公司繼續開發」「新版本上架 Cloudflare 讓朋友試玩，GitHub Page 先保持舊版」「自己 LOOP 到完成任務，然後關機」「記得讓 GPT-6 來檢查一遍，注意 UI 正確性、字體好不好閱讀，之前踩的坑不要踩」「最後記得更新 GIT」。

**Cloudflare 試玩版**：https://zhenmu-dots-beta.clawd-pet.workers.dev （Workers 靜態檔案；OAuth 權限沒有 Pages，所以用 Workers assets）。部署：`python tools/export-web.py && npx wrangler deploy --config tools/cloudflare/wrangler.jsonc`。GitHub Pages（gh-pages）不動。

**定案（問答）**
| 題目 | 答案 |
|---|---|
| 五隻王的機制 | 照企劃 `PLAN-2.0-levels.md` 三：灰狼犬＝先清狗群；貼紙羊＝一層層剝殼；扛槌兔＝在槌落地的拍子上點；雞頭合成怪＝按順序打部位；滅世珍獸＝前四種輪流 |
| 部位怎麼點 | 怪身上浮出大圓鈕，亮的是下一個；點空白處照樣有普通傷害 |
| 重走廢土 | 兩個都做：重來＋永久加成（幅度交給模擬器：第二輪比第一輪快約 30%，之後漸快）＋無盡模式 |
| 2.0 系統 | 能補的都補：離線收益、徽章、派遣 |
| 離線 | 在目前那一關持續賺錢，不會自己前進任何關卡 |
| 派遣 | 拿末世金幣，低機率拿到券 |
| 做完 | 測試與 GPT-6 複檢過、push、Cloudflare 更新、交接更新之後關機 |

**批次**
- A：五種王關機制（經濟、畫面圓鈕／節拍／層數／小怪數、模擬器、測試）
- B：王前預告關（預告痕跡提示）、2.0 新手引導、全線通行結局
- C：重走廢土（勳章加成）＋無盡模式
- D：2.0 離線收益、徽章牆、派遣
每批：測試、桌機＋手機截圖親眼看（字級、截斷、遮擋）、Codex 複檢、commit／push、Cloudflare 重新部署。

### 〇之十五、第十輪批次 A 完成：五種王關機制

| 項目 | 內容 |
|---|---|
| 規則 | `RULES.BOSS_MECH`（取代舊 `SHIELD`）。機制＝`floor(站/4)`：0 狗群（5 隻、各 6% 王血，清光 20 秒後再來 3 隻）、1 剝殼（王血 75／50／25% 長殼，殼 8%，只有點擊剝得動、放置打不動）、2 節拍（1170ms 一拍、±200ms 算踩中 ×3、沒踩中 ×0.5，連中 8 下破防）、3 部位（頭身尾照亮的順序點 4 下破防）、4 前四種每 15 秒輪流。破防 8 秒傷害 ×2；節拍／部位時放置傷害只剩 ×0.35 |
| 放置結算 | `idleBoss` 照事件時間推進（小狗死、長殼、再來、換機制、破防到期都切段），**一次結算 N 秒＝逐秒結算 N 秒**（測試有三個站對照）。坑：段尾剛好是換機制的時間點，拿段尾判斷機制會把傷害算進下一種 → `inside()` 夾在段內 |
| 畫面 | 部位＝怪身上三顆大圓鈕（亮的是下一個）；節拍＝兔子身上的收縮圈；狗群＝血條上排小狗圖示、王縮到遠處；殼＝白光；破防＝橘光。標籤文字短句、手機直式放大到 20px 並置中換行 |
| 平衡 | 機制讓舊王血 [2,3,4,5,6] 一般玩家變 228 分（滅世珍獸一隻 128 分）→ `BOSS_MULS [2,3,3.5,4,3.5]`：四個種子 161～179 分、踩中 5 成 192、慢 462、快 41，王關輸 4～5 次 |
| Codex 10A | 4 必修＋1 值得修都修了：放置依結算頻率不同（→事件推進）、打贏後又被重生翻盤（→只重生到結算終點）、分頁切回節拍圈不同步（→resume 重設）、壞存檔機制欄位（→`cleanMech`）、模擬器點擊時間不遞增 |
| 順手修 | 桌機末世商店：標題框多 2px 邊把說明字擠到虛線上、「進入招募」兩行字壓到機率行（橫式限定改列高）；1.0 測試量落點前先把 #stage 還在播的動畫播完（遊戲照 #stage 換算，測試照 #game 換算差 4.4px） |

### 〇之十六、第十輪批次 B／C／D 完成

**收工狀態（09-14 清晨，下次開工先看這段）**
- 第十輪待辦 A～D 全做完：五種王關機制、王前預告關、新手引導、結局數字、重走廢土、無盡模式、離線收益、派遣、徽章牆。
- Cloudflare 試玩版已更新到最新：https://zhenmu-dots-beta.clawd-pet.workers.dev （Workers 版本 `275a4f56`）。GitHub Pages（gh-pages `fd26878`，09-08）沒動。
- 最後一輪全跑過：`npm test` 223 過；`clicker-browser.py`（1.0 全套）、`clicker-v3-apoc.py`、`clicker-v3-apoc-play.py`、`clicker-ui-audit.py`（54 畫面 0 問題）、`clicker-v3-team.py`、`clicker-v3-teamui.py` 全過；scratchpad `batch_b/c/d.py`、`probe_offline_fail.py` 全過。
- Codex（GPT-6）四批複檢的必修全部修掉並有驗證（10A 4 條、10B 1 條、10C 1 條、10D 1 條）。
- 還沒做、可以討論的：離線＋派遣全開時一般玩家全破約 130 分（沒有這兩個系統 179 分），要不要調長；卡冊詳情停用鍵只做了淡化；沒有出 exe、沒有併 main。

驗收腳本在 scratchpad（`batch_b.py`／`batch_c.py`／`batch_d.py`，桌機 1280×860＋手機 390×844，截圖親眼看過）；Codex 複檢 `VERDICT-apoc-round10b/c/d.md`。

| 批次 | 內容 | 踩到的坑 |
|---|---|---|
| B 王前預告關 | 王前一站（index %4 === 2）舞台一圈暗紅（只動 opacity）、效果標籤寫「前方預告・王名｜打法」 | 舊化主題的標籤是紙片貼圖（border-image fill 蓋住背景），淺字整個看不見 → 保留紙片、改深紅粗字 |
| B 新手引導 | `apoc.tutorial` 0～4，四步紙片＋「知道了」；進度 ≥ 4 的舊存檔不冒；紙片出現時王關標籤移到血條上方 | 第 0 步不能等「還沒開戰」：tick 一能打就自動開戰；Codex 10B 必修：刷怪時別講「一站 5 隻」 |
| B 結局 | 六格數字（打贏／點擊／最高一擊／破防／招募／收藏） | |
| C 重走廢土 | 通關後重來：第 n 圈敵人血與獎勵 ×10×1.1^(n−1)、戰力 ×(1＋.25n)，最多 10 圈；保留收藏／隊伍／技能／券，金幣訓練進度歸零。結局面板兩顆鍵（按兩次才重走），戰績頁有「全線通行選項」。模擬一般玩家 146 → 109 → 48 → 41 → 40 分（HP_FIRST 3／4／6／10／16 第二圈 59／51／76／109／137） | `need(i)`／`reward(i)`／`killReward(i)` 全部要帶狀態（狗群血、殼門檻、刷怪、wave、normalize 舊血量換算、刷怪空檔的畫面） |
| C 無盡模式 | 第 21 站起一直打（王固定四種輪流）、記最遠、最多 100 站 | **存檔驗證 `apoc.progress <= 20`**：第 21 站 commit 被擋、畫面整個停住；關掉無盡後進度仍 >20 也被擋（Codex 10C 必修）→ `endless || cleared` 時放寬到 100 |
| D 離線收益 | 回來（載入、從桌邊切回、分頁回前景）補算最多 8 小時：（放置金幣＋一直打目前這一站的擊殺獎勵）×0.25，**不動進度與戰鬥**；王站／打完全線算前一個一般站；借 1.0 的收據面板。模擬 一般 179 → 158 分、慢 462 → 420 分 | 戰力太低時擊殺數 0，收據不寫「打了 0 隻」 |
| D 派遣 | 隊外的卡出去 2 小時，回來帶「目前這一站 20 隻」金幣 ×稀有度（神話 2、傳說 1.6、史詩 1.3），12% 多一張券；3 個位子；時間到 tick 自動收回；派遣中不能入隊／裝技能，名冊蓋「派」章 | 卡冊一跨頁 8 張，測試要翻頁才點得到 |
| D 徽章牆 | 戰績頁 14 個字章（五隻王、全線通行、重走、5 圈、無盡＋10、收藏 30／全部、破防 50、點擊一萬、派遣 10 次），從存檔現算不另存 | |

**Codex 10D 處理**
- 必修「離線入帳 commit 失敗後，下一個 tick 的 settle 把 seenAt 推到現在、收益永久消失」→ `offlinePending`：入帳沒寫進去就記著，tick 與點擊都先重試入帳、成功前不跑 settle。寫入失敗時 `store.blocked`、頁尾出「重試存檔」（`#save-error` 是載入失敗才出的面板）；按了之後下一個 tick 補發並出收據。驗收 scratchpad `probe_offline_fail.py`（藏分頁→撥回 8 小時→`Storage.setItem` 丟 QuotaExceeded→回前景→恢復→重試）。⚠ 第一版用切世界觸發：切世界本身要寫存檔，寫入失敗時根本沒進末世，測不到
- 值得修「派遣倒數不動」→ 卡冊 stateKey 派遣中帶分鐘數，每分鐘重畫
- 值得修「派遣要算進平衡」→ 模擬器每場開頭收回＋派滿隊外的卡（`DISPATCH=0`／`DISPATCH_KILLS` 可調）。一般玩家三個種子平均：不派 150 分、KILLS 20 → 81（單種子，全線少一半）、5 → 122、3 → 119、**2 → 129（−14%）取 2**。離線＋派遣全開時一般玩家約 130 分（沒有這兩個系統是 179），想拉長可以調 `OFFLINE.SHARE`、`DISPATCH.KILLS` 或 `BOSS_MULS`
- 順手：戰績格「1.0 印記加成」值太長被截成「戰力 ×1.…」→ 值只寫 ×1.10；「破盾次數」改「破防次數」（跟徽章一致）；卡冊詳情停用鍵淡化（紙片貼圖蓋住停用底色，看不出按不了）

**Python 改檔的坑**：Windows 上 `Path.write_text` 會把 `\n` 寫成 `\r\n`，整個檔案變 CRLF（git 看得出來）→ 改完一律轉回 LF（`write_bytes(b.replace(b'\r\n', b'\n'))`）。bash heredoc 裡塞含 `'''` 與反引號的 Python 會被 shell 吃掉 → 腳本先用 Write 寫檔再跑。

### 〇之十七、第十一輪：末世養成軸重做——兩週養滿（仍未上線到 gh-pages）

使用者 09-14：「我想要的是 玩家玩兩週 才有辦法把所有卡片都抽到滿星突破、卡冊全收集。
幫我依照這個方式去設計，但也要顧及打怪收益與打王的回饋感。一樣參考 Sakura Clicker 的比例去修改。」
追問範圍後定案：**「2.0 全收集的意思。1.0 算是前期新手教學，遊戲長度大概是 2.0 的三分之一而已。」**
中途補充：**「1.0 不用突破滿也可以進 2.0，我們是以 2.0 為主軸設計的」**（那道門沒有動）。

**目標函數換了**：以前是「全線通行 20 站要幾分鐘」，現在是「71 張全收集＋全滿星突破要幾天」＝14 天。
全線通行從終點降級成中繼點（第 5 天）。設計全文在 `DESIGN-2026-09-14-apoc-growth.md`。

**量出來的結構性問題（這不是調數字能解的）**：神話率 0.25% 分給 14 張＝每張 0.018%，
「14 張神話各抽到一張」期望 18,200 抽，而兩週的預算只有 1,260 抽。
**卡冊全收集不可能靠運氣**——所以補上兌換所（同 1.0 的萬用粉塵換指定卡），
而且 2.0 版**沒有的卡也能換**，換到第一顆就等於拿到這張卡。

| 項目 | 做法 |
|---|---|
| 粉塵 | `a.dust[id]` 累計、`a.universalDust` 萬用。張數（collection）與粉塵分家：collection 是「抽到過幾張」，dust 才是養成貨幣 |
| 星級 | 累計粉塵 `[1,2,3,5,8]` → 8 顆滿星（5★），每星 +25%（沿用 STAR_MUL） |
| 突破 | 滿星後五級，各 `[2,2,3,3,4]`（累計 14），每級 +10%。**滿養＝22 顆粉塵**。收下時自動突破 |
| 兌換所 | `exchangeDust()`，匯率 common/rare 1・epic 2・legendary 4・mythic 10（兩個方向同一張表，價值守恆） |
| 王掉粉塵 | 五隻王**首勝** `[30,60,120,240,480]` 萬用粉塵（重走廢土每圈重算）、無盡每站 `20×1.15^n`、派遣帶 `3×稀有度`。使用者：「也要顧及打王的回饋感」——以前王只給金幣 |
| 抽卡價 | `DRAW_GROWTH` 1.02 → **1.007**。1.02 在第 4 天就讓抽卡實質停住（單價 1.02^n 追過 2.1^站 的收入） |
| 模擬器 | `tools/sim/apoc.js` 加 `KEEP=1`（全線通行後自動開無盡玩滿天數）、會用兌換所、量全收集／全滿養的日子與萬用粉塵四個來源 |
| 畫面 | 夥伴列 `★5＋5`（不再是 ★30）、卡冊標題「滿養 n/71」、末世的粉塵罐不再收起來、結算徽章的「突破 n」 |

**驗收**（一般玩家 CPS 3、在場一半在點、一天三段 20 分）：
全收集 13 天、**全滿養 14 天**；四個種子 14／15／14／14；全線通行第 5 天 246 分（判準 3～5 天）；
王佔萬用粉塵收入 17%（判準 15～30%）。快手第 7 天、很懶第 20 天。
`npm test` 229；`clicker-v3-growth.py`（新增，18 條）、`clicker-v3-apoc`／`apoc-play`／`team`／`teamui` 全過；UI 體檢 54 畫面 0 條。

**⚠ 三個踩到的坑**
- `normalize` 原本用**合併過的** `a.dust` 判斷「有沒有粉塵」——空物件是 truthy，
  會讓**所有既有存檔的粉塵歸零**。要看 `raw.dust`。（測試抓到的，不是猜的）
- **舊存檔會被腰斬**：舊版「張數＝星數、每張 +25% 無上限」，一張抽了 59 次的精良卡以前是 ×15.5、
  現在封在 ×3.0，玩家一載入就卡在打不過的站。→ 超過滿養的張數一律折成萬用粉塵還給玩家
  （只在沒有 dust 欄位的舊存檔跑、純從 collection 推導，所以重跑不會重複加）。
- `.apoc-sticker` 是 `position:absolute; inset:7px`（為技能格那種有定位父層的小方格寫的）。
  放進兌換所的 grid 格子會脫離版面、撐滿最近的定位祖先，**變成一顆蓋住全畫面的大圓**。
  DOM 檢查全綠，只有截圖看得到——又一次「視覺改動一定要自己打開看」。

**1.0 的長度：先不動（附量測）**。`tools/sim/clicker-v3.js` 的參數是
`[每秒點幾下] [每段分鐘] [天數]`（很容易餵成 `20 3 14` ＝每秒 20 下、一天 3 分鐘，會得到完全相反的結論）。
正確的一般玩家（CPS 4）：**一天 20 分→滅世珍獸第 4.5 天（＝2.0 的 32%，剛好三分之一）；
一天 60 分→第 2.3 天（16%）**。要不要把 1.0 拉長成「同樣一天 60 分也要 4.7 天」是設計決定，
旋鈕是 `BOSS_K` 與 `CITY_MUL`，**還沒問使用者**。

**還沒做**：`clicker-browser.py` 的 round5 場景凍結仍紅（A/B 實測 `7e2c423` 也紅，長年紅燈）；
Codex 複檢這一輪還沒跑；沒有併 main、沒有出 exe、gh-pages 沒動。

### 〇之十八、第十一輪第二輪回饋：兌換所收緊、星改 1～10、王粉塵砍掉、印記粉塵下架

使用者玩過試玩版之後的四點（原話見 `DESIGN-2026-09-14-apoc-growth.md` §八）：

- **兌換所只能補已經抽到過的卡**（「這樣才有顯得第一次抽到的重要性」）。
  → 少了這條路，卡冊全收集就只剩運氣，所以補上**保底**：連續 `GROW.PITY.AT=180` 次沒新卡，
  下一張保證是還沒有的卡。保底放在抽卡裡而不是兌換所裡，是為了保住「第一次抽到」的重量。
  ⚠ 這個門檻非常敏感（能觸發幾次＝總抽數/AT，而總抽數只有三四千）：**250 就永遠集不滿、100 第 3 天集滿**。
- **星數改成連續 ★1～★10**（「★5＋5 我希望直接改成 6~10」）：前 5 顆升星、第 6～10 顆突破（寶石星）。
  ⚠ 順手抓到**末世卡冊的星星一直是舊語意**（張數＝星數，30 張畫成「★×30」），而且
  **末世詳情卡根本沒有星列**——那一支在畫星列之前就 `return` 了。兩處都補了。
- **王首勝粉塵 930 → 150**（「我認為這樣給太多」），佔粉塵收入 17% → 3%。
  補回來的方式是 `DRAW_GROWTH` 1.007 → **1.0055**。
- **印記商店的粉塵兌換下架**（`P.tradeDust` 改拋「停售」，同招募券）。
  `dustTradeCost`／`DUST_PER_TRADE` 保留——`clicker-save` 的 `marksClaimed` 對帳要用舊存檔的 `dustTrades`。

驗收：`npm test` 230；`clicker-v3-growth.py`（含 10 顆星不撐破詳情卡）、
`clicker-v3-apoc`／`apoc-play`／`team`／`teamui`、`clicker-round20.py` 全過；UI 體檢 54 畫面 0 條。
⚠ `clicker-round20.py` 的「ratio ≥200 點擊浮字 42px vs 46px」**A/B 實測 `282c084` 也紅**，不是這輪造成。

### 〇之十九、手機版三修＋窄機塞不下（使用者實玩回報）

⚠ **使用者前幾輪是在 GitHub Pages（`speshotdog.github.io/clawd-pet/`）測的，那裡還是 `0cb8b1a`（09-08）**，
所以第十一輪的東西一樣都沒有。回饋前先確認他開的是 Cloudflare 試玩版。
（辨識法：舊版底欄是「統計／場景／商店／換桌布／名冊」五顆，新版統計與場景已經搬到頂欄。）

| 回報 | 根因 | 修法 |
|---|---|---|
| 點擊特效沒有在點擊區域上（差 376px） | 浮字層與粒子畫布在直式寫死 `left:0;top:0;width:608px;height:360px`：位置釘在 `#game-content` 左上角（舞台在 y≈376），而且 608×360 的盒子配 960×640 的 canvas 點陣還被非等比擠壓 | `fitStage()` 發布 `--stage-x/--stage-y`；新增 `#fx-clip` 外框（舞台大小、`overflow:hidden`），裡面放 960×640 畫布，`scale(k) translate(-16px,-72px)` 把遊戲座標原點對回舞台 |
| 字體框突出 | 直式的 `.upgrade h2 { font-size:12.5px }`（0,1,1）**從來沒生效過**——被 `#shop h2`（1,0,1）的 18px/24px 蓋掉，18px 的字塞進 29px 的框，膠帶上下各吃 10px | `#shop .upgrade h2, #shop .recruit h2 { font-size:15px; line-height:20px; padding:5px 10px }`（框/行高 1.5，同橫式比例） |
| 神器商店不友善 | `.mark-list` 是 `1fr 1fr` 又沒有 `min-width:0`，卡片撐成 286px，兩欄 572px 塞進 350px 面板 → 右欄整排被切、價格被擠成一字一行 | 直式改單欄、價格 `nowrap`、圖示縮到 38px |
| 部分手機還是會突出（360px） | 升級／招募列中間欄用 `1fr`——自動最小值是 min-content，而標題 `nowrap` 不肯縮，右欄的「五連」鍵被擠出列外 3px；票根 `min-width:92px` 又吃掉購買區一半 | 改 `minmax(78px,1fr)`（≤340px 用 56px）、標題上省略號、票根 `flex:none`、≤380px 縮字級並放掉 `min-width`；**票根內距不能小於膠帶的 10px 邊框**，不然數字最後一位會被膠帶蓋住（360px 實測「6,047」看起來像「6,04」） |

⚠ 改 `#fx-clip` 中途踩到兩個坑，都是 UI 體檢擋下來的：
直接放 960×640 會撐出橫向捲動、整個版面推到負座標（**83 條**）；
把裁切加在 `#game-content` 上，末世開地圖時舞台沒版面、`--stage-x` 算出負值，
會把整個內容推到 x=-37（**47 條**，全在末世那幾格）。

新增 `tools/test/clicker-v3-portrait.py`（31 條）：框高/行高比、直式字級、
點擊浮字全部落在 `#tap` 內、特效層不撐出橫向捲動、
**六種手機寬度（320/360/375/393/412/430）**的升級與招募列不戳出列外、價格不被膠帶蓋掉、
神器清單單欄不超框。

驗收：`npm test` 230；UI 體檢 56 畫面 0 條；round30/31/32、v3-growth/saveio/apoc 全過。
⚠ `round33.py` 的「這一包裡有 1 張沒抽過的卡」是抽卡隨機性造成的偶發（三次跑兩次過），跟這一輪無關。

### 〇之二十、舊存檔的大掃除補償：匯入之後看不到（使用者：「朋友們導入檔案都看不到」）

**補償沒有消失，是看不到。** `legacy`（補償旗標）是 `ClickerSave.validate()` 在載入
**version 2** 的存檔時就地遷移寫上的，而 `cleanupPage()` **只在遊戲啟動時呼叫一次**
（`clicker.js` 的 init）。匯入走的是 `commit → reload()`，而 `reload()` 只重畫場景／夥伴列／舞台，
不會再叫 `cleanupPage()` ——所以玩家匯入完當下什麼都沒有，要自己按重新整理才會冒出來。

實測（`tools/test/clicker-v3-legacy.py` 之前的 scratchpad 版）：
匯入後 `legacy.seen = null`（補償還在）但 `#cleanup.hidden = true`；手動 F5 之後才 `hidden = false`。

| 改動 | 內容 |
|---|---|
| 匯入後補叫 | `confirmImport()` 成功之後呼叫 `cleanupPage()`；有開起來就不再蓋掉它的焦點（不發「存檔已匯入」的 notice） |
| 手動觸發鍵 | 戰績頁工具列新增 `#cleanup-open`「大掃除補償」（使用者指定）。**只有 `s.legacy` 存在才顯示**；`cleanupPage(true)` 可以強制開，已經看過的也能再開——當初按「保留進度」的人還能改選「從零開始」 |
| 標籤長度 | 固定五個字，看過與否放 `title`。360px 的工具列四顆鍵剛好塞滿，加「（已看過）」會擠 |

⚠ **哪些人拿不到補償**：`legacy` 只在 `version === 2` 的存檔遷移時建立。
朋友如果已經在新版開過檔（version 3），就沒有 `legacy`、也沒有補償——這是設計如此，不是 bug。
那顆鍵對他們是隱藏的，手動點也會回「這份存檔不是從舊版遷移過來的」。

新增 `tools/test/clicker-v3-legacy.py`（16 條）：全程走玩家真的會走的路
（統計 → 匯入存檔 → 檢查 → 確定覆蓋），驗補償頁當下就出現、
「從零開始」按兩次才生效且真的發徽章「舊時代的珍母」＋魔花少女收藏卡、
舊進度封存成 `legacy.snapshot`、進度真的歸零、卡冊看得到收藏卡入口、
已看過仍能手動再開、沒有 legacy 的存檔不顯示那顆鍵。

驗收：`npm test` 230；UI 體檢 56 畫面 0 條；v3-legacy／portrait／saveio／growth、round31 全過。

### 〇之二十一、2026-09-14 下半場：手機版面、收藏卡、字型、入口（仍未上線到 gh-pages）

⚠ **使用者是在 GitHub Pages 上測的，那裡停在 `0cb8b1a`（09-08）**。回饋之前先確認他開的是
Cloudflare 試玩版（辨識法：舊版底欄五顆是「統計／場景／商店／換桌布／名冊」，
新版是「模式／換桌布／編隊／名冊／商店」，統計與場景在頂欄）。

| 回報 | 根因 | 修法 |
|---|---|---|
| 點擊特效不在點擊區（差 376px） | 浮字層與粒子畫布在直式寫死 `left:0;top:0;width:608px;height:360px`：釘在 `#game-content` 左上角（舞台在 y≈376），而且 608×360 的盒子配 960×640 的 canvas 點陣被非等比擠壓 | `fitStage()` 發布 `--stage-x/--stage-y`；新增 `#fx-clip` 外框（舞台大小、`overflow:hidden`），裡面放 960×640 畫布，`scale(k) translate(-16px,-72px)` 把座標原點對回舞台 |
| 字體框突出 | 直式的 `.upgrade h2 { font-size:12.5px }`（0,1,1）**從來沒生效過**——被 `#shop h2`（1,0,1）的 18px 蓋掉 | `#shop .upgrade h2, #shop .recruit h2 { font-size:15px; line-height:20px; padding:5px 10px }` |
| 神器商店不友善 | `.mark-list` 是 `1fr 1fr` 又沒 `min-width:0`，兩欄 572px 塞進 350px 面板 | 直式改單欄、價格 `nowrap`、圖示 38px |
| 窄機「五連」鍵被切 | 中間欄用 `1fr`（自動最小值＝min-content，標題 nowrap 不肯縮）；票根 `min-width:92px` 又吃掉購買區一半 | `minmax(78px,1fr)`（≤340px 用 56px）、標題省略號、票根 `flex:none`、≤380px 縮字級。**票根內距不能小於膠帶的 10px 邊框**，不然數字最後一位被蓋住 |
| 匯入舊存檔看不到大掃除補償 | `legacy` 是 validate 遷移時寫的，但 `cleanupPage()` 只在啟動時跑一次；匯入走 `commit → reload()`，`reload()` 不會再叫它 | 匯入成功後補叫；戰績頁加手動觸發鍵 `#cleanup-open`（只有 `s.legacy` 存在才顯示，已看過也能再開） |
| 收藏卡「非常的模糊」 | **卡冊縮圖那套尺寸**：`.album-slot .card` 是 150×210 再 `transform:scale(.6)`，精裝卡是 iframe，**內部就照 150px 排版** | 收藏卡頁當獨立頁面：卡照真實尺寸畫大張（桌機高 296px、直式寬 `min(280px,78vw)`）。⚠ 百分比高度會塌成 0、`vw` 在桌機是錯的（遊戲是被縮放的盒子），只有直式的 `#game` 沒縮放才能用 vw |
| 卡面字型不對 | 內嵌的兩個 woff2 是 **996／984 bytes 的空字型**（上游 `build_round4_fonts.py` 少 `populate()`）；`fonts/holo-*.woff2` 又缺「魔」「少」「殊」 | 新增 `tools/apoc/build_fonts.py`：字集從卡池算、正確子集、產完自驗、會分辨「子集弄丟」與「原始字型本來就沒有」 |
| 卡片不是最新版 | 遊戲用的是 `221dedf`（三十四～四十一輪），`holo-gift` 已到 `7f8d08c`（四十三輪） | 新增 `tools/apoc/import_collect_card.py`：搬過來會補「嵌入模式 `?embed=1`」（原始頁沒有）並提醒跑 build_fonts |
| **手機找不到 2.0 的入口** | `#mode-open` 以前「末世沒解鎖就藏起來」，玩家根本不知道有 2.0（桌機一樣） | 一直顯示。模式面板本來就會把末世畫成鎖住的卡並寫明條件，**門檻不變** |
| 直式打王王掉出舞台 | 王圖比 `#boss-view` 再往下 50px、血條掛在 `bottom:-16px` | 直式 `#boss-view { top:18px; height:240px }`、`#boss-image` 改 100% |
| 打王時想收起背景珍母 | — | `layout()` 依 `!!s.boss \|\| bossBusy` 掛 `.boss-on`，`#hero` 淡出 .28s |
| 編隊上限「字跑不出來而且都是神話」 | 字沒錯（那是四條累加上限），是 `text-overflow:ellipsis` 截成「神話…」「神…」 | 使用者定案「一行就好」：一條 20 格長條依階級上色＋一行短圖例 |
| 未獲得的卡露出名字 | 卡面照樣印真名，但下面那行是「？？？」 | 使用者選「全遮」：`maskEntry()` 把名字換成「？？？」，卡冊格子／詳情頁標題／大卡／**放大預覽**四處都遮；稀有度留著 |

**收藏卡現在兩個世界都只有精裝版**（`makeCard` 不分世界回 `deluxeCard()`），
放大走跟末世卡同一個 `#card-zoom` 內嵌層，**詳情頁那顆開外部分頁的「看精裝版」已整顆拿掉**。

**上限格的兩個 CSS 坑**（都是使用者看出來的）：
- 配色要用**卡面那組**（`gacha-card.css` 的 `:root`）：精良藍／史詩紫／傳說橘／神話彩虹，不要自己調。
  神話的彩虹要 `background-size:100%` 壓進一格，拉開只會看到中間淡黃、像沒上色。
- 上限線不能用 `margin-right` 撐開：格子是 grid 等寬軌道，margin 從自己那格扣，
  **第 2／6／12 格會變半格寬**。改成畫在 `gap` 裡。

**新增的測試**
| 檔案 | 驗什麼 |
|---|---|
| `clicker-v3-saveio.py` | 四種尺寸走完匯出→匯入→檢查→確定覆蓋，**對面板**判斷（體檢的「畫面外」用視窗算，桌機是被 `#game` 裁掉的抓不到） |
| `clicker-v3-legacy.py` | 匯入 v2 檔補償頁當下就出現、從零開始真的發徽章＋收藏卡、手動鍵可再開 |
| `clicker-v3-portrait.py` | 框高/行高比、直式字級、浮字落在 `#tap` 內、特效層不撐出橫向捲動、**六種手機寬度**的升級／招募列 |
| `clicker-v3-collect.py` | 字型不是空的／有卡名用得到的字／精裝頁不准內嵌 data URI；兩個世界都是精裝版、不開新分頁；**未獲得的卡四處都遮**（會穿進 shadowRoot 比對，不然是永遠綠的假驗證） |
| `clicker-ui-audit.py` | 加「統計・匯入存檔」一格（56 畫面）；負控制驗過會報 5 條 |

**驗收**：`npm test` 230；UI 體檢 56 畫面 0 條；手機實機走過一遍
（點擊特效 5/5 在點擊區、上限格 20 格同寬 12.7px、模式面板寫明解鎖條件、
打王時珍母 opacity 0 且王與血條都在舞台內、無 pageerror）。
⚠ 既有紅燈：`clicker-round20.py` 的「點擊浮字 42 vs 46px」、`clicker-browser.py` 的 round5 場景凍結，
兩條都 A/B 驗過在更早的 commit 就紅了。`round33` 的「這一包剛好有 1 張新卡」是抽卡隨機性的偶發。

**還沒做**：Codex 複檢這幾輪、沒有併 main、沒出 exe、gh-pages 仍是 09-08 舊版。
使用者朋友問「單抽會不會虧」——實算過**不會**（十連一次與單抽十次都是 10,252，漲價按張數不按次數），
要不要在價牌加「≈ 每抽 X」讓它看得出來，還沒決定。

## 一、狀態一眼看

- 分支 `balance-v3`，worktree `D:\claude研究\clawd-pet-v3`，從 main `276e6fc` 開。**沒有上線、沒有出 exe、沒有動 gh-pages**（使用者：不更新網頁）。
- 程式全部由 Opus 做；素材用 SVG／CSS 佔位（使用者：先用簡單 SVG 排版面）；09-13 晚 Codex 額度恢復，生圖派給它（見三之三）。
- `npm test` **182**；Playwright：五支 v3（`clicker-v3-{gate,prestige,cleanup,team,drag}.py`）＋十一支舊套件全綠。

## 二、規則（都已落地在 `src/`）

| 規則 | 定案來源 |
|---|---|
| 小王每 10 包（門檻包之前）、30 秒、`GATE_MUL 2`×當前包需求、area 第 5 隻 ×1.5；**第一次遇到自動開打，輸過就等玩家點「挑戰」**（冷卻 30 秒） | 使用者 09-12 深夜 |
| **30 秒火力 ≥ 血量 10 倍的小王直接讓路**（給獎金、不演出）——剛換桌布或壓倒性強的時候不會每 10 包看一次動畫 | 09-13 修舊套件時發現的噪音 |
| 大王血量 `BOSS_K[站] × 門檻包需求 × 站係數`，K＝10/8/6/5/8/8/2；每輪可重打；贏過的站門檻減半 | 兩週日曆掃描 |
| C 路獎金：小王 `需求×8`、大王 `門檻需求×係數×40`，只進錢包 | 使用者拍 C |
| 編隊 20、前綴上限 2/6/12/20 **用出身算**；只有隊伍產錢；空隊伍＝自動編隊；**裝技能自動編入**；新夥伴自動入隊 | 使用者拍 (a) |
| 印記＝本輪王勝＋100/300 包＋滅世 +3（≤12/輪）、**總量上限 100**、輪迴條件＝本輪贏過王；**7 條神器線**第 r 級收 r 枚各有頂；招募券下架 | 使用者拍 D |
| 本輪當家 2 張 ×1.5／CD −20%，換桌布重抽 | — |
| 派遣 3 格 4 小時，回收粉塵 1（傳說／神話半顆）＋萬用 1，每日 9 次 | — |
| seal→充能、chaichai→重整；寶箱包 3%（+Lv150 隊員各 1%、寶箱祝福）；點擊附加商店兩項 | — |
| 大掃除 v2→v3：legacy 封存、印記＝12×輪迴次數、祝福買到頂；徽章「舊時代的珍母」＋魔花少女收藏卡 | 使用者 09-12 |
| 成長側 `PARTNER_G 1.02`（夥伴每級再乘 1.02^L）；v2 公式測試把它釘回 1 再量 | 兩週日曆掃描 |
| 路障小王**不鎖招募**（大王演出才鎖） | 抽卡是主爽感 |
| 魔花少女收藏卡：`Pool.CATALOG` kind `collect`、`s.collectibles`，只在大掃除發 | 使用者 09-13 |

## 三、UI 做到哪（都是簡單版）

| 畫面 | 有 | 沒有 |
|---|---|---|
| 舞台 | 路障（用該站大王素材縮小，sprite 逐幀）、挑戰鍵（發光≥90%）、「30 秒火力 ≈ n%」框、獎金橫幅、金色寶箱包＋小標、**夥伴列只列隊伍＋本輪當家旗**、**夥伴列拖到技能槽（`clicker-drag.js`，照 team20 骨架）** | 大王 K 的說明 |
| 換桌布面板 | 本輪帳（王勝／包數）、七條神器線（第一順位）、下一輪更快的說明、路障自動開打開關（電動手指頁） | 神器圖示（都用同一張章） |
| 大掃除頁 | 保留／整理／補償／怎麼算、收下才關、匯入也會看到一次；**魔花少女收藏卡真的發**（`card-mohuashaonv.png` 由禮物卡圖層合成、special 粉框、卡冊最後一頁） | — |
| 卡冊 | 隊伍摘要與上限、隊／派章、本輪當家旗、編入／移出、派遣／收回、收藏卡格 | 卡冊內拖曳（主畫面已有） |

## 三之二、2026-09-13 下午追加

| 項目 | 做法 |
|---|---|
| 大掃除二選一 | 「保留進度」＝重算印記、無補償；「從零開始」（按兩次）＝新存檔、`legacy.reset=true`、魔花少女＋徽章「舊時代的珍母」。收藏卡只屬於末世 |
| 1.0 編隊畫面 | `clicker-team.js`／底部「編隊」：上限列、20 格、候選、點選編入／移出／裝槽、自動編隊；**塞不進去講原因**（哪一層滿、要移出誰） |
| 末世入口殼 | `clicker-apoc.js`／底部「末世地圖」：打贏滅世珍獸 → `apoc.unlocked`、演出後自動開；四分頁 地圖／編隊（`apoc/map20.html` iframe，`team20.setScreen`）／精裝招募（`apoc/gacha.html`）／收藏卡（`apoc/mohuashaonv.html`，只在擁有時載入）；六步新手引導（遮罩＋對話泡）記在 `apoc.tutorial` |
| `src/apoc/` | 三個自包含頁從 holo-5.0 `_art/holo-test/` 複製（map20 11.6MB、deluxe-gacha-b-standalone 16MB、gift 10MB）；`export-web.py` 加 `apoc/**`。⚠ map20 的 `terrainLayout` 補了 0 寬保護；iframe 切換用 visibility 不用 display:none |
| 2.0 經濟 | **還沒接**：末世頁面是展示殼，抽到的卡不進隊、地圖不能推進。接線照 `DESIGN-2026-09-12-content-architecture.md` §八 |

## 三之三、2026-09-13 晚追加（使用者看兩張截圖後的五點）

| 項目 | 做法 |
|---|---|
| 末世＝整框模式，不是懸浮視窗 | `body[data-mode="apoc"]`：`#apoc` 蓋滿 960×640 含頂欄底欄；`src/apoc/index.html` 由 `tools/apoc/build_apoc.py` 從 holo-5.0 的 map20 模板＋`tools/apoc/extra.html` 注入自己的頂欄（末世金幣、末世券、返回 1.0）與底欄（地圖／編隊／精裝招募／卡冊・收藏卡）。postMessage：父→框 `apoc-state`／`apoc-go`，框→父 `apoc-ready`／`apoc-back`／`apoc-tab` |
| 收藏卡位置 | 末世「卡冊・收藏卡」分頁＝team20 編隊收藏畫面最下面加一列 `.team-collect`；點開用原本 three.js 禮物卡頁（`apoc/mohuashaonv.html`） |
| 從零開始 → 獎勵視窗 | `rewardPage()`：卡＋徽章、收下才關 |
| 進度快照 | `legacy.snapshot`（collection／dust／promotions／transcend／partnerLevels／badges／生涯幣／包數／輪迴）；徽章牆「舊時代的相簿」`mementoPage()` 回味 |
| 1.0 編隊＝2.0 同一套邏輯 | `clicker-team.js` 重寫照 team20：累加上限列（格子、受影響／違規）、隊伍網格 10 張一頁、獨立技能格、詳情面板（新增／替換目前成員／移除）、挑選器 `<dialog id="t20-picker">`；**塞不進去逐層講原因**「神話合計已達 2，加入後將為 3（上限 2，用出身算）」。`clicker-drag.js` 通用化 `bind()`，舞台夥伴列與編隊網格共用 |
| 驗收 | `clicker-v3-teamui.py`（含編隊畫面拖曳）、`clicker-v3-apoc.py`；round14/16/17/19/20 五支舊套件在 HEAD 之前就壞（角色數 21／25 等舊基準），不是這輪造成 |
| 生圖 | Codex 額度恢復後派 imagegen：`_art/v3-gen/BRIEF.md`（7 神器圖示＋寶箱包），成品進 `_art/v3-gen/`，尚未接進 `src/` |

## 三之四、2026-09-13 夜（使用者：今天要做到「點頭就上線」）

| 項目 | 做法 |
|---|---|
| 末世整框太擠 | `#apoc iframe` 以 1280 寬畫、`--apoc-scale`＝容器寬/1280（桌機 960→0.75）；<800px 直式手機不縮，交給 map20 自己的手機版面。隊伍網格桌機改 5×148（`tools/apoc/extra.html`，`@media (min-width:800px)`） |
| 精裝招募銜接太慢 | holo-5.0 `ceremony.js`：READ/RETURN 縮到一般卡 ≈660ms 一張（實測 common 0.66–0.72s，同 1.0 流星投遞 580+80）；傳說蓄力 900→700、神話 1300→900；`ceremony-layout.js` 十連卡片下限 190→150、邊界 140→210，「收下」不再壓卡 |
| 5.0 九張卡 | Astra 做完（holo-5.0 `711824b`，`REPORT-5.0-cards.md`）；**七張名字與稀有度是提案待使用者定**；星願龍主體偏小待調；觀星動態版待辦。已收進 `apoc/gacha.html`（72 張） |
| 素材 | Codex 生的 7 神器圖示＋寶箱包已接進 `src/`（`af26fe0`） |
| 上線前狀態 | balance-v3 對 main 是 fast-forward（main 仍在 `276e6fc`）；`npm test` 182、v3 七支全綠；dist-web 101 MiB（含 apoc 三頁）。**舊套件 round14/16/17/19/20 與 `clicker-browser.py` round7 是舊數值基準，v3 改了規則後必然紅，不是回歸**——上線後要重寫基準 |
| 已知未做 | 末世經濟未接（金幣 0、抽到的卡不進隊、地圖不能推進）；上線的是「1.0 v3＋末世展示殼」 |

## 三之五、2026-09-13 深夜：末世關卡串接第一版（能玩了）

| 項目 | 做法 |
|---|---|
| 經濟 | `src/clicker-apoc-economy.js` 純邏輯（`ApocEconomy`）：`s.apoc = {coins, tickets, progress 0–20, cooldownUntil, collection{id:n}, roster[], skills[4], stage, gifted}`。開門禮普發玥玥＋10 券；戰力 100/85/72/60 ×(1+0.25×(張數−1))；血量 3000×1.22^i（王 ×6）；通關獎勵＝血量×0.1；放置金幣＝戰力×0.01／秒；點一下＝戰力×0.5；券 1000；王關 60 秒／失敗冷卻 3 分鐘 |
| 橋 | `clicker-apoc.js`：每秒 `tick()`（1.0 的 tickTimer 叫）；frame→parent `apoc-fight／apoc-tap／apoc-buy-ticket／apoc-drawn／apoc-team`；parent→frame `apoc-state`（view）＋`apoc-win／apoc-fail` |
| 組頁補丁 | `tools/apoc/build_apoc.py`：全卡池 71 張進 team20（index.html 22.6 MB）、產 `src/apoc/pool.js`、team20 加 `setOwned／setRoster／setSkills` 與 `window.onTeamChange`、map20 加 `setCooldown／setProgress／selected`、招募頁加 `apocGacha.setTickets` 與抽到回報 `window.top` |
| HUD | `tools/apoc/extra.html`：地圖右欄戰鬥區（血條／開戰／攻擊！／王關倒數／冷卻）、頂欄「換券 1000」、收藏計數 |
| 數字 | **全部是第一版猜的**（模擬器還沒跑）：普發玥玥一張 60 戰力打第 1 站 3000 血約 50 秒放置；十連後約 700 戰力。第 20 站 780k 血。要調就改 `ApocEconomy.RULES` |
| 沒做 | 王關機制（護盾／召喚／節奏）——目前王只是血多＋限時；獨立技能格沒有效果（只記錄）；末世星數不顯示在卡面；觀星動態卡 |
| 驗收 | `node --test`（189）、`clicker-v3-apoc-play.py`（端到端） |

## 四、模擬器

`node tools/sim/clicker-v3.js 4 20 14`；環境變數覆寫 `B.V3` 任何鍵、`BOSS_K=10,8,6,5,8,8,2`、`CITY_MUL`、`NO_PRESTIGE=1`。
真人模型：一天三段 20 分鐘＋離線；只有卡住才換桌布；派遣下線前派 3 張。
現況形狀：第 1 天前四站、夜市第 1 天晚上、冰箱第 2～3 天、滅世第 10 天、6 次輪迴、14 天 5★ 35/52。
⚠ 兩個儀器教訓：模擬器換場景清單漏了 `city` 曾讓「滅世永遠過不了」；`PARTNER_G` 一個小數點＝一天與一週。

## 五、接下來

1. 素材：神器圖示、寶箱包（**生圖是唯一不由 Opus 做的**）；小王與魔花少女已用現成素材。
2. 大王 K 遞減與滅世係數要不要再調，等使用者實玩。
3. 併回 main 前**單獨問**；併之前把 `docs/clicker` 兩邊同步。

## 六、驗收

```
npm test                                            # 182
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-gate.py       # 路障、挑戰鍵、火力預估、獎金（含 60 秒真實等待）
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-prestige.py   # 換桌布面板、神器
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-cleanup.py    # 大掃除頁（v2 存檔）
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-team.py       # 卡冊編隊、派遣
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-drag.py       # 夥伴列拖到技能槽
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-teamui.py     # 編隊畫面、塞不進去的原因
PYTHONIOENCODING=utf-8 python tools/test/clicker-v3-apoc.py       # 末世入口、新手引導、四分頁
＋ NEXT-SESSION-KICKOFF.md 的十一支舊套件
```

## 七、本機試玩（不是網頁版，gh-pages 沒動）

```
cd D:\claude研究\clawd-pet-v3
python tools/export-web.py            # → dist-web/（360 檔）
python -m http.server 8123 --bind 127.0.0.1 --directory dist-web
```
開 http://127.0.0.1:8123/ 。localhost 是獨立存檔（跟 speshotdog.github.io 不通），要試大掃除就在網頁版徽章牆「匯出存檔」、在本機「匯入存檔」。
收藏卡：卡冊頂端「收藏卡」鍵（第一版只放卡與名字；正式分頁另做）。
