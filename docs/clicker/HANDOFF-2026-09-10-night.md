# 交接：2.0 末世地圖 ＋ 編隊（2026-09-10 夜）

分支 `holo-5.0`（worktree `D:\claude\clawd-pet-50`）。**接手先讀這一份，再讀下面點名的文件。**

---

## 一、現在跑得起來的東西

**`_art/holo-test/map20.html`**——單檔自包含，雙擊就開。裡面有兩個畫面：

| 畫面 | 狀態 |
|---|---|
| **地圖** | 五段 20 關、線性、廢墟地景（第一段是真的美術，二～五段用同一張當佔位並有標示）、節點四態、王節點閘口、王關 3 分鐘冷卻 |
| **編隊** | 20/20、四條累加上限、真正的卡面（1440 卡寬 180px）、詳情卡、技能四格（只有殼）|

URL 鉤子：`?progress=N`（0～20）、`?cooldown=SEC`、`?motion=off`、`?ui=off`、`?terrain=off`、`?wear=off`。
編隊入口是畫面上的「編隊」按鈕（`#open-team`）。

驗收腳本：`check_map20.py`、`check_team20.py`、`check_card_corners.py`、
`check_card_feel.py`、`check_card_feel_clearance.py`、`run_card_feel_regression.py`。

---

## 二、⚠ 今天修好的大東西：整條線的字型從來沒生效過

**`docs/clicker/VERIFY-empty-subset-font.md`**。

`build_round4_fonts.py` 少呼叫 `Subsetter.populate()`，產出 **996 bytes 的空字型**，
瀏覽器報 error 之後**安靜退回系統 fallback**。
`demo.html` 是所有卡面產物的來源，所以**卡面頁、抽卡系統、編隊畫面每一張卡的卡名都是 fallback**。

已修：996 → **233,748**（Sans）／984 → **317,200**（Serif），三頁 `fonts.check` 全 true。
還有一層是 codex 找到的：**建置器靠替換 marker 注入字型，marker 第一次建置後就沒了**，
所以光補 `populate()` 重跑也不會更新內嵌字型，重建流程一起修了。

**回家第一件事建議：打開 `docs/clicker/shots/card-feel/detail-type-pair.png` 看一眼**，
確認新字型是你要的「一樣的感覺」。不是的話現在改最便宜。

---

## 三、還沒做完的四件事（照這個順序）

### 1. hover 光影：**我的門檻訂錯了，要重訂**（規模小）

我在簡報訂「亮區質心左右位移 ≥12% 卡寬」，並註明是推的、沒量過。
codex 照規矩去量基準，結果：

| 狀態 | 左右位移 |
|---|---:|
| **修改前的詳情卡**（＝你說「一樣的感覺」的那個參考）| **3.754%** |
| 修改前總覽 | 0%（本來就沒有）|
| **修改後總覽** | **6.198%** |

**連參考實作自己都只有 3.754%，所以 12% 是無效門檻**
（`METHOD-card-face.md` 第二節：修正前也不及格 → 是門檻的問題不是實作的問題）。

**重訂**：照工法「先量一個明顯合格的樣本，往下留餘裕當下限」——
參考值 3.754%，**下限訂 3.0%（留約 20% 餘裕）、上限 45% 不變**。
按這條，總覽的 6.198% 是**通過**，而且是參考值的 1.65 倍。

⚠ 另一條 `hover vs 非 hover 像素差 3～25%` 也要重訂：實測 90%，
**因為截圖含既有的傾斜效果，沒有把傾斜造成的差異扣掉**。
要嘛量的時候鎖住傾斜，要嘛改量「只在箔面層」的差異。

### 2. 放大後的裁切：**真的有問題**（規模小）

1.04 倍逐一選取兩頁 20 張卡、捲入可見區之後的最差裁切：

| 視窗寬 | 1.04 倍 | 1.06 倍 |
|---|---:|---:|
| 1440 | **0%** | 0.584% |
| 1024 | **1.251%** | 2.173% |
| 390 | **0.936%** | 1.260% |

門檻是 0%。**1024 與 390 要修**——縮欄距或縮卡寬都可以，
**但不准取消放大**（那是你指名要的）。

### 3. 六個過期的回歸守衛要退役（規模小）

`run_card_feel_regression.py` 跑舊腳本得到 1098 PASS / 6 FAIL，**六個全是過期守衛**：

1. 舊保護清單把 `demo.html` 整檔凍結 → 本輪授權改字型 base64，要把它改成「除字型 base64 外凍結」
2. `overview_pointer_unchanged`（要求總覽 hover 不變）→ 跟本輪新增的 hover 直接衝突
3. `proxy_animations` / `automatic_animations` ×4 → 舊腳本把**指標／選取觸發的 scale transition** 也算成動畫；
   獨立的閒置 paint 檢查是 0

⚠ **退役要留下清單與理由**，不要靜靜刪掉。這條線已經退役過兩批門檻，
兩次都是因為畫面語言換了（工作盤→卡牌），不是因為做不到。

### 4. 黑角判準已收緊，但**抽卡演出途中還沒掃**

`docs/clicker/VERIFY-black-corner.md`。判準從 8%／12 收緊成 **5%／6**
（編隊注入後最暗已到 0 但佔比 7.639%，騎在 8% 門檻下緣沒變紅）。

三個畫面**現況都乾淨**、負控制都能變紅。**但只掃了靜態畫面**：
`.charge-surface`（`mix-blend-mode:screen`）與 `ceremony-canvas` 各層是在**揭卡演出途中**才開的，
**那些狀態沒掃過**。禮物卡那次的黑角就是特定 FX 狀態才出現的。

⚠ 負控制**必須注進 shadow root**（編隊的卡在 11 個 shadow host 裡），
注在頁面層會「注了等於沒注」——我踩過，差點把它當成「沒問題」。

---

## 四、完全沒開始的兩件（原任務清單的第三、四件）

- **5.0 新卡**：素材在 `C:\Users\ASUS User VII\Desktop\新卡\5.0`，三個檔要產八張卡。
  待決三件事寫在 `KICKOFF-5.0-window.md` 任務一（系列卡背景要不要一致、
  觀星系列是靜幀還動態卡、稀有度與命名）。
- **變種卡**：先研究、做一張樣品就好。規格待決寫在 `KICKOFF-5.0-window.md` 任務二。

---

## 五、設計決策在哪（不要重新發明）

| 文件 | 內容 |
|---|---|
| `DESIGN-team-and-map.md` 第七節 | 使用者對四個問題的裁決：視覺語言 C／階級分工加流派層／放置點擊·機制只在王關／失敗靠牆不靠罰＋王關 3 分鐘冷卻 |
| `DESIGN-2.0-art-direction.md` | **第一輪為什麼變成線框圖**（我把地景砍出範圍）＋**第六節：編隊為什麼改用卡牌語言** |
| `PLAN-2.0-levels.md` | 五段五王、機制配素材（用素材的缺點當設計）、預告關、進度規則 |
| `PLAN-2.0-visual-C.md` | 工作盤語言規格、卡片承載三招、退回 B 的三道門 |
| `PLAN-2.0-map-ui.md` | 第一輪 UI 討論結論 |
| `METHOD-card-face.md` | **卡面製作工法（使用者交的）**，驗收方法論的最高權威 |

### 一句要記住的

**C 沒有被推翻，被推翻的是「C 佔滿畫面」。**
第一輪地圖沒有地景時是線框圖，編隊沒有地景時是綠色表格——同一個病，兩次發作。
畫面要有主體：地圖的主體是廢墟地景，編隊的主體是卡。

---

## 六、這個視窗踩到、值得記住的坑

1. **指標過了不等於東西對了**（三次）：
   承載槽開/關只差 0.954% 卻全綠（門檻只量「開」那張圖自己的變化，沒比較開 vs 關）；
   `route.walked_alpha 0.72 PASS` 但路線在地景上看不見（alpha 在純色底≈對比，在插畫底毫無意義）；
   第一輪地景占比 0%（門檻表裡有下限沒人量）。
2. **門檻沒先量基準就是無效門檻**：hover 的 12% 連參考實作都達不到。
3. **負控制注不出紅燈時，先懷疑注錯地方**：`.foil-stack` 被 `.leaf` 圓角剪掉、
   `.hcard` 不是獨立合成層、編隊的卡在 shadow root——三次都是注錯位置。
4. **驗證腳本說「全部都壞」時，先懷疑判準**：黑角第一版報 28/32 全壞，
   是取樣落到截圖視窗外的純黑。
5. **不要拿 `tokens used` 當 codex 的結束判斷**：我的簡報紀律條文裡就有這五個字，
   codex 回顯簡報就誤觸，害我把中途 log 當成最終回報。**改用「log 停止成長 120 秒」。**
6. **`git show` 是 LF、工作目錄是 CRLF**：拿位元組比對 `demo.html` 會得到假的 False，
   要遮掉字型 base64 之後**逐行**比。
