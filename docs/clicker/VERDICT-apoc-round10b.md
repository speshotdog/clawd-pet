**必修**

1. **刷怪時仍提示「一站要打 5 隻」，與實際規則不符。**  
   HEAD `src/clicker-apoc-ui.js:311` 的第二步只判斷 `!a.stage.boss`，因此也涵蓋 `farm`；但刷怪固定一隻，打死就結算。
   
   重現步驟：
   - 引導只按過一次「知道了」，保留 `tutorial = 1`。
   - 推進到第 4 站，讓灰狼犬逾時失敗。
   - 自動返回第 3 站刷怪時，紙片顯示「一站要打 5 隻」。
   - 擊殺一隻便收到刷怪獎勵、進入重生空檔。

   已執行 HEAD 邏輯確認：`farm=true`、`waves=1`，該提示條件仍為真。建議文案改為「一般關一站要打 5 隻」，明確區別刷怪。

**值得修**

- HEAD `src/clicker-apoc-ui.js:370`：預告期間每次 render 都重新指定相同的 `label.textContent`，會反覆替換文字節點。可比照 `coach()`，文字不同時才更新；這是可省掉的 DOM 更新，未實測效能影響。

**可以不修**

- 「知道了」與 `#tap` 是不同分支的節點，按鈕也阻止 `click` 冒泡；未發現正常點按會額外打怪。
- `coach()` 只建立一次 DOM，文案已有差異檢查；`[hidden]` 有 `!important`，不會被 `display:flex` 蓋過。
- 地圖會隱藏舞台祖先；招募／開包有顯示條件阻擋；結局進度已超過引導範圍。未發現引導主動搶焦點。
- 預告轉王關會移除 `omen` 並重設王關樣式；刷怪與 `farmGap` 都不顯示預告。隱藏時殘留的文字不構成可見錯誤。
- `tutorial` 壞值測試均回到合法的 0～4；缺少的 `stats` 欄位會補零。

本次僅檢查 `git diff 5761a51 HEAD`，檔案內容全由 `git show HEAD:路徑` 取得；HEAD 經濟測試 **36/36 通過**。唯讀環境使 Playwright 無法建立暫存目錄，**390×844／1280×860 的實際排版尚未驗證**。