# 第十六輪實作報告

日期：2026-09-07；範圍：`BRIEF-astra-impl-round16.md` 第 1、2.1～2.7、3 章。

- **1 移除誤植角色**：刪除 `yuelegend` 的卡池、技能與專用借圖接線，角色總數改為 21，舊存檔靜默清除其養成、技能槽、冷卻與效果殘留，既有案例改用阿漉或原版玥玥。
- **2.1 抽樣層**：傳說以同一個 rng 的 30% 判定附加 item 級 `veil:'rare'`，提供 `Pool.shownRarity()`，不改 entry、基礎權重、神話或軟硬保底。
- **2.2 翻前偽裝**：共用卡片接受 veil 並使用 `r-rare veiled`、精良文字與 hover／微粒／LIVE_CFG，角色立繪與名字保留真身，轉色才換成傳說。
- **2.3 共用轉彩演出**：五模式的轉彩卡均接入 runtime reveal，在精良翻面完成後停留 500ms、兩次左右微抖（每拍 80ms）與高頻 sine tick、420ms 斜掃（210ms 換色）、8 顆橘火花，再播壓暗／射線／震動／傳說音效與粒子並停留 900ms，reduced motion 直接換色且略過微抖與掃帶。
- **2.4 模式對齊**：wish 啟用表面最高色階預告與同色落地漣漪、stage 保留原走位與收卡並於轉色同步 300ms 橘色燈光、summon 腳印依表面色階上色、hearthstone／rip 的卡背與 hover 沿用精良偽裝，stage／rip 也能全部翻開且手動 wish 五連不再只翻一張。
- **2.5 遊戲與重開**：付費、王包／每日包／開局免費抽沿用共用 draw，pending JSON 往返保留 veil 並拒絕非法值，重開沿用原本直接顯示摘要的行為且還原真實框色，收下與粉塵等結算不受 veil 影響。
- **2.6 卡冊箭頭**：左右翻頁鍵改為中線兩端 52×52 奶油貼紙與粗體 `‹`／`›`，hover 上浮 2px、disabled 透明度 .3，左右 grid 各增加 44px 內距，保留既有首尾 disabled、點空白翻頁與頁碼位置，故無須改動 `clicker-album.js` 的翻頁邏輯。
- **2.7 神話視覺修正**：星列改為 fit-content，兩套卡皮的 .35 彩虹鍍膜用 mask 排除角色畫窗，框、底與名牌仍維持全彩。
- **3 測試**：新增 `tools/test/clicker-round16.test.js` 七項測試，涵蓋固定 seed 2000 次基礎抽樣、30% 邊界、保底、付費／免費與 pending 驗證、誤植角色遷移、虛擬時間翻牌／取消／reduced motion，以及手動五連完整轉彩。
- **機率口徑**：2000 次統計每抽從 pity=0 開始驗證基礎 5%，另測軟硬保底重置，避免把保底提高的長期比例混入基礎機率。
- **驗證結果**：最終 `npm.cmd test` exit code 0，129 tests、129 pass、0 fail、0 cancelled、0 skipped，修改的 JavaScript 與新增測試通過 `node --check`，`git diff --check` 無空白錯誤，`src/*.js` 的 `??` 命中皆為合法空值運算子。
- **驗收限制**：未進行瀏覽器實機視覺與音效驗收，掃帶觀感、箭頭實際排版與神話混色仍需在既有可運行環境確認。
- **操作限制**：未 build、未啟動 server、未 commit、未更動素材檔。
