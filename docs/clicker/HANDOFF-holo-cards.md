# 交接：華麗卡牌（3D 鐳射／全息）＋ 精裝版抽卡

最後更新：2026-09-08 晚間。分支 `holo-cards`，已 push 到 origin。

## 〇、接手第一步

```bash
git fetch
git worktree list                     # holo-cards 有自己的 worktree，不要在 main 上做
```

⚠ **這個專案有兩條線同時在跑**：`main` 是遊戲本體（另一個 session 在開發第三十輪、商店改版等），
`holo-cards` 是卡牌研發。**兩邊各自有 worktree，不要混用**：

| 分支 | 路徑（本機） | 內容 |
|---|---|---|
| `main` | `D:\claude\clawd-pet` | 遊戲本體，別人在動 |
| `holo-cards` | `D:\claude\clawd-pet-holo` | 卡牌研發，本文件講的東西 |

（2026-09-08 曾經兩個 session 共用同一個工作區，導致對方把卡牌檔案掃進 main 的 commit。
已用 `git worktree` 拆開，main 上的卡牌檔案也移除了。**不要再在同一個資料夾切分支。**）

**打開展示頁**：`_art/holo-test/demo-standalone.html` 雙擊即可（圖片全內嵌，3.07 MB）。
開發時改 `demo.html`（相對路徑版），改完要重建 standalone。
線上版（發給使用者看的）：https://claude.ai/code/artifact/5c104d2b-adff-438a-9a56-c38719bb971e

## 一、這條線在做什麼

兩件事，目前**只做到展示頁（樣品）階段，還沒有接進遊戲**：

1. **華麗卡牌**：景深＋全息箔面的卡面。對標 `poke-holo.simey.me`。
2. **精裝版抽卡**：獨立卡池＋第六種抽卡演出（設計文件已完成，尚未實作）。

**`src/` 完全沒有被修改過。** 所有東西都在 `_art/holo-test/` 與 `docs/clicker/`。

## 二、文件地圖

| 檔 | 內容 |
|---|---|
| `docs/clicker/DESIGN-holo-cards.md` | 卡牌設計主文件（原理、方案評估、授權、效能預算、分輪計畫） |
| `docs/clicker/DESIGN-deluxe-gacha.md` | 精裝版抽卡設計（含第 10 節的使用者定案） |
| `docs/clicker/BRIEF-holo-cards-research.md` | 第一份派工簡報（含使用者最初的定案） |
| `docs/clicker/BRIEF-holo-round1~8.md` | 各輪派工簡報，每輪開頭都有使用者原話 |
| `docs/clicker/BRIEF-deluxe-gacha-research.md` | 精裝抽卡的研究派工 |
| `_art/holo-test/README.md` | 展示頁的操作說明、素材產製、授權結論、還差什麼 |
| `_art/holo-test/demo.html` | 展示頁（開發版，相對路徑） |
| `_art/holo-test/demo-standalone.html` | 展示頁（自包含，圖片內嵌，拿去分享用） |
| `_art/holo-test/check_demo_round*.py` | 各輪的 Playwright 驗證 |
| `_art/holo-test/shots/` | 各輪截圖（`r2-`～`r8-` 前綴） |

## 三、卡面：已定案的規格

### 三種卡型（資料驅動 `art.kind`）

| 卡型 | 條件 | 做法 |
|---|---|---|
| `depth` 滿版景深卡 | 主體與背景拆得開 | subject／background 兩層沿 Z 推開做視差 |
| `flat` 平面滿版卡 | 有背景但拆不出前後關係 | 整張平鋪、**不做圖內視差**；景深只發生在卡片本身傾斜，箔面反光光暈吃在卡面 |
| `framed` 去背卡框卡 | 單一角色去背 PNG | **維持舊版樣式不更新**（使用者定案，當成與精裝版的識別） |

### Z 順序（由後到前）

`圖繪背景` → `文字框` → `拆出來的角色` → `名字文字`

- 角色**會蓋過文字框的上緣**，這個「被蓋住」就是要的前後關係，不要修掉。
- **名字永遠最上層**，連角色都不能蓋住它。
- `flat` 卡沒有 subject 層，只有三層。

### 其他定案

- **字體**：`Noto Sans TC`（子集化內嵌 data URL，SIL OFL）。
  傳說／神話用**浮雕燙金**；`common`／`rare`／`epic` **維持乾淨白色粗體，不要任何字效**。
  被否決的方向：系統字、Noto 宋、遊戲金屬、像素疊描、斜體分層。
- **寶石**：照抄 `src/gacha-card.css` `.face-gem` 原版形狀，只做微調
  （配色、邊光、極輕微角度反光）。**不要立體、不要改形狀**。位置在**文字框左側**。
- **名字與稀有度置中**：基準是**卡片本身**，不是扣掉寶石之後的剩餘空間。
- **史詩要明確的紫**，跟神話拉開；**傳說與神話的框要奢華**（雙層框、角落裝飾）。
- **珍珍JPG 的透明格子是美術梗**，不是破圖，保留。

### 已否決的路線（不要再走）

- **合成滿版背景**（角色 ＋ `clicker-scene<N>` 場景層）——使用者說「都不好看」。
  `compose_legacy.py` 與 `layer-*` 檔留著當紀錄，展示頁不展示。
- **硬拆滅世珍獸**——城市與角色同色調拆不開，強拆會破壞原本的構圖。改走 `flat`。
- **不透明銘牌橫幅蓋在圖上**——第七輪拆掉了；第八輪回來的是「有前後關係的文字框」，不是黑條。

## 四、驗證（這條線最重要的資產）

`check_demo_round8.py` 是目前最完整的一份，跑法：

```bash
PYTHONIOENCODING=utf-8 python _art/holo-test/check_demo_round8.py
```

它會同時驗 `demo.html` 與「**複製到別的資料夾的 standalone**」，涵蓋：

- 名字／稀有度／寶石**置中偏差 <2px**
- 名字**不被角色或框蓋住**（`elementFromPoint` 取樣）
- 名字↔寶石／銘牌邊／卡框的**最小間距**
- **寶石對比度 ≥35**（量寶石區域與周圍背景的亮度色差）
- 無外部請求、無 console 錯誤、游標離開後 `rAF = 0`

⚠ **這些斷言是踩過坑才長出來的，不要為了讓新功能過而放寬門檻。**
每一條都對應一次「規格照做但視覺失敗」的實際事故（寶石連續三輪看不見、
名字被圖蓋住、名字被寶石推歪）。

## 五、踩過的坑（會再踩到的）

1. **搜原始碼不等於驗畫面。** 展示頁一度把中文卡名存成 `String.fromCodePoint(0x…)`，
   所以 grep「火箭狗」查不到，我回報「已改名」是假通過——畫面上還是舊名。
   **驗名字一律讀實際渲染出來的文字。**
2. **卡名不要從檔名推。** 曾經把 `foxfriend` 寫成「狐狸朋友」，
   實際遊戲裡叫「摯友之狐」。正名一律查 `src/gacha-pool.js`。
   （已修正四個：摯友之狐／咩噗噗噗／黃色蠑螈／羊咩噗）
3. **GrabCut 拆出來的邊會有色溢與暈邊。** 修法是輪廓內收 2px ＋ 窄環形 NS inpaint ＋ 0.8px 羽化。
   但更根本的解法是**別拆**（見 `flat` 卡型）。
4. **`file://` 不能用 `fetch`／XHR／ES module import**，CSS mask 也吃 CORS，
   所以 mask 與字體都得內嵌成 data URL。
5. **分享單檔要壓縮**：原始 PNG 合計 28 MB，base64 後 38 MB，塞不進網頁。
   背景轉 WebP／JPEG、主體用 WebP with alpha，壓到 3 MB 左右。
6. **授權**：`pokemon-cards-css` 是 **GPL-3.0**，只能學原理不能抄程式與貼圖；
   `holo-card-studio` 是 MIT 但明文排除生成的圖片；
   Galaxy Holo／Vecteezy **授權未確認，不採用**。
   本專案的箔紋、粒紋、框 mask 全部程式生成（固定 seed `20260908`）。

## 六、精裝版抽卡：已定案與待定

詳見 `DESIGN-deluxe-gacha.md` 第 9～10 節。摘要：

**已定案**
- 獨立卡池，**每張都是精美版**；**沒有保底**。
- 新增**第六種**抽卡演出「**裝幀典藏**」——玩家逐層完成一件典藏品（編目、裝幀、壓印、揭曉），
  跟既有五種「外部事件把卡送過來」本質不同。**既有五種一個都不准改。**
- 卡冊有**兩個版本可切換**：一般 / 精裝。
- **末世地圖**：打完最後一關 BOSS 才開啟，**只能用精裝卡戰鬥**，與舊地圖**戰力分開、資源分開**。
- 末世抽獎券用末世金幣買；**舊金幣也可以換**（比值待定），不讓舊資源沒用。
- 精裝卡**含舊角色**（但卡面是新的精裝版），以後會有精裝專屬新角色。
- 末世第一張圖**點 50 次送「普發月月」**（素材待補）。

**待使用者定案**（不要自己發明數字）
- 價格、抽率、舊金幣換券的比值
- 精美版重複怎麼處理
- 「普發月月」的稀有度、能否重複、算不算卡冊完成度、50 次是累計還是單場
- 精裝卡是同一批角色的精美版，還是末世專屬新角色（影響戰鬥平衡）
- 兩個區域的進度、場景、王、印記是否各自獨立

**技術上最容易做錯的一件事**（設計文件第 3.1 節）：
同一角色的普通版與精美版共存，要把 `characterId`（玩法）與 `cardId` + `edition`（外觀）拆成兩層，
**不可以用「這次從精裝池抽的所以套 deluxe 樣式」當唯一依據**——
pending、重開、匯入、跳過都要能還原。

## 六之二、第九輪（2026-09-08 深夜，使用者定案）

### 卡名字效改成依稀有度固定

| 稀有度 | 字效 |
|---|---|
| common / rare / epic | 乾淨白色粗體（不變） |
| legendary | **實心亮金 `#fff45c` ＋自體光暈** |
| mythic | **conic 彩虹**，用卡框同一條漸層與同一個 `--phase`，傾斜時跟著框一起轉 |

第六輪的「浮雕燙金漸層」對兩階都用，實測有兩個問題，都在 `round9-name-ink` 這個 block 裡蓋掉了：

1. 展示頁後段有一行 `.r-legendary .face-name,.r-mythic .face-name{text-shadow:…var(--accent-card)}`
   把字的外光接成稀有度主色。神話的 `--accent-card` 是**薄荷綠 `#93f0df`**，配暖金字混成橄欖色；
   傳說因為主色本來就是暖金，所以同一個 bug 看不出來。
2. 燙金漸層的 78% 是 `#b36a1f` 深棕。卡一縮小（字級掉到 15px 上下）那段就佔滿字身，
   整個名字變暗塊。改成實心＋光暈之後任何尺寸都亮。

⚠ 神話用 `background-clip:text`，所以**黑邊要用 `drop-shadow` 不能用 `text-shadow`**——
`text-shadow` 會把陰影畫在漸層的外面而不是貼著字形。

### 全卡池精裝版（展示頁 E 區）

使用者定案：**有背景的才走滿版景深／平面，一般去背卡就套精裝卡框**
（這條**取代**第三節「`framed` 維持舊版樣式不更新」的舊定案）。

- 卡池 57 張裡**有美術素材的 43 張全部出卡**（40 張 `card-*.png` ＋ 3 張 `toy-*.png`），依稀有度排序。
- 資料是**建置時直接從 `src/gacha-pool.js` 的 CATALOG 讀出來**寫進展示頁，不是手打，
  也不是從檔名推——這條是第五節第 2 點那次事故的直接對策。
- **還缺美術素材的 14 張**：珍母、膠布（原版）、玥玥（原版）、珍珍（原版）、熱狗狗狗、女僕狐狐、
  膠布、珍珍、玥玥、采華、ㄌㄎ、羊咩、熱狗、愛心。
  前 12 張在遊戲裡是**用身體零件即時組出來的角色**（沒有卡圖檔），後 2 張是 emoji。
  要讓它們也有精裝卡面，得先決定：截圖合成，還是另外畫卡圖。

### 一個技術改動：mask 的鍵改成裸檔名

`embed_masks.py` 以前把鍵存成 `../../src/card-x.png`，`build_round5_standalone.py` 再想辦法改寫成裸檔名。
43 張卡進來之後這條路開始打結，所以統一成**兩邊都用裸檔名**（`card-x.png`／`toy-x.png`／`layer-x-subject.png`）。
`makeCard` 也跟著改：`srcKey` 是裸檔名，`src` 才組路徑，並支援 `data.file`（玩具是 `toy-*.png` 不是 `card-*.png`）。

### 驗證

`check_demo_round9.py`（新）——除了沿用的無外部請求／無 console error，另外釘死：

- 65 張卡（22 張樣品 ＋ 43 張卡池）
- **卡名與稀有度逐張比對 `src/gacha-pool.js`**，名字錯一個字就紅
- 每張卡池卡的圖有載入、有 foil mask、名字置中偏差 < 2px
- **傳說的 `-webkit-text-fill-color` 必須是 `rgb(255,244,92)` 且 `background-image:none`；
  神話必須是透明填色＋`conic-gradient`；common/rare/epic 不准有任何 gradient**

`check_demo_round8.py` 的卡片數斷言從 22 改成 65，其餘未動，仍然全綠。

## 六之三、第十輪（2026-09-08 深夜，使用者定案）

### 舊卡不升級

**遊戲裡現有的卡面不動**；精裝版是「同一張卡的另一個版本」，不是把舊卡換掉。
展示頁 E 區那 43 張是「這些卡的精裝版長什麼樣」，不是要取代線上的卡面。

### 精裝版只有一種版型

以天外膠膠那張為標準版型，**每一張都必須有**：階級寶石 ＋ 文字框 ＋ 名字 ＋ 稀有度。
**不接受沒有文字框的卡面。**

- 有前後關係的圖 → 比照宇宙冒險羊做 `depth`。
- **沒有背景的卡也是同一個版型**，人物置中（`kind-framed` 的 `.art-media` 改成 grid 置中，
  圖最大 78% 寬 / 70% 高）。
- **字級不再吃 `vw`**：ResizeObserver 把名字算成卡片寬度的固定比例
  （基準 24px / 290px 卡寬＝8.28%，稀有度 9px / 290px），所以同一張卡在場景區、
  卡池區、稀有度區都是同一個比例。宇宙冒險羊之前是 28.7px、天外膠膠是 24px，
  就是 vw 規則造成的不一致。

### 四張場景卡改名與改階

| id | 舊名 | 新名 | 稀有度 | 美術 |
|---|---|---|---|---|
| `rocketdog` | 宇宙冒險羊 | 宇宙冒險羊 | legendary → **mythic** | 不變 |
| `astronaut` | 太空人狗 | **玥面探索者** | rare → **epic** | 特寫重裁 |
| `alienkitty` | 粉紅外星貓 | **天外膠膠** | mythic → **legendary** | 特寫重裁，貓與行星同為主體 |
| `fluffdog` | 白色捲毛狗 | **居家珍獸** | epic（不變） | 不變 |

重裁在 `recrop_round10.py`：兩層用同一個裁切框（視差才不會歪），維持 5:7 再取樣回 600×840，
原圖從 git（`d540b5f`）讀出來所以重跑不會疊裁。**這是重新裁切，不是重畫**——
若要真的重新構圖（例如把行星整顆畫進來）得走 imagegen，尚未做。

### 卡背重做

照 `src/gacha-cardback.jpg` 的熱狗卡背重畫再加工：深藍底、雙層奶油描邊、圓形獎章、
四角圖示（爪／骨／星／心）、`珍母點點` ＋ `DELUXE EDITION` 壓印字，外圈多一道燙金環。
全部 CSS，沒有外部素材。

⚠ **卡背裡不能用 CSS mask。** 實測（三種寫法都試過）：`.card-back` 是
`rotateY(180deg)` ＋ `backface-visibility:hidden`，在 Chromium 裡對它的 `::after` 上 mask
會讓整個 pseudo-element 消失（不是變淡，是完全不畫）。燙金環改成純幾何：
一個 conic 圓盤 `.back-ring`，上面疊一個不透明的 `.back-medallion`，中間被蓋掉就成環。

## 七、下一步

1. **等使用者確認**：`framed` 卡的「舊版樣式」是指遊戲現在線上的版本，
   還是展示頁那個「卡框＋稀有度色調底」的版型？兩種讀法工作量差很多。
2. 另外三張場景卡的名字還是暫定名（太空人狗、粉紅外星貓、白色捲毛狗），等使用者指定。
3. 卡面樣品若通過驗收，才進入「接進遊戲」的階段（`DESIGN-holo-cards.md` 第 10 節的分輪計畫）。
4. 精裝版抽卡等商業規則定案後才能接經濟層。
