# 任務：突破上限跟輪迴圈數綁（讓「為了抽卡一直玩」成立）

背景（HANDOFF §31）：輪迴倍率已拉高（血每圈 ×1.3、上限 20 圈、每圈送 10 券），但重度玩家第 4～5 天就滿養（5★＋突破 5 級），之後十幾圈是純訓練，跟抽卡無關。
使用者 2026-09-16 拍板：**突破上限跟圈數走**——圈數是難度階梯、突破是攻略階梯，兩條互相咬住；粉塵只從抽卡／王首勝／寶箱來，想打更高的圈就得抽。
你只做實作與模擬，不 commit；改完留在工作區給我驗。

## 一、規則（照做，不要自己改數字；要調只能調 §三授權的那幾個）

### 1. 突破 5 級 → 10 級，第 6 級起跟圈數綁
- `RULES.GROW.TRANSCEND: [2, 2, 3, 3, 4]` → `[2, 2, 3, 3, 4, 8, 10, 12, 15, 20]`（累計：前五級 14、全部 79；加上 8 顆滿星＝87）。
- 第 6～10 級各有**解鎖圈數**：`RULES.GROW.TRANSCEND_LAP: [0, 0, 0, 0, 0, 2, 4, 6, 8, 10]`（第 k 級要 `lapsOf(a) >= TRANSCEND_LAP[k-1]`）。沒到圈數的等級：`canTranscend` 為 false、`transcendCost` 照回數字（畫面要寫「第 N 圈解鎖」）。
- 戰力：每突破一級 +10%（`G().TRANSCEND_MUL` 不變）→ 10 級 ×2.0（原 ×1.5）。**這是抽卡的回報，不要另外加。**
- 自動突破（`autoGrow`）與粉塵罐（`exchangeDust`）照新表走，被圈數鎖住的等級不自動買。

### 2. 「滿養」要分兩種，舊的目標函數不能動
- **兩週滿養**（現有徽章／印記 `MARKS.MAXED: 10`／`isMaxed`／卡冊「滿養 N/71」／`fullDust()`）：定義**不變**＝5★＋突破 5 級＝22 顆。所有現在讀 `isMaxed`／`fullDust` 的地方語意不變。
- **輪迴滿養**（新）：`isFullyMaxed(a,id)`＝突破 10 級（87 顆）；`fullDustLoop()`。新徽章「輪迴滿養」（全 71 張到 10 級，`badges` 加一條，**不給印記**）。
- 卡冊詳情的成長列：5 級之前照舊；5 級之後寫「突破 6／10・第 2 圈解鎖・要 8 顆」；到圈數了才出現按鈕。卡冊頂的「滿養 N/71」照舊算 5 級，旁邊加小字「輪迴滿養 M/71」（只在 laps ≥ 2 時顯示）。
- `starsAt`／`dustOf`／`availableDust`／`spentDust` 的算法不變（突破 6～10 級的粉塵照樣從 dust 池扣）。溢出（超過 87 顆）照現在的規則折成萬用粉塵。

### 3. 舊存檔
- `transcend[id]` 現在最多 5，normalize 照 `TRANSCEND.length`（10）夾就好；舊檔「滿養」徽章與印記 `marksGiven.maxed` 不動、不重發、不收回。
- 沒有圈數的存檔（laps 0）第 6 級以上一律鎖著。

### 4. 推薦組合／技能／輪迴
不動。

## 二、畫面
- 卡冊詳情（`clicker-album.js` 末世分支）：突破列改成上述文字；被圈數鎖住時鍵不顯示、只顯示「第 N 圈解鎖」。
- 卡冊頂列「滿養 N/71」旁的「輪迴滿養 M/71」小字（laps ≥ 2 才出現）。
- 全線通行三張卡的「輪迴」那張多一行：「下一圈解鎖：突破第 6 級」（只在下一圈剛好是某級的解鎖圈時寫）。
- 圈末結算 `#lap-done` 若這圈解鎖了新的突破等級，多一行「突破上限 → 第 N 級」。
- 徽章牆加「輪迴滿養」。
不新做圖。

## 三、模擬（`tools/sim/apoc.js`）
- `spendDust()` 會買突破：照新表、圈數鎖住的不買。
- 跑三種玩家 × 三種子（SEED 20260913／14／15，`MARKS=1 TAP_SHARE=.5 START_MARKS=0 KEEP=1 SKILLS=1 LOOP=1`，14 天）：
  - 輕度 `20 3 14`、CPS 3：**兩週滿養（5 級）≤ 14 天那條線不能變差**（改前 §31：輕度走到第 2～3 圈；兩週滿養改前是 11／14／12，這個是鐵律）。
  - 重度 `60 3 14`、CPS 6 與 `120 3 14`、CPS 6：印 14 天走到第幾圈、輪迴滿養（10 級）第幾天、每圈用時。**目標：重度玩家 14 天內不能輪迴滿養**（要一直有卡可以養），而且圈數要比改前（第 16 圈）低——因為戰力 ×2 上限會讓牆往後退，若圈數反而變高，授權你調 `LAP.HP_GROWTH` 1.3 → 最高 1.4。
- 授權可調的只有：`TRANSCEND[5..9]` 的粉塵數（每級 ±50%）、`TRANSCEND_LAP`（每級 ±1 圈）、`LAP.HP_GROWTH`（1.3～1.4）。其他不動。改前改後的表都要印進 HANDOFF。

## 四、驗收必跑（全綠才算完成）
`npm test`（現 272；新單元測試：新表累計 87、圈數鎖、canTranscend 被鎖、autoGrow 不買被鎖的、isMaxed 語意不變＝5 級、isFullyMaxed、normalize 夾 10、溢出折萬用粉塵在 87）；
`clicker-v3-apoc-play.py`、`clicker-v3-apoc-loop.py`、`clicker-v3-growth.py`（養成軸——這支最重要，它驗的是兩週滿養那條線）、`clicker-v3-collect.py`、`clicker-ui-audit.py`。
新增 `tools/test/clicker-v3-apoc-transcend.py`：一張卡 5 級滿、laps 0 → 詳情寫「第 2 圈解鎖」沒鍵；把 laps 設 2 → 鍵出現、按下去變 6 級、粉塵扣 8；卡冊頂列出現「輪迴滿養 0/71」；圈末結算文字。

## 五、回報
`docs/clicker/HANDOFF-2026-09-14-r12.md` 新開「### 32. 突破上限跟圈數綁（2026-09-16）」：最終表、模擬改前／改後、改了哪些檔、測試結果；量不到／過不了的如實寫。

## 規矩
只改 `D:\claude\clawd-pet-balance`；不下載、不開 dev server、不 commit；`src/apoc/holo-special.css` 不碰；印記一枚不加。
