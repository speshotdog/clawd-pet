# 派工簡報：使用者裁決回來了，驗收標準換人（2026-09-11 第二輪）

你上一輪的驗收是 `VERDICT-2026-09-11-rarity-font.md`：**EXIT 1、不可 commit、裁決選 A**。
我全部接受，沒有辯解。**但使用者的回答把驗收標準整個換掉了**，所以先把新標準給你，
再請你重下一份指令。

**這一輪你仍然是指揮，我是執行。不要改程式碼、不要 build、不要 commit。**

---

## 一、使用者的三個回答（原文）

我問了你交代要問的三題，回答如下：

| 我問的 | 使用者原文 |
|---|---|
| 稀有度中英文是否都要跟卡名同字族？（附了候選版對照圖）| **「我要的是跟 卡池抽到的卡一模一樣」** |
| 要不要順便重訂字重與字距？ | **「同上一個回答」** |
| 禮物卡與 mtk 單卡是否納入？ | **「他是特別的 維持 並想辦法放入抽卡系統」** |

我的解讀（**如果你認為我解讀錯，直接否決**）：

1. **驗收標準從「卡名與稀有度同字族」換成「編隊／詳情的卡＝抽卡揭到的那張卡，一模一樣」。**
   使用者沒有要我們發明新的字體階級，他要的是**跟既有的抽卡卡面對齊**。
   所以字重字距問題自動消滅——抽卡那張是什麼樣，就是什麼樣。
2. **禮物卡（魔花少女）不要改它的外觀**（「他是特別的 維持」），
   但要**想辦法把它放進抽卡系統**。這是一件新任務，不是本輪的字型修正。
   ⚠ 它現在內嵌的是 996/984 bytes 空字型，「維持」跟「修字型」會不會衝突，請你判斷。

## 二、我照這個新標準先量了一次（`shots/rarity-font/parity.log`、`parity_probe.py`）

方法：同一張卡在三個畫面各抓一次，**把長度數值除以卡寬正規化**（三個畫面卡寬不同：
抽卡 198px、卡池 261.6px、編隊 180px），再逐屬性比對
`.face-name` 與 `.face-rarity` 的 fontFamily／fontSize／fontWeight／fontStyle／
letterSpacing／color／textShadow／backgroundImage／webkitTextFillColor／opacity／
textTransform／whiteSpace／寬高。Chromium 140、1440×900、DPR=1、等 `fonts.ready`。

### 抽卡 vs 卡池：已經一致

`beachball`、`zhenwang` 兩次獨立抽樣，差異只有 `fontSize` 正規化後第 5 位小數
（0.08278 vs 0.08276）與對應的文字寬——**是四捨五入，不是設計差異**。
`rar._h` 差 0.07364 vs 0.07167（約 0.2% 卡寬），我還沒查原因。

### 卡池 vs 編隊：**有真的差異，而且是顏色**

共同卡 10 張，逐張比對結果：

| 卡 | 差異 |
|---|---|
| `astronaut`（epic）| `rar.color` **卡池 `rgb(240,214,255)` vs 編隊 `rgb(184,117,255)`**、`webkitTextFillColor` 同、`rar._h` |
| `bianbiancaihua`（epic）| 同上 |
| `bingyang`（epic）| 同上 |
| `fluffdog`（epic）| 同上 |
| `alienkitty`（legendary）| 只有 `rar._h` |
| `foxfriend`（legendary）| 只有 `rar._h` |

也就是說：**epic 卡的稀有度文字顏色，編隊跟卡池／抽卡不一樣**——
編隊是飽和的紫（184,117,255），卡池／抽卡是淡紫（240,214,255）。
`rar._h`（文字高度／卡寬）全部 10 張都差 0.0716 vs 0.0694，約 0.22% 卡寬。

⚠ **這只是我的第一次掃描，覆蓋率不足**：mythic 與 rare／common 的共同卡我還沒比到
（`rocketdog`、`liulangyueshou`、`jiaotou`、`jinggou` 的結果被我截斷在前 6 張），
`.face-plate`／`.face-gem`／外框／箔面／卡背我完全沒比。
你上一輪點名的量測缺陷（`toIndex` 80 上限、沒帶 `isCustomFont`、
`rocketdog` 同 ID 兩張會互相吞掉、refit 沒穿 shadow root、抽卡三尺寸共同卡 0 的假 PASS）
**這支 parity 腳本同樣有**，我沒有先修就拿來掃，只當作探路。

## 三、目前的狀態

- 工作目錄有未 commit 的改動：`demo.html` 一行 CSS ＋ 被重生的 poolData／sceneData、
  九份重建產物、`shots/team-round3/build.json`、以及本輪的文件與證據。
- **照你的 EXIT 1，我沒有 commit。**
- 選 A 的十條建置順序我**還沒跑**——因為驗收標準換了，我不想在舊標準上再堆一層。

## 四、請你給的東西

1. **裁決我第一節的解讀對不對。** 錯了就直接改寫成你認為對的目標。
2. **把「一模一樣」翻成可量的定義。** 三個畫面卡寬本來就不同，
   所以不可能是像素相同。要比哪些屬性、哪些要正規化、哪些允許差、
   容差怎麼訂——照 `METHOD-card-face.md`「先量合格樣本再留餘裕」，不要憑感覺。
   ⚠ 上面那個 epic 顏色差異，要以哪一邊為準？（抽卡是使用者指名的參考，
   但我不知道編隊那個紫是不是刻意的設計，`DESIGN-team-and-map.md` 我還沒查。）
3. **本輪的執行順序**：選 A 的十條建置、parity 修正、量測腳本重寫，
   三者的先後與哪些要合併成一次。
4. **量測腳本的重寫規格**：把你上一輪點名的六個缺陷變成具體要求
   （逐節點 ID＋畫面角色＋`isCustomFont`、取消 80 上限、同 ID 多張要分開、
   refit 穿 shadow root、抽卡要能鎖定指定卡而不是隨機抽到什麼算什麼、
   證據路徑不能依賴我的 Temp）。
5. **禮物卡進抽卡系統**：這是新任務。請判斷它跟「維持外觀」有沒有衝突、
   要不要這輪碰，還是先寫成規格另案。**不要順手開工。**
6. 明確結論：這一輪的完成定義是什麼、什麼時候可以 commit。

繁體中文，明確結論不要模稜兩可。
