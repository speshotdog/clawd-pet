複檢範圍：`HEAD~1..HEAD`（`5846f36`）。所有檔案均由 `git show HEAD:路徑` 讀取，未修改工作目錄。

### 必修

**1. 離線入帳存檔失敗後，收益會被後續 tick 永久吃掉。**

位置：[clicker-apoc-ui.js:96](D:/claude研究/clawd-pet-v3/src/clicker-apoc-ui.js:96)、[clicker-apoc-economy.js:384](D:/claude研究/clawd-pet-v3/src/clicker-apoc-economy.js:384)。相關重試入口：[clicker.js:711](D:/claude研究/clawd-pet-v3/src/clicker.js:711)。

重現步驟：

1. 使用有戰力、`seenAt` 為八小時前的末世存檔。
2. 模擬儲存空間不足，使回前景時的離線入帳 `commit` 失敗。
3. 恢復儲存能力，按「重試存檔」，保持在末世。
4. 等待下一個 tick：`settle` 更新 `seenAt`，但沒有重新補發離線收益。

**故障注入實測：應補發 11,200 金幣，實際只有下一秒的 0.6 金幣；收據零次，離線基準已更新。** 必須保留待結算狀態，成功補發後才能恢復會更新 `seenAt` 的線上結算。

### 值得修

- **派遣倒數停在開啟詳情時的數字。** [clicker-album.js:313](D:/claude研究/clawd-pet-v3/src/clicker-album.js:313) 只在建構按鈕時計算分鐘；`stateKey` 不包含時間，持續開著詳情會一直顯示「120 分後回來」，直到派遣收回才更新。建議每分鐘就地更新文字。
- **派遣收益需補入平衡模擬。** 現有模擬重現一般玩家 **179 → 158 分鐘**，但沒有模擬派遣。相同設定第一場 20 分鐘收入約 **102 萬**；結束於第 10 站時，八小時離線約 **354 萬**，三個精良派遣合計約 **678 萬**。全部投入全隊訓練，可分別從 Lv.19 升到 Lv.29／31。這不直接證明失衡，但目前「少一成左右」的證據不足以涵蓋派遣。

### 可以不修

- 正常載入、桌邊提交與其他末世動作不會提早刷新 `seenAt`；`enter`／`resume` 會重設 tick 基準，連續前景事件未見重複發錢。
- `offline` 本身不改進度、血量、wave、失敗紀錄、冷卻或刷怪時間。逾期王關在回來後第一個 tick 判輸，與現有期限規則一致。
- 派遣中的重複抽卡不會自動入隊；編隊／技能限制有效；重走保留派遣；正常收回不會重複領獎，提交失敗也不會先移除任務。
- 徽章的重走／無盡判定及非法 `laps` 回退處理未見新增問題；新增徽章使用字章，派遣 UI 未引入 1.0 卡面。
- 第十輪 C 的三項修正，程式路徑檢查未見回歸。

**驗證：HEAD 末世經濟測試 40／40 通過，另完成故障注入與經濟試算。手機 390×844／桌機版面僅做靜態檢查，未實際渲染，不列為視覺驗收通過。**