# 珍母點點：第三輪實作報告

本輪以 `b0f38b6` 為基底；開工第一個命令為 `git log -4`，依序確認 `b0f38b6`、`05c5bf5`、`8ff870b`、`3961701`。使用最新提交已提供的素材，沒有另生成或改寫 PNG。

## 完成內容

- 點擊熱區的 pointerup 取得 clientX/Y，依 game 的實際 bounding rect 換回 960×640 座標，涵蓋 zoomer 縮放與畫面位移。只有真正的指標 click 使用該座標；Space、Enter、程式 click 回到新袋口 `(485,240)`。
- 浮字在同落點上方 12px，水平隨機 ±10px；每個通過原有輸入節流的點擊保留自己的浮字。60ms 放大彈出，連點 22px 金黃，重擊 26px 並回正旋轉。
- 保留上輪普通 8／連點 12／重擊 18 的原配方、10–22px 碎紙、噴射速度、壽命、翻面暗色及重擊光痕。另加連點小閃一顆、重擊 sprite 3 的 36→90 圓環（250ms、lighter）。包裝彈跳與五狀態切換流程保留。
- 珍母與熱區 left=120；包裝 left=392、top=150、高176。接觸影、寄生標籤與量尺一起移位；量尺中心對齊包裝中心，旗子和膠帶保留。
- 外框使用透明中心 9-slice，底層保留 kraft；名牌、金幣牌、價格票券、五色厚按鈕及三種小貼紙皆接上提供素材。按鈕由圖內描邊和硬陰影呈現厚度，按下整顆下移3px並裁去底部3px。
- 商店改為40px圖示欄、粉／紫標題帶、說明、虛線、價格票券與操作列。升級列為「票券／最多／升級！」；招募票券顯示單抽價，招募與五連採紫色按鈕。不足金額仍可從操作 title 查看，禁用邏輯保留。
- 我的夥伴加愛心標籤，保留圓形頭像與分頁；名冊移到頁尾，與統計、存檔狀態牌形成 cream 紙板列。招募頂列套用紙牌、名牌與同款按鈕，桌面保留深色。
- 餘額每幀逼近目標25%，一段動畫最遲約300ms落到目標；1Hz放置結算使用相同路徑。增加時數字140ms彈跳、硬幣回正；支出200ms粉紅回黑。動畫重設而不排隊。
- 每次／每秒速率和升級預覽一起改成180ms舊值淡出、新值由下方6px滑入。價格票券購買時縮至.96。
- 顯示層改為萬／億格式，餘額、速率、票券以title保留詳細數值。經濟、價格、存檔純邏輯未改。
- 量尺補上tween，跨包先到滿值、下一幀歸零，再追上最新進度；沒有變化時不啟動新的數字／量尺動畫。停止流程取消新加的rAF；既有角色呼吸、眨眼沿用。

## 驗收

- `node --check`：所有 `src/clicker*.js` 通過。
- `npm.cmd test`：29/29通過（PowerShell的npm.ps1受執行原則限制，因此使用同一npm的.cmd入口）。
- 瀏覽器：`python tools/test/clicker-browser.py` 全部PASS。使用Playwright route直接讀本機素材，沒有啟動server。結果與截圖見 `tools/test/clicker-artifacts/`。
- 新場景包含：熱區左上／右下在1倍和0.75倍縮放的真實滑鼠輸入、粒子出生點與浮字位置斷言、鍵盤袋口回退、大額增加／支出的tween中途截圖與收斂斷言、萬／億邊界與title、量尺滿值再歸零、商店新結構、招募頂列、九位夥伴第二頁。
- 保留原有節流、教學、技能、五種招募、pending恢復、儲存失敗、隱藏恢復與共用卡面CSS等價驗證。修正原有入隊截圖測試的時序：先等飛行元素出現再截圖，避免截图結束後才檢查短動畫。

## 與範例仍有的差距、未完成與限制

- 紙板構圖與元件層級已接近範例，但不能聲稱已完全達到「乍看同一個遊戲」：提供的素材線條較規整，部分素材有可見格線，少了範例粗糙手繪筆觸與紙纖維細節。本輪沒有重畫素材。
- 依指定保留系統黑體；在本機截圖中中文fallback的字形與範例手寫字明顯不同。960×640還需要容納三個技能槽，夥伴區比範例更密。
- 票券區寬度有限，五連按鈕採小字；遊戲內招募支援兩種抽法，資訊量比範例多。存檔狀態是紙板外觀的狀態文字，沒有假裝成可操作按鈕。
- 新增動畫遵循減少動態偏好；碎紙沿用既有特效系統。未宣稱重製Egg, Inc.或Tap Titans的實際動畫曲線。本輪參考 [Cookie Clicker官方頁面](https://orteil.dashnet.org/cookieclicker/) 的總量／每秒速率／價格分層，動畫參數依本次明確規格實作。
- 測試穩定性：最後調整按鈕切片後的一輪重跑，普通粒子數量斷言曾偶發失敗；補上事件／存檔／時鐘診斷後，完整重跑PASS，但尚未取得該次失敗的根因，不能宣稱此偶發已修復。
- 未進行原生Tauri實機關窗重開測試；`8ff870b`的clicker-zoom／focus與冪等resume內容未改動，瀏覽器既有隱藏恢復回歸仍保留。
- 未build、未起server、未commit；未改src-tauri、pet.js、index.html、menu或共用gacha檔案。


