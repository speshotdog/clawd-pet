# 複檢請求：末世（2.0）第六輪——王變門檻、輸了刷前一站、訓練改乘算（balance-v3，對 `86a9bea` 的未 commit 工作樹）

你是唯讀複檢。請讀 `git diff 86a9bea`，找出**會讓玩家看到錯誤、卡關卡死、丟進度、被鑽漏洞、或讓 1.0 被 2.0 的改動弄壞**的問題。
結論分成「必修」「值得修」「不用修（說明為什麼）」，每條附檔案與行號、重現步驟、建議修法。用繁體中文。

## 使用者這一輪說了什麼與定案

1. 「關卡的難度幫我也同步，不然會造成王太簡單、路上太難」（上一輪王血只有一般站 ×0.04）。
2. 選「王變門檻：打不過就去抽卡／練」，「對照之前的參考遊戲（Sakura Clicker），用等比的方式把時長降低」，時長選「約 1 小時」。
3. 詳細規則、數字、模擬結果見 `docs/clicker/HANDOFF-2026-09-13-v3.md` 〇之十。

## 改了哪些

| 檔案 | 內容 |
|---|---|
| `src/clicker-apoc-economy.js` | `BASE_NEED 5000`、`GROWTH 2.1`、`BOSS_MULS [2,3,4,5,6]`（取代 `BOSS_MUL`）、`REWARD_SHARE .08`＋`REWARD_GROWTH 1.066`；訓練 `trainMul` 改 `(1+MUL)^L`、`TRAIN` 500／300 × 1.3^L；`fight(a, now, farm)`、`canFarm`、`canFight` 允許換掉刷怪戰鬥；`settle` 刷怪勝利發 `farm` 事件、不推進度；`normalize` 驗刷怪戰鬥合法性 |
| `src/clicker-apoc-ui.js` | `autoFight` 王輸過就自動刷前一站；刷怪中顯示「再次挑戰」、血條「刷怪中」、失敗說明／通知文案；刷怪打死也演擊倒；訓練預覽改乘算 |
| `tools/sim/apoc.js` | 門檻版玩家模型（刷怪、戰力推估夠了才再挑戰、先抽卡再買最便宜的訓練）、`SEED`、打王一定會點 |
| 測試 | 單元：訓練乘算、刷怪流程（沒輸過不能刷、重新載入保留、壞資料丟掉、冷卻中不能挑戰、刷怪勝利不推進度、再次挑戰滿血）、王血逐隻遞增；`clicker-v3-apoc-play.py`：輸了自動刷怪、血條寫刷怪中、再次挑戰滿血；`clicker-v3-apoc.py`：扣血檢查容許換站 |

## 請特別看

1. **刷怪狀態機**：刷怪中按「再次挑戰」、冷卻中按、刷怪中切到地圖／1.0／招募層再回來、重新整理、存檔編輯（`farm:true` 但 index 或 bossFailed 不對）、刷怪中剛好冷卻結束；會不會卡在「沒有戰鬥也不自動開打」或「一直刷、按鈕消失」。
2. **刷怪能不能被鑽**：刷怪一場最快一秒一場，獎勵是前一站的 ×0.08×1.066^站——有沒有辦法不輸王也進刷怪（例如先輸一次王之後永遠留在刷怪，會不會比推進度划算、或讓後面的站變簡單到失去意義）；`bossFailed` 在贏了王之後有沒有清掉。
3. **舊存檔**：上一版存檔的 `teamLevel`／`clickLevel` 在乘算下會變成多少倍（線性 ×1.03 的 Lv10 → 乘算 ×2.59）；進行中的戰鬥換算；`BOSS_MUL` 被拿掉後有沒有地方還在讀。
4. **數值溢位與顯示**：乘算訓練到高等級 `1.1^L`、`1.3^L` 價格、`GROWTH 2.1^19` 的血量——`format`、`Math.round`、訓練「最多」的 1000 級上限迴圈會不會卡或出現 Infinity／NaN。
5. **畫面**：刷怪中的挑戰鍵與失敗說明紙條同時出現會不會擋點擊、直式縮放會不會擠壞；刷怪一秒一隻時擊倒演出、音效、通知會不會洗版。

## 驗收（我這邊跑過的）

見本輪回報：`npm test`、`clicker-v3-apoc.py`、`clicker-v3-apoc-play.py`、UI 體檢、模擬器三個種子。
