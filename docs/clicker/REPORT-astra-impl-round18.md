# 第十八輪實作報告

日期：2026-09-08；範圍：`BRIEF-astra-impl-round18.md` 兩章。

- **平均訓練邏輯**：新增 `trainAll(state, now)`，每輪依當前等級低到高、同級依角色目錄順序，沿用 `train()` 逐級購買，跳過未招募／200 級／買不起者，無購買即停止，每次最多 500 級並回傳總級數、花費與各夥伴級數。
- **卡冊按鈕**：粉塵罐旁新增「平均訓練」，以所有可訓練夥伴各升一級的總價顯示「約／需 X 幣」，不足整輪、無可訓練夥伴或存檔阻擋時停用，餘額結算時即時更新。
- **操作與里程碑**：一次操作只 commit 一次，成功後播放 upgrade 音效、顯示人數／級數／花費並重繪卡冊與已開展示頁，25／50／75／100 技能副軸沿用既有等級計算，單人「訓練／最多」保留。
- **縮放與置中**：`applyZoom` 將縮放比向下取 0.25 倍數、下限 0.75，網頁置中採實際取整後比例，Tauri 接收縮放值亦經相同處理，Rust 未修改。
- **貼圖寬度**：檢查 src 的 border-image 寬度，標題帶、幣牌、票券等奇數 px 寬度改為偶數並同比例微調切片，素材檔未修改。
- **單元測試**：`npm.cmd test` exit code 0，142 tests／142 pass／0 fail／0 skipped，新增五例覆蓋低等級優先、精確預算、不修改輸入、餘額不足、跳過滿級與買不起者、500 級上限及各角色技能里程碑與單人訓練一致。
- **瀏覽器驗收**：沿用工作區原有未追蹤的 `tools/test/clicker-round18.py`，結果 ALL PASS，實際按鈕使總等級由 1500 增至 2000，無 pageerror。
- **四尺寸截圖**：執行 `python _art/shot.py` 的 2560×1215、1920×1080、1280×860、1366×768 四組，皆 `errors []`，透過 Playwright route 讀取 src，未啟動 server。
- **角落檢視**：四尺寸縮放比分別為 1.75／1.5／1.25／1，已檢視卡冊標頭與按鈕、紙框四角放大圖，完整截圖前綴為 `_art/out/r18-*`、`r18-1920-*`、`r18-1280-*`、`r18-1366-*`。

| 解析度 | 3 倍角落圖（上排按鈕、下排紙框；左上／右上／左下／右下） |
|---|---|
| 2560×1215 | `_art/out/r18-corner-verified-2560.png` |
| 1920×1080 | `_art/out/r18-corner-verified-1920.png` |
| 1280×860 | `_art/out/r18-corner-verified-1280.png` |
| 1366×768 | `_art/out/r18-corner-verified-1366.png` |

- **保留事項**：`WISH_TELEGRAPH=false`、切入 `T`／`EASE`、點空白關面板、卡冊箭頭、轉彩 30% 與第十七輪既有行為未改，未 build、未起 server、未 commit、未修改素材或精簡演出。
- **文字與差異檢查**：環境無 rg／grep，以 `Select-String -SimpleMatch '??'` 檢查 src JS／HTML，命中均為既有空值運算子；中文以 UTF-8 儲存，無 U+FFFD，`git diff --check` 通過。
- **環境限制**：`.git` 為唯讀，未執行交接文件中的 fetch／pull；本輪未執行 Tauri 桌面版實機驗證。
